# Generated by scripts/generate_builtin_executor_contracts.py.
manifest_format = "1.0"
executor_standard = "metnos.executor/1.0"
name = "delete_tasks"
version = "1.0.0"
author = "Metnos builtin maintainers"
affinity = ["task", "cancella", "elimina", "rimuovi", "delete", "remove"]
platforms = ["linux"]
revertible = false
lifecycle = "active"

[description]
it = "SCOPO: Elimina task schedulati identificati per id o nome. PATTERN: richiesta naturale tipizzata dal planner. NON: ampliare autorita o inventare input. OUT: risultato JSON dichiarato."
en = "SCOPO: Deletes scheduled tasks identified by id or name. PATTERN: natural request typed by the planner. NON: enlarge authority or invent inputs. OUT: declared JSON result."

[code]
files = ["../../recurring_tasks.py"]
digest = "sha256:d1c4834cd77ca470da36f8c0ade970bafbb6c1db56dd0516261ebb561ad9cdd4"

[args]
type = "object"
required = []

[args.properties.id]
type = "integer"

[args.properties.id.description]
it = "ID numerico univoco del task. PREFERITO se l'utente cita un numero o se ci sono task con nomi simili."
en = "Known stable identifier."

[args.properties.name]
type = "string"

[args.properties.name.description]
it = "Slug del task (es. 'check_posta_mattutina'). Usa solo se l'id non e' disponibile."
en = "Known stable name."

[output]
schema_inline = "{ok: bool, results?: Array<object>, error?: str, error_class?: str, error_code?: str}"

[[capabilities]]
name = "metnos:write"
hint = ["scheduled_tasks"]

[placement]
scope = "server"
device_ok = false

[[tests]]
name = "builtin_contract_domain_suite"
reference = "runtime/tests/test_builtin_executor_contracts.py"
