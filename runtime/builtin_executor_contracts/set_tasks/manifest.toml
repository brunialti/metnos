# Generated by scripts/generate_builtin_executor_contracts.py.
manifest_format = "1.0"
executor_standard = "metnos.executor/1.0"
name = "set_tasks"
version = "1.0.0"
author = "Metnos builtin maintainers"
affinity = ["task", "abilita", "disabilita", "pausa", "enable", "disable", "fire"]
platforms = ["linux"]
revertible = false
lifecycle = "active"

[description]
it = "SCOPO: Aggiorna stato o fire controllato di un task schedulato. PATTERN: richiesta naturale tipizzata dal planner. NON: ampliare autorita o inventare input. OUT: risultato JSON dichiarato."
en = "SCOPO: Updates state or performs a controlled fire of a scheduled task. PATTERN: natural request typed by the planner. NON: enlarge authority or invent inputs. OUT: declared JSON result."

[code]
files = ["implementation.py.src"]
digest = "sha256:adb2daa5d236237981dc126a08e7c788a1834f60e35bec278d3361a961a2ad43"

[args]
type = "object"
required = ["name"]

[args.properties.name]
type = "string"

[args.properties.name.description]
it = "Nome del task (slug)."
en = "Known stable name."

[args.properties.enabled]
type = "boolean"

[args.properties.enabled.description]
it = "true=abilita, false=disabilita. Mutex con fire_now."
en = "Whether the resource is enabled."

[args.properties.fire_now]
type = "boolean"

[args.properties.fire_now.description]
it = "true=fire immediato. Mutex con enabled. Solo HOST."
en = "Request one controlled immediate task fire."

[output]
schema_inline = "{ok: bool, results?: Array<object>, task?: object, error?: str, error_class?: str, error_code?: str}"

[[capabilities]]
name = "metnos:write"
hint = ["scheduled_tasks"]

[placement]
scope = "server"
device_ok = false

[[tests]]
name = "builtin_contract_domain_suite"
reference = "tests/runtime/skills/test_builtin_executor_contracts.py"
