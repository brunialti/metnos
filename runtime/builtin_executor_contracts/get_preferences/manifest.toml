# Generated by scripts/generate_builtin_executor_contracts.py.
manifest_format = "1.0"
executor_standard = "metnos.executor/1.0"
name = "get_preferences"
version = "1.0.0"
author = "Metnos builtin maintainers"
affinity = ["preferenza", "preferenze", "impostazioni", "preference", "preferences", "settings", "elenco", "list"]
platforms = ["linux"]
revertible = false
lifecycle = "active"

[description]
it = "SCOPO: Elenca le preferenze personali impostate dall'utente. PATTERN: get_preferences(). NON: non elenca i task schedulati (list_tasks), le skill installate (list_skills) o il profilo anagrafico (read_persons). OUT: entries=[{key, value, origin, applied}] piu' `available` con tutto cio' che si puo' impostare."
en = "SCOPO: Lists the personal preferences the user has set. PATTERN: get_preferences(). NON: it does not list scheduled tasks (list_tasks), installed skills (list_skills) or the person profile (read_persons). OUT: entries=[{key, value, origin, applied}] plus `available` with everything that can be set."

[code]
files = ["../../user_preferences.py"]
digest = "sha256:d5413ddb48d76b97f06130d3bf85ba03430b7c55b94589ad6b0f949698117c37"

[args]
type = "object"
required = []

[output]
schema_inline = "{ok: bool, entries: Array<object>, available: Array<object>, note?: str, error?: str, error_class?: str, error_code?: str}"

[[capabilities]]
name = "metnos:read"
hint = ["user_prefs"]

[placement]
scope = "server"
device_ok = false

[[tests]]
name = "builtin_contract_domain_suite"
reference = "tests/runtime/skills/test_builtin_executor_contracts.py"
