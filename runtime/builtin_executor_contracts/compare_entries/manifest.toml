# Generated by scripts/generate_builtin_executor_contracts.py.
manifest_format = "1.0"
executor_standard = "metnos.executor/1.0"
name = "compare_entries"
version = "1.0.0"
author = "Metnos builtin maintainers"
affinity = ["simile", "similarità", "distanza semantica", "confronta", "dedup", "piu vicino", "vicinanza", "ranking semantico", "similar", "semantic distance", "nearest", "rank by similarity", "compare"]
platforms = ["linux"]
revertible = false
lifecycle = "active"

[description]
it = "SCOPO: Confronta entries in memoria con una metrica semantica bounded. PATTERN: richiesta naturale tipizzata dal planner. NON: ampliare autorita o inventare input. OUT: risultato JSON dichiarato."
en = "SCOPO: Compares in-memory entries with a bounded semantic metric. PATTERN: natural request typed by the planner. NON: enlarge authority or invent inputs. OUT: declared JSON result."

[code]
files = ["../../compare_entries.py"]
digest = "sha256:bf1fef5f6f10881e3bb01c0514eac484a46c072ab21e31dcbf2d934846d316b1"

[args]
type = "object"
required = ["reference"]

[args.properties.reference]
type = "string"

[args.properties.reference.description]
it = "Testo input con cui confrontare i candidati. Es. il testo di una issue nuova."
en = "Reference text or entry used for comparison."

[args.properties.entries]
type = "array"

[args.properties.entries.description]
it = "Candidati (dict). Di norma via `from_step=N` top-level: il runtime espande in `entries`."
en = "Input entries already available in memory."

[args.properties.field]
type = "string"

[args.properties.field.description]
it = "Campo-testo dei candidati da confrontare. Omesso = auto (title/summary/text/...). Se un candidato ha `embedding`, quello vince."
en = "Entry field used by the operation."

[args.properties.order]
type = "string"
enum = ["desc", "asc"]

[args.properties.order.description]
it = "Ordinamento per similarity. Default 'desc' (piu' simili prima)."
en = "Fields used for deterministic ordering."

[args.properties.top_n]
type = "integer"

[args.properties.top_n.description]
it = "Cap: tieni i primi N per similarity (§2.7)."
en = "Maximum number of top-ranked entries."

[args.properties.min_similarity]
type = "number"

[args.properties.min_similarity.description]
it = "Soglia: scarta i candidati sotto questa similarity (0..1). Default nessuna."
en = "Minimum accepted similarity score."

[output]
schema_inline = "{ok: bool, entries: Array<object>, used?: int, error?: str, error_class?: str, error_code?: str}"

[[capabilities]]
name = "compute:pure"
hint = ["semantic_similarity"]

[placement]
scope = "server"
device_ok = false

[[tests]]
name = "builtin_contract_domain_suite"
reference = "runtime/tests/test_builtin_executor_contracts.py"
