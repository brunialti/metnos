# Generated by scripts/generate_builtin_executor_contracts.py.
manifest_format = "1.0"
executor_standard = "metnos.executor/1.0"
name = "admin"
version = "1.0.0"
author = "Metnos builtin maintainers"
affinity = []
platforms = ["linux"]
revertible = false
lifecycle = "active"

[description]
it = "SCOPO: Esegue un comando shell di sistema privilegiato solo attraverso il vaglio. PATTERN: richiesta naturale tipizzata dal planner. NON: ampliare autorita o inventare input. OUT: risultato JSON dichiarato."
en = "SCOPO: Runs one privileged system shell command only through the approval gate. PATTERN: natural request typed by the planner. NON: enlarge authority or invent inputs. OUT: declared JSON result."

[code]
files = ["../../system/admin.py"]
digest = "sha256:d797d014e29aadf8dbd46569c4799a0b83dcff420a860bd070879ae8cd6ecc7a"

[args]
type = "object"
required = ["intent", "command_proposed"]

[args.properties.intent]
type = "string"

[args.properties.intent.description]
it = "Descrizione naturale (1 frase, IT o EN) di cosa l'utente vuole fare a livello di sistema. Es. \"montare lo share CIFS del NAS sotto /mnt/nas\". NO null/vuoto."
en = "Natural-language description of the requested system operation."

[args.properties.command_proposed]
type = "string"

[args.properties.command_proposed.description]
it = "argv di UN comando shell, token separati da spazi. DEVI: comando singolo. Path assoluti letterali. Placeholder `${METNOS_CIFS_CREDS}` per credenziali. UID hardcoded (es. `uid=1000`). NON DEVI: `&&`, `||`, `;`, `|`, `>`, `<`, `$()`, backtick. Es: \"mount -t cifs //192.0.2.20/Public /mnt/nas -o credentials=${METNOS_CIFS_CREDS},uid=1000\"."
en = "Single proposed command argv string; no shell chaining."

[args.properties.credentials_domain]
type = "string"

[args.properties.credentials_domain.description]
it = "Opzionale. Nome dominio credenziali salvate, formato \"<binding>_<host>\" (es. \"cifs_192.0.2.20\"). Default auto-derivato dal command_proposed quando omesso."
en = "Optional credential binding domain resolved by runtime."

[args.properties.actor_consent_token]
type = "string"
runtime_resolved = true

[args.properties.actor_consent_token.description]
it = "NON impostare manualmente. Il runtime lo inietta al rilancio dopo conferma utente sulla carta vaglio. Quando presente, admin salta la carta e procede a sudoer."
en = "Opaque runtime-owned consent token."

[output]
schema_inline = "{ok: bool, decision?: str, argv?: Array<str>, error?: str, error_class?: str, error_code?: str}"

[[capabilities]]
name = "system:admin"
hint = ["mount", "systemctl", "chmod", "chown", "apt"]

[placement]
scope = "server"
device_ok = false

[[tests]]
name = "builtin_contract_domain_suite"
reference = "runtime/tests/test_builtin_executor_contracts.py"
