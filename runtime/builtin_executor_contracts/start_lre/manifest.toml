# Generated by scripts/generate_builtin_executor_contracts.py.
manifest_format = "1.0"
executor_standard = "metnos.executor/1.0"
name = "start_lre"
version = "1.0.0"
author = "Metnos builtin maintainers"
affinity = ["start_lre", "profilo lre registrato", "registered lre profile"]
platforms = ["linux"]
revertible = false
lifecycle = "active"

[description]
it = "SCOPO: Avvia, per compatibilità tecnica, un piano LRE già registrato. PATTERN: start_lre(profile=\"images.questions.v1\", paths=[\"/percorso/assoluto\"]). USA: soltanto integrazioni che conoscono già il profilo registrato. NON: non usarlo per decidere se un’attività debba usare LRE; il normale motore affida automaticamente le azioni lunghe ammissibili. OUT: identificativo, stato, numero di sorgenti e collegamento alla console LRE."
en = "SCOPO: Starts an already registered LRE plan for technical compatibility. PATTERN: start_lre(profile=\"images.questions.v1\", paths=[\"/absolute/path\"]). USE: only integrations that already know the registered profile. NON: do not use it to decide whether work should use LRE; the normal engine automatically submits eligible long actions. OUT: identifier, state, source count, and LRE console link."

[code]
files = ["../../lre_submission.py"]
digest = "sha256:e2819eed3a4c73f25bec4465e8573a7e90adab7d6324ef46dffd1f909fd7634d"

[args]
type = "object"
required = ["profile", "paths"]

[args.properties.profile]
type = "string"
enum = ["images.questions.v1"]

[args.properties.profile.description]
it = "Profilo LRE registrato adatto al risultato richiesto."
en = "Exact registered LRE profile selected for the requested result."

[args.properties.paths]
type = "array"
minItems = 1
maxItems = 1024

[args.properties.paths.description]
it = "File o cartelle locali, espressi con percorsi assoluti."
en = "Absolute local files or directories to include."

[args.properties.paths.items]
type = "string"

[output]
schema_inline = "{ok: bool, decision?: 'accepted', workload_id?: str, revision_id?: str, state?: str, expected_sources?: int, plan_summary?: {stage_count: int, required_stage_count: int}, limits?: object, status_url?: str, final_message_hint?: str, error?: str, error_class?: str, error_code?: str}"

[[capabilities]]
name = "metnos:write"
hint = ["lre"]

[execution]
effect = "create_only"
parallelism_class = 0
resource_class = "local_io"
concurrency_key = "none"
equivalence_gate = "unverified"

[placement]
scope = "server"
device_ok = false

[[tests]]
name = "builtin_contract_domain_suite"
reference = "tests/runtime/skills/test_builtin_executor_contracts.py"
