# Generated by scripts/generate_builtin_executor_contracts.py.
manifest_format = "1.0"
executor_standard = "metnos.executor/1.0"
name = "start_lre"
version = "1.0.0"
author = "Metnos builtin maintainers"
affinity = ["lre", "long run", "lavoro lungo", "in background", "riprendi dopo riavvio", "intero corpus", "tutta la cartella", "attività prolungata", "resume after restart", "whole folder", "large corpus", "molti file", "many files"]
platforms = ["linux"]
revertible = false
lifecycle = "active"

[description]
it = "SCOPO: Affida a LRE una richiesta lunga basata su un profilo registrato. PATTERN: start_lre(profile=\"images.questions.v1\", paths=[\"/percorso/assoluto\"]). USA: corpus ampio o attività oltre il turno. NON: letture brevi, sorgenti remote o input inventati. OUT: identificativo, stato, numero di sorgenti e collegamento alla console LRE."
en = "SCOPO: Submits a long-running request to LRE through a registered profile. PATTERN: start_lre(profile=\"images.questions.v1\", paths=[\"/absolute/path\"]). USE: large corpora or work beyond the turn. NON: short reads, remote sources, invented profiles or paths. OUT: identifier, state, source count, and LRE console link."

[code]
files = ["../../lre_submission.py"]
digest = "sha256:4867f561b7ba8910b696657f73a1c3666f71ae38e96979d2b1818715c1bba1dd"

[args]
type = "object"
required = ["profile", "paths"]

[args.properties.profile]
type = "string"
enum = ["images.questions.v1"]

[args.properties.profile.description]
it = "Profilo LRE registrato adatto al risultato richiesto."
en = "Exact registered LRE profile selected for the requested result."

[args.properties.paths]
type = "array"
minItems = 1
maxItems = 1024

[args.properties.paths.description]
it = "File o cartelle locali, espressi con percorsi assoluti."
en = "Absolute local files or directories to include."

[args.properties.paths.items]
type = "string"

[output]
schema_inline = "{ok: bool, decision?: 'accepted', workload_id?: str, revision_id?: str, state?: str, expected_sources?: int, plan_summary?: {stage_count: int, required_stage_count: int}, limits?: object, status_url?: str, final_message_hint?: str, error?: str, error_class?: str, error_code?: str}"

[[capabilities]]
name = "metnos:write"
hint = ["lre"]

[execution]
effect = "create_only"
parallelism_class = 0
resource_class = "local_io"
concurrency_key = "none"
equivalence_gate = "unverified"

[placement]
scope = "server"
device_ok = false

[[tests]]
name = "builtin_contract_domain_suite"
reference = "tests/runtime/skills/test_builtin_executor_contracts.py"
