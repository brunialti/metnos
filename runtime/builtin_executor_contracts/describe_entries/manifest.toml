# Generated by scripts/generate_builtin_executor_contracts.py.
manifest_format = "1.0"
executor_standard = "metnos.executor/1.0"
name = "describe_entries"
version = "1.0.0"
author = "Metnos builtin maintainers"
affinity = []
platforms = ["linux"]
revertible = false
lifecycle = "active"

[description]
it = "SCOPO: Sintetizza entries in memoria entro budget dichiarati. PATTERN: richiesta naturale tipizzata dal planner. NON: ampliare autorita o inventare input. OUT: risultato JSON dichiarato."
en = "SCOPO: Summarizes in-memory entries within declared budgets. PATTERN: natural request typed by the planner. NON: enlarge authority or invent inputs. OUT: declared JSON result."

[code]
files = ["../../describe_entries.py"]
digest = "sha256:23930d87742618f2632912f6ee1aa9cea708eae0d663e5ccf2d6cc347d0b67d6"

[args]
type = "object"
required = ["from_step"]

[args.properties.from_step]
type = "integer"
minimum = 1

[args.properties.from_step.description]
it = "Numero dello step precedente (in questo turno) che ha prodotto la lista da riassumere. Es. se al passo 1 hai chiamato read_messages, qui passi from_step=1."
en = "Previous producer step supplying entries."

[args.properties.style]
type = "string"
enum = ["by_importance", "by_relevance", "compact"]

[args.properties.style.description]
it = "Preset di prompt: 'by_importance' (default), 'by_relevance' (richiede context), 'compact'."
en = "Requested summary style."

[args.properties.context]
type = "string"

[args.properties.context.description]
it = "Per style='by_relevance': la richiesta originale dell'utente, da usare come metro di pertinenza."
en = "Optional summary context."

[args.properties.group_by]
type = "string"

[args.properties.group_by.description]
it = "Chiave di raggruppamento RICHIESTA dall'utente (es. 'mailbox', 'mittente', 'size'): l'output viene organizzato in sezioni/ordine per quella chiave e VINCE sul raggruppamento intrinseco per tema. Risolta sul campo reale delle entries."
en = "Optional grouping field."

[args.properties.data_kind]
type = "string"

[args.properties.data_kind.description]
it = "Tipo semantico delle entries (es. 'email', 'web_result', 'log_line', 'file'). Se omesso, viene dedotto dai campi 'kind' delle entries o euristicamente. Sovrascrive il 'kind' per-entry nel prompt."
en = "Input data kind hint."

[args.properties.format]
type = "string"
enum = ["markdown", "html", "plain", "bullet_list", "json"]

[args.properties.format.description]
it = "Formato di output desiderato. 'markdown' (default, leggero, Telegram-friendly), 'html' (Telegram parse_mode HTML), 'plain' (no markup), 'bullet_list' (solo elenco puntato), 'json' (oggetto strutturato)."
en = "Requested output format."

[args.properties.tier]
type = "string"
default = "auto"
enum = ["auto", "fast", "middle", "wise"]

[args.properties.tier.description]
it = "Tier LLM da usare. Default 'auto': sceglie fast/middle/wise in base alla dimensione del bundle (entries corte → fast, medie → middle, lunghe o numerose → wise). Override esplicito solo se sai che serve."
en = "Runtime model tier selector."

[args.properties.max_tokens]
type = "integer"
default = 600

[args.properties.max_tokens.description]
it = "Tetto per l'output del LLM. Default 600."
en = "Maximum model output tokens."

[output]
schema_inline = "{ok: bool, content?: str, entries?: Array<object>, truncated?: bool, error?: str, error_class?: str, error_code?: str}"

[[capabilities]]
name = "llm:local"
hint = ["summarization"]

[[capabilities]]
name = "llm:online"
hint = ["summarization"]

[placement]
scope = "server"
device_ok = false

[[tests]]
name = "builtin_contract_domain_suite"
reference = "runtime/tests/test_builtin_executor_contracts.py"
