# Generated by scripts/generate_builtin_executor_contracts.py.
manifest_format = "1.0"
executor_standard = "metnos.executor/1.0"
name = "write_entries"
version = "1.0.0"
author = "Metnos builtin maintainers"
affinity = ["store", "archivio", "archivi", "raccolta", "collezione", "registro dati", "database interno", "collection", "datastore", "memorizza nello store", "salva nell'archivio"]
platforms = ["linux"]
revertible = false
lifecycle = "active"

[description]
it = "SCOPO: Scrive o aggiorna record in uno store dichiarato. PATTERN: richiesta naturale tipizzata dal planner. NON: ampliare autorita o inventare input. OUT: risultato JSON dichiarato."
en = "SCOPO: Writes or updates records in a declared store. PATTERN: natural request typed by the planner. NON: enlarge authority or invent inputs. OUT: declared JSON result."

[code]
files = ["../../store_entries.py"]
digest = "sha256:0502ad6dc5a70da4ac3c60f62571e4e81c02cf32711227981b8ced9123679fbf"

[args]
type = "object"
required = ["store", "from_step"]

[args.properties.store]
type = "string"

[args.properties.store.description]
it = "Nome dello store su cui scrivere."
en = "Name of a declared runtime store."

[args.properties.from_step]
type = "integer"
minimum = 1

[args.properties.from_step.description]
it = "Step che ha prodotto i record da persistere (il runtime espande in entries)."
en = "Previous producer step supplying entries."

[args.properties.key]
type = "array"

[args.properties.key.description]
it = "Campi-chiave per l'upsert (conflitto). Es. [\"id\"]. Assente -> insert puro."
en = "Fields forming the upsert key."

[args.properties.key.items]
type = "string"

[args.properties.set_fields]
type = "object"

[args.properties.set_fields.description]
it = "Override {campo: valore} applicato a OGNI record prima dell'upsert (aggiorna lo stato). Es. {\"status\":\"posted\"}."
en = "Constant fields applied to every written entry."

[output]
schema_inline = "{ok: bool, results: Array<object>, partial?: bool, error?: str, error_class?: str, error_code?: str}"

[[capabilities]]
name = "metnos:write"
hint = ["store_registry"]

[placement]
scope = "server"
device_ok = false

[[tests]]
name = "builtin_contract_domain_suite"
reference = "runtime/tests/test_builtin_executor_contracts.py"
