# Generated by scripts/generate_builtin_executor_contracts.py.
manifest_format = "1.0"
executor_standard = "metnos.executor/1.0"
name = "describe_images"
version = "1.0.0"
author = "Metnos builtin maintainers"
affinity = ["descrivi foto", "cosa c'e' nella foto", "descrivi immagine", "contenuto foto", "che foto e'", "descrivi questa immagine", "describe photo", "describe image", "what's in the photo", "image content", "caption"]
platforms = ["linux"]
revertible = false
lifecycle = "active"

[description]
it = "SCOPO: Descrive immagini locali osservate senza modificarle. PATTERN: richiesta naturale tipizzata dal planner. NON: ampliare autorita o inventare input. OUT: risultato JSON dichiarato."
en = "SCOPO: Describes observed local images without modifying them. PATTERN: natural request typed by the planner. NON: enlarge authority or invent inputs. OUT: declared JSON result."

[code]
files = ["implementation.py.src"]
digest = "sha256:bc77c86e650213176c5e154542e0b42ba24f1aa6779c1cf70689f252a9cfe984"

[args]
type = "object"
required = []

[args.properties.reference_images]
type = "array"

[args.properties.reference_images.description]
it = "Path locali delle immagini da descrivere."
en = "Local image paths to describe."

[args.properties.reference_images.items]
type = "string"

[args.properties.paths]
type = "array"

[args.properties.paths.description]
it = "Alias di reference_images."
en = "Alias for local image paths."

[args.properties.paths.items]
type = "string"

[args.properties.entries]
type = "array"

[args.properties.entries.description]
it = "Entries con campo path/reference_image (da from_step). Alternativa a reference_images."
en = "Input entries already available in memory."

[args.properties.entries.items]
type = "object"

[output]
schema_inline = "{ok: bool, entries: Array<object>, ok_count: int, fail_count: int, partial?: bool, error?: str, error_class?: str, error_code?: str}"

[[capabilities]]
name = "llm:local"
hint = ["vlm"]

[[capabilities]]
name = "fs:read"
hint = ["arg:reference_images", "arg:paths"]

[placement]
scope = "server"
device_ok = false

[[tests]]
name = "builtin_contract_domain_suite"
reference = "tests/runtime/skills/test_builtin_executor_contracts.py"
