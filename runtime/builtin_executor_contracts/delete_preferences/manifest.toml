# Generated by scripts/generate_builtin_executor_contracts.py.
manifest_format = "1.0"
executor_standard = "metnos.executor/1.0"
name = "delete_preferences"
version = "1.0.0"
author = "Metnos builtin maintainers"
affinity = ["preferenza", "preferenze", "impostazione", "preference", "preferences", "setting", "togli", "rimuovi", "remove", "azzera", "reset", "predefinito", "default"]
platforms = ["linux"]
revertible = false
lifecycle = "active"

[description]
it = "SCOPO: Rimuove preferenze personali riportandole al valore predefinito. PATTERN: delete_preferences(keys=[\"tone\"]) o delete_preferences(all=true). USA: rimuovere o ripristinare preferenze. NON: utenti o task. OUT: results=[{key, removed, previous}]."
en = "SCOPO: Removes personal preferences, restoring their default value. PATTERN: delete_preferences(keys=[\"tone\"]) or delete_preferences(all=true). USE: remove or restore preferences. NON: users or tasks. OUT: results=[{key, removed, previous}]."

[code]
files = ["../../user_preferences.py"]
digest = "sha256:d5413ddb48d76b97f06130d3bf85ba03430b7c55b94589ad6b0f949698117c37"

[args]
type = "object"
required = []

[args.properties.keys]
type = "array"

[args.properties.keys.description]
it = "Preferenze da rimuovere, per nome esatto. Ammesse: lang, tone, reply_length, units, sites_browser_mode, sites_stealth, sites_auto_allow_resources, sites_stealth_webdriver, sites_stealth_user_agent, sites_stealth_mobile, sites_stealth_context_coherence, sites_stealth_browser_apis, sites_stealth_human_delays, sites_stealth_focus_events, sites_stealth_session_reuse"
en = "Preferences to remove, by exact name. Allowed: lang, tone, reply_length, units, sites_browser_mode, sites_stealth, sites_auto_allow_resources, sites_stealth_webdriver, sites_stealth_user_agent, sites_stealth_mobile, sites_stealth_context_coherence, sites_stealth_browser_apis, sites_stealth_human_delays, sites_stealth_focus_events, sites_stealth_session_reuse"

[args.properties.keys.items]
type = "string"

[args.properties.all]
type = "boolean"

[args.properties.all.description]
it = "true rimuove tutte le preferenze impostate dall'utente."
en = "true removes every preference the user has set."

[output]
schema_inline = "{ok: bool, results?: Array<object>, ok_count?: int, error?: str, error_class?: str, error_code?: str}"

[[capabilities]]
name = "metnos:write"
hint = ["user_prefs"]

[placement]
scope = "server"
device_ok = false

[[tests]]
name = "builtin_contract_domain_suite"
reference = "tests/runtime/skills/test_builtin_executor_contracts.py"
