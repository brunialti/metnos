"""Package contenitore delle task functions chiamate dallo scheduler v2.

Ciascun modulo espone una funzione `task_<name>(payload)` registrata in
`scheduler_v2/builtin_callbacks.py::install_default_callbacks`.
"""
