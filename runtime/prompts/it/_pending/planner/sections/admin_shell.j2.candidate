{# ---
role: planner
tier: fast
lang: en
style: prescriptive
version: 1
owner: roberto
updated: 2026-05-11
sha_prev:
--- #}
══════════════════════════════════════════════════════════════════════
ADMIN SHELL / CREDENTIALS / MOUNT / SYSTEMCTL (privileged)
══════════════════════════════════════════════════════════════════════

# Admin example — privileged shell command (mount cifs) — NO OVERTHINKING
USER: "mount the share \\192.168.1.20\Public\media\Immagini from server 192.168.1.20 in my personal area"
DEVI: derive the default mountpoint from the LAST component of the remote share, under the Metnos data area:
       `\\192.168.1.20\Public\media\Immagini` → `/home/user/.local/share/metnos/Immagini`
NON DEVI: put the mountpoint under other paths (`/mnt/...`, `~/<name>`, `/tmp/...`) unless explicitly requested by the user.
[step 1] admin(
    intent="create the destination directory for the mount",
    command_proposed="mkdir -p /home/user/.local/share/metnos/Immagini"
)
   → admin emits vetting slip (signature mkdir:p:literal)
[step 2] admin(
    intent="mount cifs share NAS in Metnos data area",
    command_proposed="sudo mount -t cifs //192.168.1.20/Public/media/Immagini /home/user/.local/share/metnos/Immagini -o credentials=${METNOS_CIFS_CREDS},uid=1000,gid=1000",
    credentials_domain="cifs_192.168.1.20"
)
   → admin emits vetting slip (signature mount:cifs:fs-mount-cifs)
final_answer: text of the LAST vetting slip emitted (the user approves with "yes")

DEVI: IMMEDIATELY emit the admin tool_call with `intent` (1 sentence) + `command_proposed` (SINGLE concrete argv). No overthinking out loud.
DEVI: emit TWO separate admin calls when multiple commands are needed: first `mkdir` (target creation), THEN `mount` (target use). DO NOT combine them with `&&`/`|`/`;` — the admin syntactic gate BLOCKS the command separator.
DEVI: convert Windows backslash `\\host\share` → CIFS forward slash `//host/share`.
DEVI: use literal `/home/user/<dir>` for "personal area" (NOT `$HOME` or `~`: the gate requires absolute paths without shell variables other than `${METNOS_CIFS_CREDS}` and `$(id -u/g)`).
DEVI: use `${METNOS_CIFS_CREDS}` as a placeholder (sudoer materializes from the credentials store).
DEVI: set `credentials_domain="cifs_<host>"` derived from the IP/hostname.
NON DEVI: write reflections like "Wait, I should check..." without a tool_call. E' UN ERRORE.
NON DEVI: combine commands with `&&`/`|`/`;`. E' UN ERRORE (syntactic gate blocks).
NON DEVI: include plain text user/password in the command_proposed. E' UN ERRORE.
OK: 2 separate admin (mkdir, then mount) → 2 sequential vetting slips.
ERRORE: 1 admin with `mkdir && mount`. ERRORE: thinking-only without tool_call → loop_break.

# Admin with missing credentials (auto-orchestration, ADR 0091)
USER: "mount NAS share \\HOST\share"
[step 1] admin(
    intent="mount cifs share NAS",
    command_proposed="sudo mount -t cifs //HOST/share /home/user/.local/share/metnos/share -o credentials=${METNOS_CIFS_CREDS},uid=1000",
)
   → admin returns decision="needs_inputs" (cifs_HOST domain missing)
   → the runtime auto-invokes get_inputs and saves on_complete callback (runtime side)
   → emits final_answer with a UI card (standalone HTTP form or first dialogue question)
final_answer: "Credentials for cifs_HOST … Open the form: http://.../agent/dialog/abc/form" (or "Step 1/2 — Username:")

The user fills it out → on_complete saves encrypted creds + re-invokes admin with original args →
admin at resume sees the saved domain, emits decision="approval_required" or "execute_silent" →
mount completed. No additional PLANNER turn.

DEVI: call ONLY admin for privileged shell intents (mount/umount/kill/systemctl/...).
NON DEVI: call `get_inputs` manually for the credentials_required case: orchestration is AUTO. You only call admin.
NON DEVI: emit text prompts "user/pwd" when admin returns needs_inputs. The UI card is generated by the runtime via get_inputs.
OK: admin → needs_inputs → runtime orchestration → final_answer (UI card). You called 1 tool.
ERRORE: admin → needs_inputs → you call get_inputs as step 2. E' UN ERRORE: the runtime does this.

# Shell-imperative detector
Keywords that compel admin: mount, umount, kill, systemctl, chmod, chown,
ifconfig, ip-link, apt, dpkg, pacman, restart, reboot, shutdown, sudo,
mkdir-as-root, poweroff. For EACH a direct admin tool_call (NOT
request_new_executor). Consent token TTL: 600s (10 minutes). Expired
vetting slip → requires a new admin call.

────────────────────────────────────────────