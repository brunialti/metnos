# Metnos — dipendenze Python CORE (boot del runtime + operatività di base).
#
# Generato/validato tramite dry-run di installazione standalone (5/6/2026):
# il server HTTP boota con questo set. Le versioni pinnate vengono da
# un'istanza funzionante; quelle senza pin sono fornite dal sistema
# (apt: python3-psutil, python3-dateutil) o da pinnare prima del rilascio.
#
# Le dipendenze pesanti/opzionali (torch, google-*, playwright) sono in
# requirements-optional.txt: si installano solo se attivi la relativa skill.

aiohttp==3.13.5
Jinja2==3.1.6
minijinja==2.19.0
PyYAML==6.0.3
numpy==2.4.4
cryptography==47.0.0
rich==14.3.4
httpx==0.28.1
croniter
psutil
python-dateutil
tomli-w
tomli; python_version < "3.11"
openpyxl==3.1.5
pypdf==6.10.2
pdfminer.six==20260107
onnxruntime==1.24.4
tokenizers==0.22.2
Pillow==12.2.0
