Auto-audit del ciclo introvertiva di stanotte (Metnos). Lavora silenzioso e in background, NIENTE conferme, NIENTE chiacchiere.

CONTESTO: Metnos è l'assistente personale self-hosted in /opt/metnos. Il scheduler ha appena finito (ore 05:30 locali) `introvertiva_propose` (05:00) + `introvertiva_apply` (05:30) + `apply_executor_ager` (03:30). I tuoi obiettivi: verificare i risultati, correggere errori, applicare ottimizzazioni quando sicuro.

LEGGI PRIMA `/opt/metnos/CLAUDE.md` (vincolante) + `~/.claude/projects/-opt-myclaw/memory/MEMORY.md` (contesto sessioni precedenti). Date di riferimento: ADR 0067, 0072, 0074, 0076, 0077. Se esiste, leggi anche il report del giorno precedente (`~/.claude/projects/-opt-myclaw/memory/metnos_auto_audit_*.md`) per confronto trend.

CHECKLIST OPERATIVA:

1. **Esiti scheduler**:
   sqlite3 /opt/metnos/workspace/.scheduler/state.sqlite "SELECT task,started_at,status,duration_ms,output FROM runs WHERE task IN ('introvertiva_propose','introvertiva_apply','apply_executor_ager') AND started_at > datetime('now','-12 hours') ORDER BY id DESC LIMIT 6"
   Se status != 'ok', leggi output completo e diagnostica.

2. **Qualità proposte introvertiva** — leggi gli ultimi `~/.local/share/metnos/introvertiva/candidates_{dedupe,generalize,specialize}_*.jsonl`. Verifica:
   - Nessun proposed_name finisce in `_True`/`_False` o ha doppio underscore (validator ADR 0077 attivo).
   - Nessun arg di flusso (`entries`, `from_step`, `results`) come `arg_name` (skip-list ADR 0077).
   - Nessun pattern generalize che include executor non in catalog.
   Se trovi violazioni → BUG nei filtri introvertiva.py. Apri il file, identifica la falla, correggi, fai girare i test:
   cd /opt/metnos/runtime && python3 -m pytest tests/test_introvertiva.py -q

3. **Audit applied** — `~/.local/share/metnos/introvertiva/auto_applied_*.jsonl` più recente: verifica che ogni `applied` non abbia `error`, e che il file generato in `~/.local/share/metnos/executors/<name>/` esista, sia firmato (`manifest.toml.sig`) e venga caricato dal loader senza reject. Se reject persistente → backup il synth in `/tmp/metnos_synth_<name>_<ts>` e annota in memoria.

4. **Synt cascade spuria** — controlla turni recenti:
   ls ~/.local/share/metnos/turns/ | tail -3
   Se ci sono `chosen_tool=='request_new_executor'`, leggi il `result.message`: se `redirected: true` o `already_in_catalog: true`, OK (short-circuit ADR 0076 funziona). Se invece la cascata è davvero partita, verifica collisioni con handcrafted (/opt/metnos/executors/) e pulisci: backup in /tmp/, rimuovi.

5. **Bench latenza**:
   python3 /opt/metnos/runtime/bench_latency_breakdown.py 2>&1 | head -25
   Confronta con i numeri base nel report del giorno precedente. Se `request_new_executor` calls > 0 in last 24h, indaga.

6. **Fix automatici sicuri** (puoi applicare senza chiedere):
   - Import mancanti in moduli runtime (NameError pattern).
   - Cleanup di synth file rejected dal loader (collision).
   - Filtri introvertiva mancanti (es. nuovo tipo di valore non skipato).
   - Bump soglie di evidenza in introvertiva quando il volume di rumore aumenta.

7. **NIENTE auto** (annota solo):
   - Modifiche al prompt PLANNER (`agent_runtime.py`).
   - Modifiche al vocab.py.
   - Rimozione di executor handcrafted.
   - Cambio tier LLM (router.py).
   - Modifiche a CLAUDE.md sezioni 1-9.

8. **Test sempre** dopo ogni fix:
   cd /opt/metnos/runtime && python3 -m pytest tests/ -q
   Se rosso, rollback la modifica.

9. **Salva un report** in `~/.claude/projects/-opt-myclaw/memory/metnos_auto_audit_<YYYY-MM-DD>.md` (data corrente):
   - Cosa hai trovato (verde / rosso / arancione).
   - Cosa hai fixato (path:line + test result).
   - Cosa è rimasto aperto (lista, priorità, motivo).
   - Numero proposte introvertiva, % violazioni, applied count, top errori.
   Aggiungi pointer in MEMORY.md (1 riga sotto ~150 char).

10. Se è il 7/5/2026 (ultimo dei 3 audit), nel report finale:
    - Confronta i 3 audit (5/5, 6/5, 7/5).
    - Identifica pattern persistenti (errori che ricorrono → bug strutturale).
    - Identifica drift (volumi proposte cambiati, trend latenza).
    - Suggerisci fix permanenti da promuovere a CLAUDE.md o nuovi ADR.

VINCOLI:
- Codice deterministico > LLM (CLAUDE.md §7.9): preferisci sempre sql/grep/regex/parsing a chiamate LLM dove equipotente.
- Niente deploy.sh notturno (i doc HTML non vanno toccati senza review).
- Telegram daemon NON va riavviato (CLAUDE.md §8.6).
- Risposta finale conciso: 1 paragrafo + check-list di cosa hai fatto.
