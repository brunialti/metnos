# SPDX-License-Identifier: AGPL-3.0-only
# Metnos installer — Python package.
#
# Entry point: `python -m install` (invoked by install/bootstrap.sh after
# the venv is ready). See install/__main__.py for the orchestrator and
# install/phases/ for the six install phases.

__version__ = "0.1.0"
