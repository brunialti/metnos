# Candidate v0.3 self-review

Scope is one universal prompt-projection change. The three roots appear together
before the registry catalog; dependency guidance now shows a complete graph,
requires operation ordinal zero to omit `from`, and forbids self/later references
in prose. `from:[0]` appears exactly once, on the second step of the positive
complete graph; no negative JSON exemplar can prime the bad form. A post-catalog mini-check repeats root choice, necessity,
and domination constraints.

All route/control/reason values remain registry-derived. The same Unicode prompt
path is used for every structurally valid BCP47 tag. No query, benchmark identifier,
expected answer, model port, outcome, path, continuation, or repair rule was added.
Schema, registry, validator, compiler, adapter semantics, backend profile, and
evaluator are outside this overlay and are integrity-bound to frozen v0.2 bytes.
Complete grandfathered BCP47 handling is separately integrity-bound as v0.2.1;
it is a contract prerequisite, not part of the prompt hypothesis.

This is laboratory code. JSON dictionaries here are protocol artifacts; a future
production port requires immutable typed objects.

Offline evidence before independent audit: overlay suite 9/9, unchanged base
suite 32/32, structural round-trip 124/124, and BCP47 prerequisite suite 4/4.
