# RM-0008 G8 — independent internal review

Date: 15 September 2026.
Reviewed checkpoint: `04aa1b14`, followed by the local terminal-replay repair.
Reviewer: independent Codex agent `rm0008_g8_independent_review`.
Scope: terminal replay, contract candidate N1–N5, then the bounded public
historical-reader diff on source base `07603d86`. No reviewer edits,
deployment, private-key access or repetition of the recorded test suites.
This is not the external final review requested by Roberto.

## Findings and disposition

### P2 — an admitted recovery hint could replay as a rejected terminal

`runtime/executor_birth_operational.py::_replay_terminal` compared publication
presence but not the signed outcome/error with the stored state. An authentic
hint placed in a rejected row returned admitted, no error and no publication.
The reviewer reproduced this with real functions and an ephemeral key in
memory. It was a pre-existing residual omission, not introduced by `04aa1b14`.

Fixed locally by checking the committed or rejected outcome family and the
operational error binding. The reviewer inspected the revised diff and found
no additional blocker in this bounded repair. Genuine preexercise, rejection,
recovery and reuse of a receipt already persisted remain supported. Strict
validation of the complete future certification dossier is a separate task.

### P1 — historical original bytes were incorrectly assumed available

`contract_store.GENERATION_FILES` preserves manifest, signature and linguistic
state. `executor_birth_authoring.cleanup_transaction` removes the temporary
journal and backup. Release evidence stores distribution metadata/signature;
release sources may recover some code, but do not guarantee each act's journal
or transformed candidate. Coverage of the 69 metadata candidates is unproven.

Disposition: Roberto explicitly approved the first option after a plain-language
explanation: verify signed receipts, exact bindings and persistent state,
and disclose unavailable originals. The detailed decision is in
`../design/decision_rm0008_g8_scope_15_9_2026.md`. No threshold is certified by
this decision; per-act verification and inventory remain required.

### P1 — public historical verification must be transitively public-only

`executor_birth_prepared_set.load_authority_set_v1` loads private material and
compares the historical author with today's active author. Its
`authority_registry_v1` also reaches private loaders. Merely returning a public
projection does not make these paths suitable historical readers.

Acceptance: use the exact authenticated historical context/inventory without
private-key reads; succeed with private files inaccessible and the active
author rotated. Reuse the persistent public material in the frozen context
and owner-local authentication of immutable generation payloads. Do not pass
a historical generation to a current-selection reader.

Disposition: implemented the closed reader contract from work-plan §3.5.
The Ownership public loader now uses marker/set/material/public files without
loading private material. Historical target contexts are selected only from
the fixed authenticated chain, reread around the acquisition. Returned views
are immutable evidence, not operational capabilities. Local tests deny
private reads and rotate the author; installed and native Windows evidence
remain separate open checks.

### P3 — historical registry version was unnecessarily fixed

The first reader diff required registry component version `"1"`, contrary to
the closed contract's independence from the current component catalog. The
reviewer identified this as a compatibility limitation, not a trust bypass;
no installed record exercising it was claimed.

Fixed after a discriminating red test using the real builder with a different
recorded version. The decoder still verifies the material schema, context
identity and registry digest with that recorded version. The reviewer checked
the final change and the public/immutable boundary, finding no additional
blocker. This static review did not repeat any tests.

### P1 — persisted Producer protocols must not be conflated

`executor_birth_reattestation._execute` finalizes with a receipt-result binding
and no schema-2 terminal. Such acts must be authenticated under their own
protocol and excluded from technical-admission counts. Ordinary technical
admissions still derive request-v1 identities. Their terminal may omit the
embedded admission when the commit publisher reuses a durable receipt.

Acceptance: derive the actual request protocol, verify exact receipt/issuer/
capability bindings, independently reread the durable admission when needed,
and never manufacture V2 requests or reject valid history merely for having
a different terminal representation.

## Verification and remaining limits

The main agent reproduced five new negative cases before repair, then ran
20 distinct affected cases: 17 replay/outcome cases, one new preexercise
terminal case and two crash-recovery cases. All passed. These overlap earlier
coverage and are not added to the earlier 82 as a disjoint total. The
preexercise case tests the terminal boundary, not a new Synt certification.

N1/N2 still need exact certification/ledger schemas, issuer custody and
revocation, complete stable frontier, and current-release compatibility.
N4/N5 still need the complete cycle profile and authenticated defect census.
The signed act time is captured before checks, not a proven finalization time.

No installed admission count, live HTTP test, native Windows proof, source-pin
refresh, release approval or F5 activation follows from this internal review.

The later public-reader increment passed 32 new cases and 15 affected existing
cases; repeated decoder-confirmation cases are not added again. A small
follow-up normalizes malformed canonical JSON into the existing named error;
the reviewer confirmed that it does not broaden accepted input. Chain-selector
tests explicitly use a mocked chain boundary and real protected set reads;
only the key-exclusion test traverses the actual public-loader entry. Neither
is described as installed-chain cryptographic proof or production E2E.

The subsequent hybrid live-artifact proof is recorded separately in work-plan
§5.2 (`run-87e_n1bg`). Before execution, the reviewer required installed-only
dependency resolution, execution of the three measured candidate source
buffers rather than cached bytecode, recorded module origins and equality
between HTTP's selected release and the authenticated required distribution.
The main agent's successful run checked two real context sets with the real
cold chain; the reviewer did not independently rerun it. It remains a bounded
integration proof, not certification of the full candidate or F5.

### Historical Producer author-scope clarification

The reviewer distinguished authentic signatures from independent policy
verification. The stored public registry binds a Producer namespace and key,
not `allowed_authors`; the receipt cannot grant itself that authority. The
Admission act time, once authenticated, is the relevant Producer validity
time. Unsigned database timestamps and an invented pre-expiry time are not
substitutes. Current generation validation is likewise not a historical
manifest-policy check.

The main agent's subsequent source-availability observation, recorded in
work-plan §5.3 (`run-37ldnu_4`), found the exact same public Producer-policy
source in every signed distribution record from release 1 through 53. This
removes the observed missing-source obstacle, not the need to bind the policy
to each selected historical context or to verify each act. The reviewer did
not rerun that observation. The bounded contract and subsequent diff review
of the inert projection are now complete; no new certificate authority or F5
qualification is implied.

The reviewer found no blocker in the §3.6 implementation. The generic reader
preserves the sealed payload, derived root, exact manifest entry and protected
capture. The current descriptor retains its two current-release checks. The
historical projection binds transition, build, source and frontier, extracts
bounded literal AST data and returns an immutable map restricted to the
historical set's namespaces. It does not introduce an `IssuerRegistry`,
implicit authority scope or admission qualification. The main agent recorded
45 new and 10 affected existing passing cases in work-plan §5.4; the reviewer
did not repeat those tests. The explicit chain/admin-metadata test seams
prevent these results from being described as installed proof.

### Public schema-2 terminal evidence

The reviewer accepted and inspected the shared decoder/public-verifier
contract in work-plan §3.7. The implementation retains the operational
state/hash/postcondition checks and does not equate a valid terminal signature
with durable publication. Its caller must establish the public ring's
authority and separately verify any embedded Admission. The expected report
contract and reconstructed defaults are explicitly not additional signed facts.

The internal epoch diagnostic's real path retains live key acquisition,
generation authentication and signature/issuance/twin checks. Its explicit
fixture path alone avoids an unused live-authority lookup. The updated
foreign-contract negative still requires rejection, now at the earlier exact
wire/issuance binding. The reviewer found no blocker; no tests were rerun.
The main agent's 52 new, 54 operational and 21 diagnostic passing cases, plus
the setup corrections and metadata-only installed observation, are recorded
in work-plan §5.5. No installed or full-N3 certification is claimed.

### Complete raw Producer history

The reviewer accepted §3.8 as a bounded owner read, not a new certification
authority. The two tables remain separate and complete, preserving row IDs,
all states and exact field bytes. One read transaction covers schema, length
checks and payload reads. The WAL test uses a genuine second connection and
commit between table scans. No private signer, schema migration, operational
claim/recovery or caller-selected database enters the new interface.

Review required an explicit initialized-runtime precondition: existing
transitive imports reach `config.ensure_dirs`. A guard placed only inside
the new API cannot make cold imports pure. The reused installed probe puts
its mutation protection in place before any product imports instead.

Two P3 defects were found in the first candidate: timeout overflow and
non-TEXT schema SQL escaping the stable error contract. The main agent
reproduced all three added negative/edge cases, repaired the two causes and
ran the affected family. Final static review found both fixes complete and
no further blocker. The reviewer did not rerun tests or access production.

The `producer-history` probe retains pre-import guards, permits only its
declared read-only SQLite connection and emits aggregates. Its live result
and 46 distinct new passing tests are recorded in work-plan §5.6. This proves
the candidate reader can acquire the selected raw history, not signatures,
a common cross-store frontier, F5 activation or full candidate deployment.
The Python audit does not promise to prevent SQLite shared-memory bookkeeping.

### Exact historical publication acquisition

The reviewer accepted work-plan §3.9 and found no blocker in the bounded
owner reader. It keeps explicit V1/V2 selection, exact contract binding,
bounded regular single-link files, descriptor/path coherence, nonblocking
FIFO refusal, the exact generation file set and digest, repeated binding/
receipt reads and immutable raw output. It does not authenticate signatures,
invent a current `ManifestRef` or invoke today's manifest policy. The two
new structural inventory classifications preserve the checker's conservative
owner capability model; they do not grant additional publication rights.

The main agent's 39 distinct tests and explicit-layout live probes are
recorded in work-plan §5.7. The reviewer did not repeat them. The harness
retains failed V2 observations, adds no automatic V1 fallback and continues
to deny import-time log writes rather than allowing them.

Review identified one reporting limit: a separate V1 probe selects the first
item of a new Producer snapshot without retaining a cross-run sample ID.
Its success therefore does not by itself prove the exact earlier V2-failing
item was read. The main agent narrowed that claim in the ledger. Complete
enumeration must distinguish persisted layout from a terminal's context;
acquisition samples do not establish historical authentication or F5 entry.

### Complete raw contract receipt inventory

The reviewer accepted work-plan §3.10 and confirmed there was no existing
equivalent complete raw reader. Live manifest inventory constructs references
from the origin map and can omit an unbound scaffold through a locking helper;
`diagnose_store` is reference/key-based and treats an absent root as empty.
Neither is substituted for the new owner census.

The implementation preserves all bound contracts, both physical receipt
layouts without deduplication, structurally separate generation/tombstone
IDs, raw bytes and empty receipt sets. Directory membership and metadata of
all traversed files, including unread payload/current/lock files, are checked
again. Budgets apply during scanning before sorting and during final checks.
It creates no locks, current selection, authority or publication.

One P3 defect was found: a growing file could be read beyond the remaining
aggregate byte budget before refusal. The main agent reproduced it and passed
the residual limit to the shared file reader. Static review confirmed the
fix. The conservative graph classifications cover only three new owner
scopes; renaming a generic local `read` avoided accidentally propagating its
classification to unchanged stream readers without changing the checker.

After installed acquisition found a missing binding, review recommended an
explicit acquisition-only refinement: preserve the exact empty-unbound
namespace as a separate opaque record, never a `ContractId` or an admission.
Owner creation order supports structural compatibility with pre-binding, not
proof of crash provenance or absence of erased history. Exact child set,
single NUL, private POSIX modes/owner and the same bounds/final reread are
required; every deviation still fails. No cleanup or live catalog lock helper
is invoked. Certification must reconcile this observation under its common
barrier; acquisition completeness does not waive that requirement.

The reviewer found no blocker in that refinement. The main agent's 72
distinct tests, failed installed attempts, diagnostic harness correction and
successful complete raw inventory are recorded in work-plan §5.8. No tests
or production probes were independently repeated. No signature qualification,
native Windows result, release or F5 activation is implied.

### Initial-context public keys

The reviewer accepted the opt-in first-predecessor selection in work-plan
§3.11 and found no blocker in the implementation. The default target-only
selector remains unchanged for Producer policy. Public acquisition binds the
protected marker/set's exact previous set, context and epoch, rejects an
ambiguous target/predecessor match and rereads the chain frontier. The
explicit `binding_kind` prevents presenting the first successor distribution
as the predecessor's policy. Private keystores and old runtime activation
remain outside the read path.

Caller inspection found only evidence tools/tests, not an ordinary bootstrap
or routing caller. This respects Roberto's availability clarification: a
historical certification limitation must not disable unrelated normal use.
The reviewer repeated no tests or installed probes. The main agent's eleven
new cases, twelve affected existing cases and successful explicit initial
public-key proof are recorded in work-plan §5.9.

For the next per-act verifier, review recommends deriving the operation,
origin and author from authenticated historical declarations plus exact
request/key binding, never the receipt's own declarations or mutable database
capability text. Verify Producer validity at signed Admission act time. The
request-v1 digest uses the bootstrap length-prefixed string codec, not the
typed Producer-store codec. Missing original code bytes permit authenticated
cross-binding under the approved evidence option, not candidate/source-ID
recomputation from manifest digests.

Initial public-key proof does not settle the initial Producer-policy source.
The marker's provisioner-build digest covers different modules from a signed
distribution build; matching successor policy files cannot authenticate the
predecessor retroactively. Investigate retained authenticated bindings before
claiming the history qualifies. This is a certification limit, not a new
productive runtime restriction.
