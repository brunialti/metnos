# RM-0008 — release and verification ledger, 2026-09-15

Status: release 51 is selected and ready; live incremental LRE completed and
delivered its completion notice. A follow-up search exposed path words in a
separate health-intent shortcut. The shared path filter now covers those
consumers too; related tests and the updated real-model E2E passed (see below
for two independently reproduced authoring-catalog baseline failures). Public push and website
publication remain pending. The owner's archive and both existing indices
are preserved. Earlier successful live checks are retained below.

## Authorized scope

The owner authorized production deployment, a local commit and incremental
publication to the existing public Git repository after verification. No new
external search API, account, credential, subscription or provider is authorized.
Web relevance remains deferred under `WEB-SEARCH-001` in `internal/design/TODO.md`.
BACHECA monitoring is disabled and its files are excluded from these changes.

## Delivered candidate and evidence

- Shared, timezone-aware date/time resolution, constrained local interpretation,
  ordinary ambiguity selection and exact mail receipt filtering:
  [temporal and real mail evidence](rm0008-temporal-20260915.md).
- Automatic missing-index admission into resumable LRE phases, controlled local
  model startup, complete-generation publication and image relevance:
  [service-identity cold-start E2E](rm0008-image-indexing-20260915.md).
- Bounded central concurrency and durable model-token reservations:
  [parallelism evidence](rm0008-lre-parallelism-20260915.md).
- Capability-aware source access and honest empty-filter answers:
  [guard and finalizer evidence](rm0008-guard-empty-filter-20260915.md).
- Bounded web search/reranking without restoring rejected results:
  [web findings and explicit deferral](rm0008-search-e0de40fe-20260915.md).

The combined changed/related-code gate passed 1,313 tests, with 9 expected
skips and 8 subtests. Subsequent boundary guard tests passed 78 tests. Tutor
tests on the genuinely signed candidate passed 102 tests. The final public-doc,
photo-card, prompt and temporal-documentation group passed 74 tests. These groups
overlap; their counts must not be added as unique coverage. This is not a
claim that every test in the repository was run or passed.

The direct Tutor suite initially used unsigned edited authoring manifests and
an absent worktree model directory. It was rerun on a copied, current catalog
signed by a fresh fixture identity, with real installed BGE artifacts; no
production private key was copied and verification was not disabled. One
textual prompt assertion was updated to the new informational/instructional
contract without removing its natural-example, language or secret safeguards.

## Tutor convergence and preserved failed oracles

The original question is unchanged: `l'indicizzazione delle foto avviene
inautomatico?` (original turn `7a5207e0d0a14d1b`). Candidate tests use actual
HTTP routing, local models, a compiled signed catalog and no executed actions.

- `/tmp/rm0008-tutor-candidate-dkfc9q4e/result.json` passed structural checks
  but failed semantic review: it suggested a manual start/confirmation and
  invented an acronym expansion. It is not a successful final answer.
- `/tmp/rm0008-tutor-candidate-why8uy9e/result.json` exposed a contradictory
  Italian denial and a missing instructional example. The shared composition
  prompt now distinguishes explanatory questions from instructions, preserves
  conditional availability, and distinguishes a quoted example from execution.
- `/tmp/rm0008-tutor-candidate-1cho0uzf/result.json` contains a correct Italian
  answer, but an English request sent during fresh-instance initialization
  fell through to planning. A concurrent lexicon initialization warning is
  recorded; causation or a production regression is not established. This run
  is not counted as a complete pass. Subsequent tests wait for the ordinary
  catalog-ready event; they do not disable or replace startup code.
- The prepared runs under that same fixture independently verified both
  informational answers and both instructional openings. One helper initially
  read the asynchronous turn log too early; its bounded wait was corrected.
  Those controls exposed stale text in the published photo card, still saying
  that index creation could be proposed. The IT/EN card now agrees with the
  automatic first-search guide, with regression tests for both source classes.

The public guide keeps first-search admission, prerequisites, asynchronous
duration, completion notification and index reuse in one bounded source unit.
It also includes a search example, not an instruction to start indexing first.
Both public guides and the surviving Tutor card are authoritative inputs to
the signed catalog; neither may retain the obsolete manual-start explanation.

The final complete run passed all four unchanged questions after ordinary
startup, with no executor actions or evidence gaps:

| Case | Turn | Seconds |
| --- | --- | --- |
| Original Italian automatic-indexing question | `1690c4edbf504409` | 6.813 |
| English automatic-indexing question | `6b219c0a922e48b6` | 4.933 |
| Italian search instructions | `3be69d3edd2f4e15` | 7.283 |
| English search instructions | `15ffde23fd024a66` | 5.564 |

Evidence: `/tmp/rm0008-tutor-candidate-dtqo9sh7/result.json`. Fresh signed
catalog compilation took 134.282 seconds; catalog SHA-256 is
`574181c5a56275cda0678345396e814f6fe544c4251b27b53489a62bd15b3b7f`.
The two informational answers affirm the conditional automatic behavior without
a manual-start/consent instruction. The two how-to answers show an actual search
request, describe the asynchronous preparation and preserve the ordinary Tutor
explanation/action boundary. Prompt SHA-256: IT
`18aa8a8f3d736c7e1d356443f7f93b33653d65608de3082599c2932f4b52cad4`, EN
`56998ae72f7cea53a063e70fe2137f545f862eb697c235960ecfc01f3b9fc465`.

## Release safeguards and remaining confirmations

The boundary inventory explicitly classifies the new prerequisite callback as
`live_reader`: it only supplies `load_catalog(verify=True)` for signed rules;
ordinary argument, capability and durable-admission checks retain authority.
No guard role or compiled security exception was broadened.

Final production evidence must record the actual release sequence, source roots,
component-admission result, service health, live Tutor check, documentation
deployment and local/public commit identities below. Crossing is allowed only
with no active user turn. A crossing alone is not sufficient: component
publication and verification as the service identity must also complete.

Prepared source profiles after the explicitly reviewed recipe-pin correction:
private 767 Python files,
`sha256:93b8c7bcc55a6a3e1208e6bb3cd5c904451a18a5f9ca7ef7972ed69b770986d2`;
public 755 Python files,
`sha256:9b1e3be8246beb015a439bdfae2650d4ae4f96644a92836a066dfcb66e44904a`.

Large-archive performance remains for the owner's later test. The initial
real photo E2E uses two drawn images; multi-group interruption/resumption uses
a 33-image simulation. The initial candidate's Telegram adapter was simulated;
subsequent live delivery evidence is recorded below. No user photo index was
deleted or rebuilt.

## Production outcome

Initial local implementation commit:
`6faf1af910d3a6022ec97c4b40e2a576295a30f3`.

The first release-44 attempt was refused with `service source recipe`, after
the ordinary transition had stopped services and before cutover completion.
The reviewed LRE scheduler opt-in changed `SERVICE_SOURCE_V1`, but the autonomous
verifier still pinned the previous recipe. The existing differential material
test reproduces this omission. The corrected fixed pin is
`sha256:8d657fff0dbfff0c20aa650b5147598e26c481e697458064587d83962bb1bd4f`;
only the declared `METNOS_EXECUTOR_PARALLEL=1` worker setting is added. Both
independent readers accept it and reject a missing, zero or different setting.
No runtime-computed acceptance pin, drop-in or verifier bypass was introduced.

Read-only observation `run-x4ny0qlr` confirmed that release 43 still attested,
but the HTTP PID was zero and services were inactive. The narrow recovery
`run-ghbj6wl_` re-attested that exact predecessor, required the HTTP process
to be absent and restarted its ordinary `metnos.target` successfully. This
corrects the earlier assumption that a refused crossing left services running.
The failed candidate evidence is retained under
`/var/lib/metnos-admin/rm0008-cycle-evidence-bc05c911c0191c6a`.

The public preflight also refused the modified mail manifest's historical
commercial sender examples: its exact-payload exemption no longer applied.
Both languages now use reserved example domains, and the obsolete exemption
was removed. Signature-bearing export bytes are not rewritten to pass the
privacy gate. The 173-test recipe/catalog/privacy group passed, including the
new differential scheduler cases. The broader preflight/launch/material/TCB/
catalog/privacy group passed 486 tests, with 2 explicit skips. Public export
preflight completed with zero GII findings. Recovery health `run-gszk1cr1`
confirmed release 43, `ok=true`, `ready=true` and zero active HTTP turns.

A combined boundary group exposed a test-isolation defect: deleting an entry
from a shared policy dictionary and letting `monkeypatch` reinsert it changed
its insertion order for subsequent exact-order tests. The unchanged projection
suite passed all 54 tests alone. The mutant now replaces a local mapping copy,
preserving the original shared object and all refusal assertions; production
policy bytes and the compiled projection are unchanged. The exact combined
boundary/policy/language-state group then passed all 291 tests in 14.50 seconds.

The revised prepared tree contains 1,771 files, census
`771b92c2cc2c86e946c8dbb7d409e4cbc6d4905b954cd807da7b64e7347b8941`.
Corrective local commit: `d2c57065` (full identity recorded by Git).

### Confirmed recovery frontier and authorized extension

The next attempt first met a temporarily busy ordinary lock. A subsequent
unchanged retry passed that lock and was refused at `successor edge`; no
additional service stop, deployment or component admission took place.
Read-only authenticated observation `run-2003kkal` establishes:

- Selected release 43: `PREFLIGHT_VERIFIED`, sequence 6, required head
  `sha256:2198c104932c1e438d133008591c8206c6d95c9c8faa8bb26417e9dc84f07ba1`.
- Unselected release 44: `RECEIPTS_COMPLETE`, sequence 1; request
  `sha256:ab244a79f3f5d1a8cf64ed241f773c67ff2f79c3f7d4f366d6a6d7118189fd47`,
  provisioning transaction `63b3f175b9b3686cfeea094d83cfbf23`.
- The failed transaction has records 000 and 001 and a current receipt proof,
  but no head or certificate payload hash. There are no pending unopened claims.
- Its immutable source is
  `sha256:58c18c8ef00f974e363aab8347e0f413ad005e6bf34a5146d5836811bf793ea3`;
  the corrected received source is
  `sha256:a789ff6477d1f4ec705d3059617bbd0ad3cd4bb50c3a7cad80b195e94c03a090`.

Before this correction, the withdrawal helper accepted only an unopened claim or a sequence-zero
`PREPARED` transaction without receipts. The product abandonment path accepts
only a proved unattestable `HEAD_REQUIRED` crossing, sequence 5. Neither applies
to this frontier. Replaying the failed immutable source retains its recipe-pin
defect; changing its signed files or manufacturing a terminal state is not a
recovery option. No such operation was attempted.

The owner explicitly authorized correcting the release procedure so it can
withdraw this intermediate state. The local helper now accepts only `PREPARED`
or `RECEIPTS_COMPLETE`, verifies the full canonical journal, proves the exact
predecessor remains selected and archives the four exact superseded objects
with the claim last. Receipt proofs remain in the archived journal; backing
receipt and producer stores are not moved or changed. Later states, an unchanged
source and an initial release are not recoverable through this helper. The
product coordinator's state machine and abandonment rules are unchanged.

The independent service-recipe check now runs before crossing effects or service
stops, so the original stale-pin defect is detected without taking services down.
The release-procedure suite passed 158 tests; the combined procedure, independent
preflight material, service catalog and public privacy group passed 331 tests.

The initial, insufficient rehearsal `run-3527plgh` passed five cases: an uninterrupted withdrawal and a
restart after each of the four renames. It used copies of the actual public
control records and the real provisioning header, with private provisioning
payloads replaced by an opaque fingerprint stand-in; no signing key was copied.
Canonical readers verified every journal, selected history remained identical,
each archive preserved its exact input objects, and the ordinary next-release
edge became available. The startup observation on the copy was explicitly
frozen from a positive live predecessor attestation. Production objects were
fingerprinted before and after and remained unchanged. Rehearsed helper SHA-256:
`8941506a51c39ca72f5a131c27562f0cf6260a340c25606082749f3792dcf547`.

The production retry then stopped after the first rename because independent
startup authentication reported `orphan context transition`. The coordinator
graph alone does not validate every ownership-chain relationship; freezing the
startup observation in that rehearsal missed this dependency. Services were
not stopped and the selected release did not change. Recovery `run-jxnk7wu5`
matched the archived journal byte-for-byte against the earlier root-owned copy,
restored only that exact directory with a no-replace rename under all three
locks, preserved selected history and re-attested release 43. No record was
rewritten and no verifier refusal was ignored.

The corrected ordering archives the attempt's exact, content-addressed context
transition first, if published, while its coordinator journal still exists.
The canonical format and the journal binding are checked; other context records
and their inventory remain unchanged. The original four moves then follow,
still with the claim last. An interrupted prefix must preserve the selected
release's complete authentication, not only its coordinator graph. Procedure
tests now pass 172 cases; the combined group passes 345 tests in 2.65 seconds.
The replacement rehearsal authenticates copied control records using the
unchanged independent verifier and actual public signatures; private authority
files are zero-filled fixtures, never copied or used for signing. Its first
setup attempt (`run-jqopf78i`) was refused before testing because the administrator's
restrictive umask gave two newly created fixture directories the wrong modes;
the fixture now sets their exact required modes explicitly. Production remained
unchanged during that setup failure.

The complete replacement rehearsal `run-rw25y6e3` passed all six cases in
107.035 seconds: normal completion and an interruption after each of the five
moves. It reproduced the former orphan-context error before testing the corrected
ordering. Each case performed 9–11 independent authenticated-prefix checks with
the real public signatures, retained the exact archived bytes and selected
history, and exposed the normal next-release edge. Final live snapshots and
predecessor attestation were unchanged. Verified procedure SHA-256:
`5bbd8bbfc6d2c203560f70098d2726582e82e1020fcd7a5c3f1df9fee9f25e0b`.

The corrected withdrawal completed in production and the new release-44 build
is `sha256:892ac7d3f965f750a08b9720c4d9974b25606460ed311078b918ff0981c617fc`.
Its audit and native probe passed, but G6 then refused `administrative file
content`, before selecting a new head. Observation `run-h11se1yo` confirmed
that services had stopped and release 43 still attested. Its ordinary target
was restored successfully by `run-otr83rqx`.

Observation `run-fu7b1kb6` proves the remaining administrative artifact belonged
to the first failed, now archived release-44 attempt. Its SHA-256 is
`43ee54b1d7cf8089e0ecc49574974407a1de807a4138013522875b37b52c1c30`.
The descriptor-bound installer backup exactly matches release 43's signed
artifact, SHA-256
`410d13cb0d04d84ccdb63116c8a1efdff645bf0a74836f4dc40a9eb8fb6aa43b`.
The failed attempt had already installed its helper before the earlier
service-recipe refusal. This was not an arbitrary manual edit or an artifact
that could be normalized away during verification.

Withdrawal now also restores this exact predecessor artifact, after authenticating
both signed inputs and proving the predecessor is still selected. It atomically
exchanges the two exact installer trees, then moves the superseded helper into
the same recoverable archive. The live helper path never becomes absent.
Unknown bytes, extra files and conflicting backups are refused. Both interrupted
prefixes resume. A read-only check before crossing also rejects a live helper
that matches neither the signed predecessor nor the exact current candidate,
before any service stop.

The first administrative-copy probes (`run-8ezbafg8`, `run-opz_crld`) exposed
an unsuitable runtime trust read: it re-acquired the provisioning lock already
held by recovery. The implementation now uses the existing administrative cold
signature verifier against the fixed public trust root; no lock, signature or
ownership check is disabled. Rehearsal `run-_yipuy9p` passed the complete wrapper
on actual copied signed artifacts, normally and after each of its two moves,
with positive startup attestation and exact tree preservation throughout.
Live state remained unchanged. The combined targeted suite passed 353 tests
in 2.72 seconds. Verified tool SHA-256:
`e1c549eb7c43969dcd4c2e3bb3ee0f61ebcdd2b046f6765243527d1689973e2f`.

A diagnostic import initially created a root-owned empty `workspace` directory
inside release 43, containing only empty `.mnestoma` and `.scheduler` directories.
This violated the immutable distribution inventory and caused scheduled checks
to refuse the release. The probe now uses isolated configuration and workspace
roots before importing runtime code. Recovery `run-5vh4r41d` validated the exact
three empty directories and moved them recoverably to
`/var/lib/metnos-admin/agent-runs/run-5vh4r41d/preserved-empty-workspace`.
No user workspace or index was touched. The rehearsal's live attestation then
confirmed release 43 again.

The existing public branch was checked read-only: local mirror, tracking ref and
remote `main` all point to `bbb6b0a214ea1f749e78b530a2858d65723f0471`.
All three documentation generators pass `--check`; the published-document
validator accepts 99 indexable IT/EN pages. The push and online deployment are
held until the release is completed, so the public result is not reported as
installed behavior. The final live Tutor helper is prepared but has not run;
candidate Tutor evidence above is not relabeled as production evidence.

### Release 44 selected; component admission stopped

Actual administrative recovery `run-hknfj6w1` succeeded in 7.769 seconds,
preserving the selected history and keeping the superseded artifact in the
withdrawal archive. The next ordinary cycle successfully selected release
44, build `sha256:892ac7d3f965f750a08b9720c4d9974b25606460ed311078b918ff0981c617fc`,
request `sha256:7b40f5ba59f941a5af074a0e2c5c258e199793b3de4def972253881c5d376957`.
`CUTOVER_OK` reached `PREFLIGHT_VERIFIED`. Live readiness `run-wgx9tait`
reported `ok=true`, `ready=true`, no active HTTP turn and a ready Tutor
catalog. The watchdog's failed unit still needs a fresh check; a ready HTTP
service alone does not establish that every unit is healthy.

The subsequent ordinary service-user admission stopped on
`create_images_indices`: `semantic_core_unknown_field: lre_plan`. No changed
component was activated. The seven planned components remained pending;
an installed runtime is not the same as successfully published executors.
Earlier fixture-signed photo E2E bypassed the actual Birth admission and
therefore did not detect this integration error.

The central closed identity grammar now includes `lre_plan` and the exact
`prerequisites` object/binding shape. Each technical field affects the
semantic identity, while unknown neighboring fields are still refused.
The existing read-only release preview now computes that identity for both
new and installed candidates, rather than discovering such an error only
after cutover. This remains an informational preview, not an admission.

Tests capture every real core/builtin authoring manifest. Eight real
contracts (the seven release edits plus the prior process regression)
cross the isolated Birth core through a verified test receipt. The latter
uses the existing test property oracle and publisher, not a production
publication. The focused identity, operational Birth, reconciliation,
release recovery, prerequisite, image-plan and semantic-policy group passes
598 tests in 5.81 seconds. Reconciliation fixtures now use the supported
localized-description table instead of a scalar; refusal assertions were
not weakened. The next release, live E2E, public push and website deployment
are still pending.

The release-45 preparation completed with 1,771 files, census
`55342aa2281b7007703d2007e672a624b1bd9b62bf89e25894c8e861ec3f393b`.
Reviewed Python profiles: private 767 files,
`sha256:d8d1da9d5c11a30001e62f1eacad63d181fe879811a32e5861174ae0e419c3f9`;
public 755 files,
`sha256:cd67d2ba63917782d5309e8db42b1b5b2ef7be13c99692aca8aae4adba905f46`.
Subsequent ordinary watchdog runs on release 44 completed successfully with
all checks passing, catalog parity and LRE ready; its earlier failed-unit
state did not persist. The first preparation could not stage outside the
session's writable roots; the same ordinary preparation then completed with
the required filesystem permission, without changing deployment policy.

### Release 45 activation and production E2E

The ordinary release cycle selected release 45 and completed publication as
the service identity: all seven edits were `store_verified` and activated,
including `create_images_indices` and `start_lre`. Selected head:
`sha256:dfba1516bfb186380340ef941ce8d7e406827bf30ce1639c9d69b0711e10e1af`;
build `sha256:14ecc72a67fe61ded2261c7462cae4161bed7b33c9237c9b24ee6652768d2d99`.
Evidence: `/var/lib/metnos-admin/rm0008-cycle-evidence-14ecc72a67fe61de`.

- Production Tutor `run-_xi2ddxa`, turn `554522ede9144425`, passed in
  5.466 seconds with the unchanged original automatic-indexing question,
  grounded public-guide sources, no action and no evidence gap.
- Production mail `run-oclx3d1x`, turn `80949a997f064df0`, passed in
  11.46 seconds. The exact 48-hour interval was September 13–15 at
  14:35:20.873475 Europe/Rome. Fourteen messages were read and classified,
  zero failed; the final filter returned zero. Evidence records structure
  and receipt bounds, not private message contents.
- Production photo `run-5913wyf8`, turn `d100b13883874149`, correctly
  admitted `wrk_d473909a81904fbdb20556b3bac19e49` but failed its analysis
  unit. It is explicitly a failed E2E, not a successful index. The fixture
  contains only two drawn JPEGs in `rm0008-live-check-myllqz3r` under the
  service workspace. No owner archive was scanned or changed.

The immutable release contains no `models` directory. Text already selected
its shared installed artifacts, but image and face resolution still relied
on the absent release-local defaults. The previous copied fixture had a
`models` symlink and therefore missed this deployment-layout defect. Face
now honors the same embedding-tier configuration used by artifact projection;
the sandbox-specific read-only path still takes precedence for execution.
Only declared model files are projected, never the surrounding configuration.
Configuration update `run-3qswmt29` preserved file ownership and mode and
backed up the exact previous file privately. No model was downloaded.

The same E2E exposed an unnecessary writer lock when each parallel worker
opened an already-current durable database. Validation now uses a deferred
read transaction; only actual migrations acquire the writer lock and reread
the version. Regression coverage holds a concurrent writer while opening a
current store. Related durable/model/photo tests: 516 passed, 10 explicit
skips. A stale VLM editor invalidation reference to the removed startup latch
was also removed; request-client configuration invalidation remains tested.

The scheduled photo refresh still imported and invoked the former monolithic
builder. It now resolves the configured archive and exact live host owner,
loads the verified catalog, applies the standard guard and submits the
ordinary LRE plan. It never falls back to direct execution. Scheduler/model
tests: 187 passed, 5 explicit skips (overlapping coverage, not additive).

Read-only observation `run-5fw4jxd_` confirms both the owner's logical path
and the service alias point to `/mnt/nas_public/media/Immagini`. Both have an
existing schema-4 index with 30,937 entries. The service copy's metadata is
older than the owner's copy; neither is certified current by this check.
No index is deleted, copied, moved or rebuilt. A search reuses materialized
results; it does not automatically refresh every existing index per query.
Explicit incremental updates and scheduled maintenance go through LRE.

Release-46 preparation passed with 1,771 staged files, census
`c73e2a7e635b5af017046825d5987bf63ac8bc1aac4b5c97f2b1f640faa20e71`.
Reviewed Python profiles: private 767 files,
`sha256:4b32bf2b5fc0fa43b9d7580e42ba5ca97740a1048fcaa58ef31da9c6e60db656`;
public 755 files,
`sha256:aca17c8d452e4b4bcadcefc2efc045be127eea8856c380c1e506552364e4b0c4`.
The new scheduler catalog read is explicitly classified `live_reader`, not
an authoring or publication exception. Boundary, public-document and signed
prerequisite checks pass 138 tests after preparing the current source pins.

### Release 46 selected

Ordinary deployment reached `PREFLIGHT_VERIFIED` and completed component
admission/activation checks without a second restart or additional component
edits. Source `sha256:e0e9d0fe09596bdf6815fbef717bea13c15acd49caa45e2f0275379847fb5d4f`;
build `sha256:2afc5153f1a56f3ab447ddfc4102a8e895cef18e090c886e6e7ce6ff5a849139`;
request `sha256:3ba203beadcfb8cbf53da8fae97ca982346ea90eb0a38ff219370d26fa68667b`;
cutover `sha256:9140c553cbed1657654887adfb667748d5587925192d5a864511cc2e05696b89`.
Evidence: `/var/lib/metnos-admin/rm0008-cycle-evidence-2afc5153f1a56f3a`.
Local implementation commit: `6457e4e2`.

Readiness `run-vun59x92` attested release 46, service UID 995, `ok=true`,
`ready=true`, zero active user turns and catalog
`38aa5c0005457874fe69374f07f789ffb8cf1239168e999c6f47705125127c4e`.
The current public export preflight passes with zero GII findings; no push
was performed during that check.

### Release-46 photo build passed; relevance oracle failed

`run-lzrc90l6` used only `rm0008-live-check-g3icghq4` in the service workspace.
Turn `0aa15e07c26f45ad` admitted `wrk_44e372765ec341f29a9de6d089fd148d` in
10.530 seconds. All five phases succeeded on attempt 1 and published the
complete two-image index. This establishes the production model-path fix.
Search turn `66bf4f268c444cdb` took 10.364 seconds and wrongly returned both
the computer and the mountain. The incremental leg did not run, because the
oracle stopped on this failure. Selected head:
`sha256:46a98c26b9e6dd5482feb7b6833171a42a1d7901bddf60cfdea63717493d7e4d`.

The exact query argument was `computer`, with no explicit result count.
Read-only observation `run-nsslagsu` found the same inherited folder context,
`Foto: photos.`, on both synthetic entries. Corpus expansion could select a
shared folder term because that context was incorrectly classified as image
description evidence. The new regression reproduced the false positive before
the fix. Folder labels and their explanation now share one non-discriminating
context vocabulary; literal folder queries remain valid, and actual image
descriptions/keywords retain independent evidence. No new threshold, keyword
exception, remote service or model call was introduced. The related real-BGE,
unified-search and signed-contract group passed 56 tests; an additional replay
retains the exact production captions and context without rerunning the VLM.

That exact real-BGE replay passed; the complete small-corpus suite passed
20 tests. Current boundary, resumable photo simulation and scheduler bridge
checks passed 89 tests. Release-47 preparation contains 1,771 staged files,
census `7de3fcaf1874e48878d42fee224d8cee86f1a35384e45cfc88bdfcbd5ca3f1e8`.
Reviewed Python profiles: private 767 files,
`sha256:d47c3c6e26e65124277f17ba8f619a5e9e413abeb3e870e0795291f00edca379`;
public 755 files,
`sha256:52fd201e61aa86d352cb143e2532a73e5a37609b8a5c03554a1ef71663b4c755`.

### Release 47 and preserved incremental failure

Release 47 selected source
`sha256:7080a2de0cc886747b6e03c3a67350acc7d166b3768ce5269a4cad35c7f245d3`,
build `sha256:1f2f9a593912a058f04186a7ceedbc56cfc8ed11c96b96cbfb498c6d706110a3`;
head `sha256:9ded15d0fe47bf3e4bfd6cff88066709259ddcdc53f190dd3524845cd0c82f79`.
The changed `find_images_indices` component was store-verified and activated;
readiness `run-vrntd31s` passed. Implementation commit: `5696b3a2`.

`run-t79tmp8l` reused the release-46 fixture index without rebuilding it.
Turn `d6d6a1beb7714e50` repeated the exact failed search in 5.349 seconds and
returned the computer only. Both owner-event and actual Telegram transports
acknowledged completion of the original job. This proves transport delivery,
not that the human recipient read the notification.

The test then added one copy of the computer fixture and requested the exact
incremental update. Turn `4db3b5b1a4164166` took 19.129 seconds and failed before
admission: central vocabulary assigned index maintenance to both `create`
and `order`, while the available contract is `create_images_indices`.
Read-only diagnostic `run-sx6av1pb` confirms `intent_verb=order` and no action.
The third synthetic image remains available for resuming this failed leg;
successful earlier tests must not be relabelled as release-48 evidence.

### Corrective candidate: semantics and admission visibility

- Index construction, update and rebuild have one canonical action, `create`,
  retaining the indexed domain. IT/EN versioned action boundaries and aliases
  agree. Twelve real local-model intent cases passed, including ordinary sort
  and resize controls (`rm0008-index-intent-cqfm0azj`, `-gw8ic442`).
- The compact proposer surface now preserves bounded JSON-schema defaults.
  The description of the image builder stays within the existing 240-character
  head budget. No threshold or executor-specific keyword override was added.
- Description-prefix matching in `regex_extract` incorrectly enabled both
  `force` and `dry_run` from ordinary photo/path words. Boolean intent remains
  semantic according to `arg_provenance`; the token extractor no longer invents
  it from prose. Existing typed path/time/count extraction is preserved.
- The durable admission receipt now survives dispatch, runtime conversion and
  turn persistence. A valid accepted receipt is neither inline completion nor
  a missing action. Invalid or incomplete receipts do not bypass the final
  honesty check. No fabricated executor invocation is logged.

Preserved candidate failures: `run-zs59o59b` and `run-xssgi8wk` exposed the
fabricated modes; `run-qc3d4v9m` exposed receipt loss after actual LRE admission.
They are not passing E2E results. The final candidate `run-dtwpu0v8` passed:
service UID 995, real HTTP, verified fixture signatures, actual local VLM,
all five phases on attempt 1, complete-generation observations without errors,
and a relevant computer-only search. Turn `28d4651f0373419c` acknowledged the
job in 4.877 seconds; turn `b766e70dc2374861` searched in 6.286 seconds.
The persisted analysis binding has `force=false`. The isolated Telegram
transport is explicitly simulated; its replay claimed no duplicate message.
Evidence: `/tmp/rm0008-image-indexing-real-h4ixztm_/result.json`.

Related groups passed 90 schema/planner tests, 93 extraction/scope/temporal
tests, and six admission/finalizer cases (overlapping, not additive).
A broader language group returned 662 passed, 1,166 subtests and two failures:
the existing RM0005 executable-lexicon fingerprint census and seed metadata
for `ERR_FROM_STEP_IDENTITIES_UNRESOLVED`. Baseline comparison against
`5696b3a2` confirms 2,888 census findings and the same seed metadata mismatch;
the changed source also changes fingerprint findings, so the census is not
claimed clean or silently rebaselined. Evidence:
`/tmp/rm0008-lexicon-baseline-896aqzar/result.json`.
One authoring-tree catalog test cannot load modified unsigned contracts;
actual signed-candidate admission is covered by the complete E2E above.

The additional real planner checks exposed a technical `phase` argument in
the model-facing schema. Worker-owned arguments now use the existing signed
`runtime_resolved` declaration. Runtime-owned `entries` do not invent a public
`from_step`; the ignored legacy `idx` argument was removed. The public schema
exactly matches the LRE request boundary. Four Italian and four English local
planner cases distinguish update, full rebuild, simulation and negation while
preserving defaults and excluding worker fields (`rm0008-index-plan-yvebx9mo`,
`rm0008-index-plan-1t3j98m6`). The earlier boolean-only replay is not a full
argument-validation pass.

Final signed-fixture E2E `run-s32m_n_i` passed against these exact runtime and
manifest changes: turn `88ccfc767e464357` admitted in 7.230 seconds;
`wrk_d76b6b2882ce4ef98312165dd337aa86` completed all five phases on attempt 1;
search `00a9334e7bfd4a7d` returned only the computer in 6.924 seconds.
There were 140 complete-pointer observations and no pointer errors.
Evidence: `/tmp/rm0008-image-indexing-real-7re76tz9/result.json`.
The final image/schema/finalizer group passed 90 tests; boundary, vocabulary
and scheduler checks passed 114 tests. Public documentation validates 99
indexable bilingual HTML files. Inventory entries and scan roots are unchanged.
Release-48 preparation: 1,771 files, census
`ad19391b87d12f9172b52c39cfd7c23150eff6c1e739f23deac6dd13b8d5da54`;
reviewed private source 767 files,
`sha256:4f6673b403f21cf410256895d1e52d71d065b9ebf4fae3c11c8308a756b00e07`;
public source 755 files,
`sha256:d59b78baed33243c5f21ec450dc168a3454f1a4e6b13fe6ee1d8b2916dd23397`.

### Release 48: preserved publication failure and early validation

Release 48 selected source
`sha256:ca97131d3d3f1439a90fa4e1228aad083778cd34bfb2247d7f0db04667fa67b1`,
build `sha256:f17dad1cfb134fcbcad6a3067335dd30a55000b6dbc2d324544338e68d9c556a`.
The ordinary authority completed cutover, then returned exit 78:
`release_edits_child_failed`, `birth_admission_failed`,
`create_images_indices: language_state_coverage`. No component activation
was reported. Evidence directory:
`/var/lib/metnos-admin/rm0008-cycle-evidence-f17dad1cfb134fcb`.
Selected immutable release 48 is not withdrawn or edited.

The authoring companion still described the removed `idx` parameter and
carried the old description hashes. Publication correctly refused it. The
read-only release preview had not applied the same companion validation;
the synthetic signed E2E prepared fixture companions before signing, so it
did not detect this authoring-input defect. Its functional passes remain
valid but do not prove that release-48 source admission succeeded.

The source companion is explicitly realigned with the existing canonical
migration function. Candidate capture remains exact and does not silently
repair metadata. Both preview and changed-only admission now use the same
strict decoder as publication before invoking Birth. Eight regression cases
cover extra/missing selectors, stale text hashes and noncanonical encoding,
with no Birth call or source mutation and preserved unrelated outcomes.
The existing byte-change test now uses valid language provenance rather than
an invalid placeholder. Reconciliation, materialization and candidate-snapshot
suites pass 150 tests in 2.16 seconds. This is a general preflight check, not
an exception for image indexing.

Read-only readiness `run-a04tnwoe` confirms release-48 HTTP and the durable
worker are healthy; the failed component admission is still not called a
success. Direct strict validation passes for all 107 source contract
companions. Release-49 preparation contains 1,771 staged files, census
`5349c1c01f3df9d4a22e045ef19f8cab56b63f717b9e1fbee6e4efe9524f7d80`.
Reviewed private source: 767 files,
`sha256:6ade6ee22c2495935643c76ec126e522f081edb56135f6e020818587bef325ba`;
public: 755 files,
`sha256:e5e7def7ea20cac504e1506727cd10cf159d6f8949608276c4dd88e396f981b1`.
Inventory entries and scan roots are unchanged; only the source census moves.

### Release 49: complete component activation

Implementation commit: `de51f8aa`. The ordinary release authority completed
with exit 0. Source:
`sha256:6bf404e9777506735f5129fcff69cacf86f6739efa131ee855f9c057b09c6501`;
build: `sha256:4fa0694c5b4fe4e895f45ff4cb54aafcc37390f6e9843a503dd26d509ce4e942`;
request: `sha256:e7cda5ad11b136752510960ed5e76aad08600b454296e616b894cff64aa14243`;
cutover: `sha256:ad98d4829d3c7f1a76e1d477598aebc759cc26032a873b93804e30d6c5b90a34`.
Evidence: `/var/lib/metnos-admin/rm0008-cycle-evidence-4fa0694c5b4fe4e8`.

The preview and post-cutover plan both identified only `create_images_indices`
as changed. Admission as the service identity returned `store_verified`,
generation `sha256:55c81fe6664ec91f762c27419ef800be68a4f0faa692dd432fc63b44be6494f3`;
activation and restart completed. Readiness `run-fq1ynqhv` confirms HTTP,
durable worker and the verified Tutor catalog are ready, with no active turn.
Catalog digest: `a9627c1cb9d6d9317bb0638f04222c51b5d3ae232d5c28de22d46bcee368dc06`.

Read-only metadata check `run-bnnx8w1g` confirms both archive aliases still
resolve to `/mnt/nas_public/media/Immagini`; both pre-existing indices still
contain 30,937 entries. Their metadata hashes exactly match the earlier
check. Neither index nor the owner's photos were rebuilt, moved or deleted.
The public export preflight passed with zero GII findings; publication is
not inferred from that read-only check.

### Preserved release-49 failure: an installed seed is not a live database

Live replay `run-8qrppvxo`, turn `5b5310ec1c164ad3` (10.636 seconds), still
classified the exact incremental request as `order` and admitted no work.
Read-only diagnosis `run-3t71rtbm` identified `i18n seed merge skipped:
attempt to write a readonly database`. The action-surface lexicon updated,
but the separate versioned semantic boundary catalog did not.

`run-ku_ck3s0` proves the cause as UID 995 against the attested release-49
artifact: ordinary read-only SQLite access returns `SQLITE_READONLY_DIRECTORY`,
while immutable access reads the correct new boundaries. The service database
is owned by UID 995 and writable; it retains the old released wording,
including the incorrect instruction to use `order` for index refresh.
The distributed seed uses WAL format without sidecars, inside the immutable
root-owned installation. SQLite's documented read-only WAL constraints apply:
[SQLite read-only WAL databases](https://www.sqlite.org/wal.html#read_only_databases).

The fix opens only the released, checkpointed seed as an immutable snapshot,
using an encoded file URI. It does not change the live database's concurrency
mode, permissions, merge policy or local-translation ownership. A real
permission-based regression covers an old official row, a local customization,
a new key, a path requiring URI escaping, unchanged source bytes and absent
sidecars. Related i18n/reconciliation suites pass 150 tests; the action-vocabulary
suite passes nine tests. Counts overlap earlier runs and are not additive.
The next E2E upgrades the four old IT/EN boundary rows read from production
inside an isolated instance with a protected source seed; production rows
are never directly rewritten to make the test pass.

The first upgrade-fixture attempt (`run-tjvoes0m`) stopped before HTTP turns:
its source copy retained untracked, empty SQLite sidecars from the writable
worktree. The fixture now mirrors the release projection, excluding only those
seed sidecars after verifying the WAL is empty. This is a harness setup failure,
not a successful E2E run and not another production failure.

Release-50 preparation: 1,771 staged files, census
`458800033bfc15c641f995822c0d6a51914a343470061f265adb626513eb8a05`.
Reviewed private Python: 767 files,
`sha256:89433bde50fee8abf6a4a080bf1df1dabbd536423ada97d7008cf5421d5a19cd`;
public Python: 755 files,
`sha256:c484d61e8e276dc96016362c812e84017e260665378ae49c8283ddc3a13d70c5`.
Inventory entries and scan roots are unchanged. The release waits for the
upgrade-path E2E result, rather than relying on the earlier fresh-database pass.

Upgrade E2E `run-hqu0k6gh` passed with UID 995/GID 985, four old production
boundary rows, a protected checkpointed seed, real HTTP/local models and
sandboxed LRE. Automatic upgrade was explicitly checked; source bytes remained
unchanged and no SQLite sidecar was created. Turn `d3aaab674d0c4b46` admitted
maintenance in 7.220 seconds; `wrk_1225f8dd5be3404aa223599546d01c72` completed
all five phases on attempt 1. Search `7d5af21fde4e4279` returned the computer
only in 6.287 seconds. The actual VLM started on demand; there were 128 valid
complete-generation observations and zero pointer errors. Telegram was a
simulated adapter in this isolated fixture. Evidence:
`/tmp/rm0008-image-indexing-real-52f_cubl/result.json`. Implementation commit:
`9d921e1a`. This authorizes the already-approved ordinary successor release;
it does not relabel the failed release-49 live incremental test as a pass.

### Release 50 and the exact-path lexical failure

The ordinary release authority completed with exit 0. Source:
`sha256:b9ce946a9ffc0bdb406e31a12a59aba6a7647849404e8b876cf1c30dd104c840`;
build: `sha256:d5a2c6c2306bcc1ccc21fac6b3d84e5a18aad08f2894d019cc45f130691f0c38`;
head: `sha256:ba51d554bc0e5730695df079b3f86e3ad116e2cbf25f5a5919946ad8d2eb8ebc`.
Evidence: `/var/lib/metnos-admin/rm0008-cycle-evidence-d5a2c6c2306bcc1c`.
Both component plans were unchanged, as expected for a runtime-only fix.
Readiness and catalog `run-7mpsu3n9` passed. Read-only `run-csihsymm` proves
HTTP runs from release 50 and both current editorial boundaries now match
the released seed. No live translation was manually replaced.

Live replay `run-3dl65zrs`, turn `eb2154bc4ba94e4f` (10.857 seconds), still
admitted no work. Diagnosis `run-7sgp563c` distinguishes this from the previous
bug: the intent is correctly `create/images`, but the final plan check invents
a required `read`. The existing corpus path contains `check` and `share`;
whole-query tokenization treated these identifier components as actions.
The shorter isolated fixture path did not contain them.

A pure no-model replay reproduces the extra obligation on the exact path.
Tokenization now reuses the existing structural file-system path filter also
used by provider-marker recognition. That filter recognizes Windows separators
and quoted path tokens too. The original request and executor arguments are
unchanged. No executor name, photo-specific keyword or machine path is wired
into the fix. Ten path cases and a real compound-action control preserve the
required mutation while excluding invented actions; related domain/core/recovery
tests pass 86 tests and four subtests. Grammar, routing, compound, vocabulary
and image-LRE groups pass 101 tests (overlapping coverage, not additive).

The next isolated E2E uses a physical path containing `share/live-check`, not
only its neutral alias. Its upgrade baseline is the retained immutable
release-47 seed, rather than a moving production database. The owner corpus
remains outside all these tests.

Combined upgrade/exact-path E2E `run-1uljydbj` passed: four old IT/EN rows
upgraded through the protected seed; turn `08b0fde315114b59` admitted in
8.345 seconds; `wrk_78aec4efecc541bb95d5401b45058b5b` completed all five phases
on attempt 1; search `a0c74139a0e949f6` returned the computer only in 7.695
seconds. There were 157 complete-generation observations, no pointer errors
and no missing model telemetry. Telegram is explicitly simulated here.
Evidence: `/tmp/rm0008-image-indexing-real-mwhu0vyw/result.json`.

The stack watchdog's transition-time failure cleared on its ordinary timer:
the 16:39:07–16:39:57 CEST run exited 0 with `Result=success`. No manual
restart, timer change or BACHECA monitoring was introduced.

Release-51 preparation contains 1,771 files, census
`61c6df4f1c0c3045ee7c0f030cec1a57456469a997c0d6896f729cb01cae2106`;
reviewed private Python: 767 files,
`sha256:2556ba50c8cb8d3d7de85c33976fad5af3c05697caf228e681832a2c2ad1ef13`;
public Python: 755 files,
`sha256:25b397b3cf8f8f09000e59795f1dc33fb5eb3bf8d409fd925f75bffb436d3d17`.
Inventory entries and scan roots remain unchanged.

### Release 51: incremental completion and a health-shortcut collision

The ordinary cycle completed with exit 0; release 51 is selected and ready.
Source: `sha256:0e2ea9995baec01f6755e8253bca53699b193285fe10619789147ebb2aae3eee`;
build: `sha256:7048815b433d82361c216a1a081aa70712e378728b58572db0c9b7f883402544`;
head: `sha256:5fce7ebf4bf785b5613c93400b7a7176e630f091c6d3fb1b38bdc8b8a8b35d15`.
Evidence: `/var/lib/metnos-admin/rm0008-cycle-evidence-7048815b433d8236`.
Readiness `run-qk76exl9` passed with no active user turn.

Live `run-9rji3_kn`: turn `578eb1bf2517473e` admitted the exact incremental
request in 7.600 seconds, retaining the real durable receipt. Workload
`wrk_a9328cc3a4e44b269b86d21e8fd7ad0c` completed all five stages on attempt 1,
with `force=false`; owner-event and actual Telegram completion deliveries
reached `sent`. The follow-up search `e977198d773d4206` failed after 117.386
seconds without executing a tool. This is NOT an indexing failure, and the
successful incremental work must not be repeated to test the search fix.

Diagnosis `run-w0ku55a7` and a no-model probe show that the deterministic
hardware shortcut selected `get/processes`: `computer` came from the request,
but `service` came only from its path. The semantic model was never called
for intent extraction. This selected the wrong proposer pool, producing two
truncated/expanded attempts at an irrelevant selection form. The fix reuses
the same structural path filter for intent shortcuts, device machine focus,
health argument completion and health answer focus. The model and argument
binding continue to receive the original path. No photo keyword, machine path
or new domain-specific rerouting rule is introduced.

Related tests: 111 passed and 5 subtests, with two failures reproduced against
unchanged HEAD bytes using `/tmp/rm0008-baseline-health-tests.py`. Both baseline
failures depend on unsigned authoring catalogs (`recovery_challenger_realigned`
and `two_enforce_guards_compose`); no signature verification was disabled.
Fourteen new checks cover IT/EN, Unix/Windows/UNC paths, retained real hardware
shortcuts, unchanged model input and downstream health consumers.

Real-model E2E `run-usr6ztxg` passes with `metnos-service/share/live-check`
inside the synthetic path. HTTP turn `c8982c8b95f04dab` admitted in 7.838
seconds; work `wrk_9ead8c3dc97c4ce88d30d124df93361d` completed all five
stages on attempt 1; search `169ffe211f114ac8` returned only the computer in
6.890 seconds. Four old IT/EN rows upgraded from a protected seed; the real
VLM started on demand, 140 complete-generation observations had no errors.
Telegram is simulated in this isolated test. Evidence:
`/tmp/rm0008-image-indexing-real-pv9ttez_/result.json`.

Read-only `run-zpa7_8z9` proves the live synthetic index now contains 3 entries;
both real archive metadata hashes and their 30,937 entries remain unchanged.
Release-52 preparation: 1,771 files, census
`818a0050706cd742d6f3d027f05d2387f2404fc51bc0a71ac77d1b0a60ca58e1`;
private Python 767 files, `sha256:9e24972ee39c68c032fea42226e97225c183a079c9a2b19d677677460615fe62`;
public Python 755 files, `sha256:d08440cdcf4e7db8bab178a611c9768d25795e6241f186da615984a2a7462660`.
