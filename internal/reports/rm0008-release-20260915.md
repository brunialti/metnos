# RM-0008 — release and verification ledger, 2026-09-15

Status: candidate verified and committed; the owner-authorized recovery of the
unselected release-44 `RECEIPTS_COMPLETE` transaction passed the complete
independent ownership reader after a narrower rehearsal missed a context
dependency. Production release 43 remains selected and attests again.
Deployment and publication are still pending.

## Authorized scope

The owner authorized production deployment, a local commit and incremental
publication to the existing public Git repository after verification. No new
external search API, account, credential, subscription or provider is authorized.
Web relevance remains deferred under `WEB-SEARCH-001` in `internal/design/TODO.md`.
BACHECA monitoring is disabled and its files are excluded from these changes.

## Delivered candidate and evidence

- Shared, timezone-aware date/time resolution, constrained local interpretation,
  ordinary ambiguity selection and exact mail receipt filtering:
  [temporal and real mail evidence](rm0008-temporal-20260915.md).
- Automatic missing-index admission into resumable LRE phases, controlled local
  model startup, complete-generation publication and image relevance:
  [service-identity cold-start E2E](rm0008-image-indexing-20260915.md).
- Bounded central concurrency and durable model-token reservations:
  [parallelism evidence](rm0008-lre-parallelism-20260915.md).
- Capability-aware source access and honest empty-filter answers:
  [guard and finalizer evidence](rm0008-guard-empty-filter-20260915.md).
- Bounded web search/reranking without restoring rejected results:
  [web findings and explicit deferral](rm0008-search-e0de40fe-20260915.md).

The combined changed/related-code gate passed 1,313 tests, with 9 expected
skips and 8 subtests. Subsequent boundary guard tests passed 78 tests. Tutor
tests on the genuinely signed candidate passed 102 tests. The final public-doc,
photo-card, prompt and temporal-documentation group passed 74 tests. These groups
overlap; their counts must not be added as unique coverage. This is not a
claim that every test in the repository was run or passed.

The direct Tutor suite initially used unsigned edited authoring manifests and
an absent worktree model directory. It was rerun on a copied, current catalog
signed by a fresh fixture identity, with real installed BGE artifacts; no
production private key was copied and verification was not disabled. One
textual prompt assertion was updated to the new informational/instructional
contract without removing its natural-example, language or secret safeguards.

## Tutor convergence and preserved failed oracles

The original question is unchanged: `l'indicizzazione delle foto avviene
inautomatico?` (original turn `7a5207e0d0a14d1b`). Candidate tests use actual
HTTP routing, local models, a compiled signed catalog and no executed actions.

- `/tmp/rm0008-tutor-candidate-dkfc9q4e/result.json` passed structural checks
  but failed semantic review: it suggested a manual start/confirmation and
  invented an acronym expansion. It is not a successful final answer.
- `/tmp/rm0008-tutor-candidate-why8uy9e/result.json` exposed a contradictory
  Italian denial and a missing instructional example. The shared composition
  prompt now distinguishes explanatory questions from instructions, preserves
  conditional availability, and distinguishes a quoted example from execution.
- `/tmp/rm0008-tutor-candidate-1cho0uzf/result.json` contains a correct Italian
  answer, but an English request sent during fresh-instance initialization
  fell through to planning. A concurrent lexicon initialization warning is
  recorded; causation or a production regression is not established. This run
  is not counted as a complete pass. Subsequent tests wait for the ordinary
  catalog-ready event; they do not disable or replace startup code.
- The prepared runs under that same fixture independently verified both
  informational answers and both instructional openings. One helper initially
  read the asynchronous turn log too early; its bounded wait was corrected.
  Those controls exposed stale text in the published photo card, still saying
  that index creation could be proposed. The IT/EN card now agrees with the
  automatic first-search guide, with regression tests for both source classes.

The public guide keeps first-search admission, prerequisites, asynchronous
duration, completion notification and index reuse in one bounded source unit.
It also includes a search example, not an instruction to start indexing first.
Both public guides and the surviving Tutor card are authoritative inputs to
the signed catalog; neither may retain the obsolete manual-start explanation.

The final complete run passed all four unchanged questions after ordinary
startup, with no executor actions or evidence gaps:

| Case | Turn | Seconds |
| --- | --- | --- |
| Original Italian automatic-indexing question | `1690c4edbf504409` | 6.813 |
| English automatic-indexing question | `6b219c0a922e48b6` | 4.933 |
| Italian search instructions | `3be69d3edd2f4e15` | 7.283 |
| English search instructions | `15ffde23fd024a66` | 5.564 |

Evidence: `/tmp/rm0008-tutor-candidate-dtqo9sh7/result.json`. Fresh signed
catalog compilation took 134.282 seconds; catalog SHA-256 is
`574181c5a56275cda0678345396e814f6fe544c4251b27b53489a62bd15b3b7f`.
The two informational answers affirm the conditional automatic behavior without
a manual-start/consent instruction. The two how-to answers show an actual search
request, describe the asynchronous preparation and preserve the ordinary Tutor
explanation/action boundary. Prompt SHA-256: IT
`18aa8a8f3d736c7e1d356443f7f93b33653d65608de3082599c2932f4b52cad4`, EN
`56998ae72f7cea53a063e70fe2137f545f862eb697c235960ecfc01f3b9fc465`.

## Release safeguards and remaining confirmations

The boundary inventory explicitly classifies the new prerequisite callback as
`live_reader`: it only supplies `load_catalog(verify=True)` for signed rules;
ordinary argument, capability and durable-admission checks retain authority.
No guard role or compiled security exception was broadened.

Final production evidence must record the actual release sequence, source roots,
component-admission result, service health, live Tutor check, documentation
deployment and local/public commit identities below. Crossing is allowed only
with no active user turn. A crossing alone is not sufficient: component
publication and verification as the service identity must also complete.

Prepared source profiles after the explicitly reviewed recipe-pin correction:
private 767 Python files,
`sha256:93b8c7bcc55a6a3e1208e6bb3cd5c904451a18a5f9ca7ef7972ed69b770986d2`;
public 755 Python files,
`sha256:9b1e3be8246beb015a439bdfae2650d4ae4f96644a92836a066dfcb66e44904a`.

Large-archive performance remains for the owner's later test. The real photo
E2E uses two drawn images; multi-group interruption/resumption uses a 33-image
simulation. Telegram outbox/adapter delivery was simulated, not sent to a real
recipient. No user photo index was deleted or rebuilt.

## Production outcome

Initial local implementation commit:
`6faf1af910d3a6022ec97c4b40e2a576295a30f3`.

The first release-44 attempt was refused with `service source recipe`, after
the ordinary transition had stopped services and before cutover completion.
The reviewed LRE scheduler opt-in changed `SERVICE_SOURCE_V1`, but the autonomous
verifier still pinned the previous recipe. The existing differential material
test reproduces this omission. The corrected fixed pin is
`sha256:8d657fff0dbfff0c20aa650b5147598e26c481e697458064587d83962bb1bd4f`;
only the declared `METNOS_EXECUTOR_PARALLEL=1` worker setting is added. Both
independent readers accept it and reject a missing, zero or different setting.
No runtime-computed acceptance pin, drop-in or verifier bypass was introduced.

Read-only observation `run-x4ny0qlr` confirmed that release 43 still attested,
but the HTTP PID was zero and services were inactive. The narrow recovery
`run-ghbj6wl_` re-attested that exact predecessor, required the HTTP process
to be absent and restarted its ordinary `metnos.target` successfully. This
corrects the earlier assumption that a refused crossing left services running.
The failed candidate evidence is retained under
`/var/lib/metnos-admin/rm0008-cycle-evidence-bc05c911c0191c6a`.

The public preflight also refused the modified mail manifest's historical
commercial sender examples: its exact-payload exemption no longer applied.
Both languages now use reserved example domains, and the obsolete exemption
was removed. Signature-bearing export bytes are not rewritten to pass the
privacy gate. The 173-test recipe/catalog/privacy group passed, including the
new differential scheduler cases. The broader preflight/launch/material/TCB/
catalog/privacy group passed 486 tests, with 2 explicit skips. Public export
preflight completed with zero GII findings. Recovery health `run-gszk1cr1`
confirmed release 43, `ok=true`, `ready=true` and zero active HTTP turns.

A combined boundary group exposed a test-isolation defect: deleting an entry
from a shared policy dictionary and letting `monkeypatch` reinsert it changed
its insertion order for subsequent exact-order tests. The unchanged projection
suite passed all 54 tests alone. The mutant now replaces a local mapping copy,
preserving the original shared object and all refusal assertions; production
policy bytes and the compiled projection are unchanged. The exact combined
boundary/policy/language-state group then passed all 291 tests in 14.50 seconds.

The revised prepared tree contains 1,771 files, census
`771b92c2cc2c86e946c8dbb7d409e4cbc6d4905b954cd807da7b64e7347b8941`.
Corrective local commit: `d2c57065` (full identity recorded by Git).

### Confirmed recovery frontier and authorized extension

The next attempt first met a temporarily busy ordinary lock. A subsequent
unchanged retry passed that lock and was refused at `successor edge`; no
additional service stop, deployment or component admission took place.
Read-only authenticated observation `run-2003kkal` establishes:

- Selected release 43: `PREFLIGHT_VERIFIED`, sequence 6, required head
  `sha256:2198c104932c1e438d133008591c8206c6d95c9c8faa8bb26417e9dc84f07ba1`.
- Unselected release 44: `RECEIPTS_COMPLETE`, sequence 1; request
  `sha256:ab244a79f3f5d1a8cf64ed241f773c67ff2f79c3f7d4f366d6a6d7118189fd47`,
  provisioning transaction `63b3f175b9b3686cfeea094d83cfbf23`.
- The failed transaction has records 000 and 001 and a current receipt proof,
  but no head or certificate payload hash. There are no pending unopened claims.
- Its immutable source is
  `sha256:58c18c8ef00f974e363aab8347e0f413ad005e6bf34a5146d5836811bf793ea3`;
  the corrected received source is
  `sha256:a789ff6477d1f4ec705d3059617bbd0ad3cd4bb50c3a7cad80b195e94c03a090`.

Before this correction, the withdrawal helper accepted only an unopened claim or a sequence-zero
`PREPARED` transaction without receipts. The product abandonment path accepts
only a proved unattestable `HEAD_REQUIRED` crossing, sequence 5. Neither applies
to this frontier. Replaying the failed immutable source retains its recipe-pin
defect; changing its signed files or manufacturing a terminal state is not a
recovery option. No such operation was attempted.

The owner explicitly authorized correcting the release procedure so it can
withdraw this intermediate state. The local helper now accepts only `PREPARED`
or `RECEIPTS_COMPLETE`, verifies the full canonical journal, proves the exact
predecessor remains selected and archives the four exact superseded objects
with the claim last. Receipt proofs remain in the archived journal; backing
receipt and producer stores are not moved or changed. Later states, an unchanged
source and an initial release are not recoverable through this helper. The
product coordinator's state machine and abandonment rules are unchanged.

The independent service-recipe check now runs before crossing effects or service
stops, so the original stale-pin defect is detected without taking services down.
The release-procedure suite passed 158 tests; the combined procedure, independent
preflight material, service catalog and public privacy group passed 331 tests.

The initial, insufficient rehearsal `run-3527plgh` passed five cases: an uninterrupted withdrawal and a
restart after each of the four renames. It used copies of the actual public
control records and the real provisioning header, with private provisioning
payloads replaced by an opaque fingerprint stand-in; no signing key was copied.
Canonical readers verified every journal, selected history remained identical,
each archive preserved its exact input objects, and the ordinary next-release
edge became available. The startup observation on the copy was explicitly
frozen from a positive live predecessor attestation. Production objects were
fingerprinted before and after and remained unchanged. Rehearsed helper SHA-256:
`8941506a51c39ca72f5a131c27562f0cf6260a340c25606082749f3792dcf547`.

The production retry then stopped after the first rename because independent
startup authentication reported `orphan context transition`. The coordinator
graph alone does not validate every ownership-chain relationship; freezing the
startup observation in that rehearsal missed this dependency. Services were
not stopped and the selected release did not change. Recovery `run-jxnk7wu5`
matched the archived journal byte-for-byte against the earlier root-owned copy,
restored only that exact directory with a no-replace rename under all three
locks, preserved selected history and re-attested release 43. No record was
rewritten and no verifier refusal was ignored.

The corrected ordering archives the attempt's exact, content-addressed context
transition first, if published, while its coordinator journal still exists.
The canonical format and the journal binding are checked; other context records
and their inventory remain unchanged. The original four moves then follow,
still with the claim last. An interrupted prefix must preserve the selected
release's complete authentication, not only its coordinator graph. Procedure
tests now pass 172 cases; the combined group passes 345 tests in 2.65 seconds.
The replacement rehearsal authenticates copied control records using the
unchanged independent verifier and actual public signatures; private authority
files are zero-filled fixtures, never copied or used for signing. Its first
setup attempt (`run-jqopf78i`) was refused before testing because the administrator's
restrictive umask gave two newly created fixture directories the wrong modes;
the fixture now sets their exact required modes explicitly. Production remained
unchanged during that setup failure.

The complete replacement rehearsal `run-rw25y6e3` passed all six cases in
107.035 seconds: normal completion and an interruption after each of the five
moves. It reproduced the former orphan-context error before testing the corrected
ordering. Each case performed 9–11 independent authenticated-prefix checks with
the real public signatures, retained the exact archived bytes and selected
history, and exposed the normal next-release edge. Final live snapshots and
predecessor attestation were unchanged. Verified procedure SHA-256:
`5bbd8bbfc6d2c203560f70098d2726582e82e1020fcd7a5c3f1df9fee9f25e0b`.

A diagnostic import initially created a root-owned empty `workspace` directory
inside release 43, containing only empty `.mnestoma` and `.scheduler` directories.
This violated the immutable distribution inventory and caused scheduled checks
to refuse the release. The probe now uses isolated configuration and workspace
roots before importing runtime code. Recovery `run-5vh4r41d` validated the exact
three empty directories and moved them recoverably to
`/var/lib/metnos-admin/agent-runs/run-5vh4r41d/preserved-empty-workspace`.
No user workspace or index was touched. The rehearsal's live attestation then
confirmed release 43 again.

The existing public branch was checked read-only: local mirror, tracking ref and
remote `main` all point to `bbb6b0a214ea1f749e78b530a2858d65723f0471`.
All three documentation generators pass `--check`; the published-document
validator accepts 99 indexable IT/EN pages. The push and online deployment are
held until the release is completed, so the public result is not reported as
installed behavior. The final live Tutor helper is prepared but has not run;
candidate Tutor evidence above is not relabeled as production evidence.
