gravita: {'bloccante': 9, 'serio': 18, 'minore': 11} totale 38

==================== BLOCCANTE ====================

[B1] F4 — Dati (`episode_refs`, `relations`) e Innesti («Isolamento nella ricerca», «Lettura del registro turni»)
  DIFETTO: F4 chiave tutto su `owner_user_id`, mentre lo store nasce in F1 chiavato su `principal_id` (`memory_state`, `source_events`, `memories`, `evidence` hanno tutte la colonna `principal_id`). Tre conseguenze provate. (a) L'interrogazione dichiarata «`memory_fts JOIN memories … con memories.owner_user_id = ?`» punta a una colonna che F1 non crea: F4 non è costruibile sullo schema della fase precedente. (b) In `users.db` `owner_user_id` è il PROPRIETARIO dell'ospite, non l'ospite [PROVATO runtime/users.py:53 `owner_user_id TEXT` nella tabella `users`, `ROLES = ("host","guest")` a :42]: chiavare episodi e ricerca sull'owner fonde ospite e proprietario in un unico secchio, contro l'invariante 3 e UC-12, e contro §5.1/§5.2 che elencano `principal_id` come chiave dello snapshot. (c) `read_turn_for_principal` confronta `record["owner_user_id"]`, ma quel campo del TurnLog è oggi popolato dalla catena di ripiego `owner_user_id or actor or "host"` [PROVATO runtime/agent_runtime.py:6679, campo dichiarato a :3976 con default `""` e `actor: str = "host"` a :3972]: per i turni reali vale la stringa `host`, che non è un `users.id` e non coinciderà mai con `principal.owner_user_id`.
  RIMEDIO: Chiavare `episode_refs`, `relations` e ogni predicato di ricerca su `principal_id`, unica colonna che F1 crea, e dichiararle nel registro `store.CASCADE_TABLES` con quella colonna. Sostituire il confronto su `record["owner_user_id"]` con il `principal_ref` serializzato che F0 già aggiunge al TurnLog, rifiutando (`unavailable/no_principal`) ogni riga priva di riferimento — cioè tutte quelle anteriori a F0. Se serve anche la vista proprietario (quota, amministrazione), è una colonna in più, non la chiave.

[B2] F2 — Dati («Scrive nelle tabelle F1: `source_events` con `kind='user_clause'`/`'correction'`, `memories`, `evidence`») contro F1 — Contratti (`service.py`) e F4 — Fuori da questa fase
  DIFETTO: F2 deve scrivere un evento sorgente per ogni «ricorda»/«correggi», perché `evidence.event_id REFERENCES source_events(event_id) ON DELETE RESTRICT` (F1) e perché l'invariante 16 pretende che il claim citi una clausola reale. Ma nessuna fase ≤ F2 costruisce il percorso di scrittura di `source_events`: la facciata F1 espone soltanto `put_memories/get_memories/list_memories/inventory/forget/revisions/build_snapshot` — nessuna funzione di accodamento — e il tipo `NewMemory` usato nella firma non è definito da nessuna parte. Peggio, F4 dichiara esplicitamente che «l'accodamento di `user_clause`, il filtro di §7.2 e i codici di scarto restano F5», e F5 costruisce infatti `source_events.py` con `EventKind.USER_CLAUSE`. F2 quindi chiude la propria uscita (UC-01/UC-09/UC-10) usando un modulo che nasce tre fasi dopo.
  RIMEDIO: Assegnare a F1 l'accodamento autenticato senza filtro (`append_event(principal, kind, text, span, …) -> event_id`) e definire `NewMemory` come coppia claim+evento, così che `put_memories` scriva evento, memoria ed evidenza in un'unica transazione. F5 aggiunge sopra soltanto `filter_candidate`, i codici di scarto e `enqueue_from_turn`, senza reinventare la scrittura. In F2 esplicitare che «ricorda» passa da quella funzione di F1.

[B3] F5 — Interruttore («Non viene accodato **alcun** evento») e F5 — Innesti («Accodamento una-volta-per-turno») contro F4 — Innesti («Conteggi autorevoli del turno»)
  DIFETTO: Due difetti gemelli sullo stesso punto di strozzatura `TurnLog.write`. (1) Spegnere F5 rompe F4: F5 dichiara che a interruttore spento «non viene accodato alcun evento», ma nello stesso paragrafo promette che «restano attivi… gli episodi di F4», mentre `episode_refs.source_event_id` richiede un `source_events` di tipo `runtime_outcome`. Violazione diretta del criterio «spegnere la fase N lascia integre 0..N-1». (2) Accendendo entrambe, lo stesso evento è emesso due volte da due meccanismi diversi: F4 lo emette «subito dopo i conteggi» dentro `write()` e si affida a `UNIQUE(owner_user_id, turn_id)` di `episode_refs`; F5 lo emette con `enqueue_from_turn` protetto da un flag `_user_context_recorded` sul modello di `_canonical_recorded` [PROVATO runtime/agent_runtime.py:5476 e :4997]. Il duplicato non è intercettato da `idx_se_dedup(principal_id, kind, turn_id, payload_digest)` perché i due chiamanti costruiscono payload diversi; il conteggio degli «episodi indipendenti» di F5 (§7.3) e `n_success` di F6 raddoppiano, e la soglia scatta a metà delle prove richieste.
  RIMEDIO: Un solo emettitore: F4 costruisce l'accodamento di `runtime_outcome` in `write()` e lo dichiara indipendente dall'interruttore di F5 (è un dato di esito, non apprendimento); F5 consuma quegli eventi e aggiunge soltanto `user_clause`/`correction`, il filtro e i codici di scarto. Un'unica prova di invariante: per ogni `turn_id` esiste al più una riga `source_events(kind='runtime_outcome')` e al più una riga `episode_refs`.

[B4] F3 — Ordine di costruzione punto 3 e Dati (`user_defaults`, `"references"`) contro F1 — «Ampiezza dell'oblio» (decisione ratificata 1)
  DIFETTO: La risoluzione del bersaglio per chiave è congelata in F1 su due sole superfici: `delete_pref` su W2 e le righe di `memories` con lo stesso `(principal_id, slot_key, scope)`. F3 introduce due contenitori di valori tipizzati chiavati esattamente allo stesso modo — `user_defaults(user_id, key, scope_key)` e `"references"(principal_id, slot, alias_norm)` — ma estende soltanto la cascata di cancellazione del PRINCIPALE («una cancellazione del principale svuota le tre tabelle»), non il ramo `DeleteScope.KEY`. Esito concreto: dopo «dimentica il calendario del lavoro» la preferenza sparisce e il default operativo in `user_defaults` sopravvive e continua a riempire l'argomento dopo il piano — cioè il gemello tipizzato già compilato sopravvive in silenzio, che è precisamente ciò che la decisione 1 vieta. Il registro `CASCADE_TABLES` di F1 non copre il buco: è dichiarato per le tabelle dello store memorie, mentre `user_defaults` vive in `users.db`.
  RIMEDIO: Trasformare anche la risoluzione per chiave in un registro estensibile (`KEY_TARGETS`: store, tabella, colonne di chiave e di ambito, funzione di cancellazione), con una prova di invariante che diventa rossa quando una fase aggiunge un contenitore chiavato per `(chiave, ambito)` senza dichiararlo. F3 vi registra `user_defaults` e `"references"`, F6 `routine_bindings`.

[B5] F0 — «Ordine di costruzione» passo 4 + colonna «Innesti» (i «dodici ripieghi censiti»)
  DIFETTO: Il criterio di falsificazione («l'insieme dei rifiuti deve coincidere ESATTAMENTE con i dodici ripieghi censiti, ne' uno in piu' ne' uno in meno») non e' soddisfacibile: il censimento e' una selezione, non un'enumerazione. `grep -rn 'or "host"' runtime/ --include=*.py` da 66 occorrenze [PROVATO]; tre di esse sono citate NELLA STESSA tabella senza essere numerate — `runtime/orchestration.py:604` `actor=str(state.get("actor") or "host")` [PROVATO], `runtime/agent_server.py:234` `actor=rec.get("actor") or "host"` [PROVATO], `runtime/http_routes_agent.py:1647` `actor = final_state.get("actor") or "host"` [PROVATO] — e altre non citate affatto (`runtime/engine/executor.py:1913` `actor=args.get("_actor") or "host"` [PROVATO], `runtime/agent_runtime.py:3470,3603,3701,4471,5736,6397,6743,6917` [PROVATO]). Un passo che non puo' passare non e' verificato: e' bloccato.
  RIMEDIO: Sostituire il numero fisso con un inventario generato a macchina (regex su `or "host"` piu' i confronti `actor == "host"`) committato come impronta, e far fallire il passo quando l'inventario cambia, non quando non fa dodici.

[B6] F4 «Dati» (tabella `relations`) contro F5 «Dati» (paragrafo «`relations` non e' creata qui»)
  DIFETTO: Le due fasi descrivono schemi incompatibili della stessa tabella. F4 scrive `decided_by TEXT NOT NULL CHECK (decided_by IN ('deterministic'))` e `kind ... CHECK (kind IN ('supersedes'))`; F5 pretende che «lo schema di F4 deve gia' portare `kind IN ('supports','contradicts','supersedes')` e `origin IN ('deterministic','model_proposed')`» — colonna con nome diverso (`decided_by` vs `origin`) e vincolo diverso. In piu' F4 promette che «F5 amplia il CHECK», mentre SQLite non consente di modificare un CHECK senza ricostruire la tabella, migrazione che nessuna delle due fasi pianifica. Come scritto, F5 non e' costruibile su F4.
  RIMEDIO: Fissare in F4 il nome definitivo della colonna e il vocabolario pieno (`kind` tre valori, `origin` due), lasciando a F5 solo il permesso di scrivere `model_proposed`, cosi' nessuna ricostruzione di tabella serve.

[B7] F1 «Ampiezza dell'oblio», risoluzione per `MEMORY_ID` via `claim_digest`
  DIFETTO: Il digest dei claim e' definito «calcolato sulla normalizzazione gia' esistente `normalize_query`». Quella funzione e' tarata sulla cache L0, non sulla semantica: la sua lista di parole vuote comprende imperativi e verbi pieni — `dimmi, mostrami, mostra, fammi, fai, quali, sono, che, cosa, show, tell, give, list, do, does` [PROVATO `runtime/engine/cluster.py:76-105`, non 78-109 come citato]. Ne segue che «fai il caffe'» e «caffe'» producono lo STESSO digest, e un «dimentica» su uno cancella l'altro; e ogni futura taratura della cache cambia in silenzio che cosa muore con un oblio. E' la dipendenza opposta a quella che il documento dichiara di proteggere (§5.10, «nulla di personale entra nelle cache»).
  RIMEDIO: Definire in F1 una normalizzazione propria dell'oblio (minuscole, spazi, punteggiatura, NIENTE parole vuote) in `user_context/`, e vietare per prova l'import di `engine.cluster` dai moduli di memoria.

[B8] F0 «Dati» (`user_revisions.deletion_epoch`), F1 «Dati» (`memory_state.deletion_epoch` e `ALTER TABLE users ADD COLUMN memory_deletion_epoch`), F1 `deletion_journal.epoch_of`
  DIFETTO: Lo stesso valore esiste in quattro luoghi (giornale autorevole, `memory_state`, `user_revisions`, colonna in `users`) e nessuna riga dice quale prevale quando divergono. `continuity(principal_id, mirrored_epoch)` accetta «lo specchio» al singolare senza dire quale dei tre. Un oblio e' esattamente l'operazione in cui la divergenza si manifesta. Difetto affine: F3 apre la sezione «Dati» con «`prefs_revision` nasce in F1», mentre F0 e F2 lo creano in F0.
  RIMEDIO: Un solo specchio (`memory_state`), giornale unica autorita', cancellare `user_revisions.deletion_epoch` e `users.memory_deletion_epoch`, e correggere in F3 la fase d'origine di `prefs_revision`.

[B9] F4 «Innesti» riga «Isolamento nella ricerca» e «Dati» (`episode_refs`, `relations`)
  DIFETTO: F4 interroga «`memory_fts JOIN memories ON …` con `memories.owner_user_id = ?`», ma la tabella `memories` definita in F1 NON ha quella colonna: ha `principal_id`. Le due tabelle nuove di F4 sono poi chiavate su `owner_user_id`, mentre F1, F5 e F6 usano `principal_id`. Non e' solo nomenclatura: `PrincipalContext` distingue `principal_id` da `owner_user_id`, quindi per un ospite gli episodi finiscono nel secchio del proprietario e `recall_episodes` restituirebbe i turni altrui — contraddicendo la prova avversariale di F2 «l'ospite non vede i claim del proprietario».
  RIMEDIO: Usare `principal_id` in TUTTE le tabelle dello store e nelle interrogazioni, e aggiungere una prova che rifiuta uno schema con una colonna di soggetto diversa da quella dichiarata in `CASCADE_TABLES`.

==================== SERIO ====================

[S1] F5 — Dati («Le due chiavi di stato `implicit_learning` e `compiler_mode` sono righe di `memory_state` (F1), non colonne nuove») contro F1 — Dati (`CREATE TABLE memory_state`)
  DIFETTO: `memory_state` nasce in F1 come tabella a UNA riga per principale con colonne fisse (`principal_id` chiave primaria, `memory_revision`, `deletion_epoch`, `policy_version`, `rows_active`, `bytes_total`, `health`, `updated_at`): non è un contenitore chiave-valore e non può ospitare «righe» di stato. F5 nega di aggiungere colonne mentre di fatto pretende due campi che F1 non crea, e ne fa dipendere il controllo in chat («non imparare più automaticamente») e la precedenza restrittiva riga-su-ambiente.
  RIMEDIO: Scegliere e dichiarare: o due colonne in `memory_state` aggiunte da F5 con migrazione additiva idempotente (coerente con il resto dello schema), o una tabella `principal_settings(principal_id, key, value)` con vocabolario chiuso, in ogni caso registrata nella cascata dell'oblio. Correggere la frase che nega l'estensione dello schema.

[S2] F5 — Dati («lo schema di F4 deve già portare `kind IN ('supports','contradicts','supersedes')` e `origin IN ('deterministic','model_proposed')`») contro F4 — Dati (`CREATE TABLE relations`)
  DIFETTO: F5 esige da F4 una tabella che F4 non costruisce, ed è la stessa trappola che la correzione ratificata voleva chiudere: F4, per decisione, crea `relations` limitata al `supersedes` deterministico, con `kind IN ('supersedes')` e la colonna chiamata `decided_by`, non `origin`. Inoltre F4 promette che «F5 amplia il CHECK»: in SQLite un vincolo `CHECK` non è alterabile con `ALTER TABLE`, serve ricostruire la tabella e ricopiare le righe. La dipendenza è quindi doppiamente insoddisfatta: nome di colonna diverso e vincolo non estendibile come dichiarato.
  RIMEDIO: Fissare in F4 il nome definitivo della colonna (`origin`) e mantenere il vincolo stretto sui valori ammessi in F4 (`kind='supersedes'`, `origin='deterministic'`); in F5 dichiarare esplicitamente la migrazione per ricostruzione (tabella nuova, copia, rinomina, indici) come passo numerato con prova di idempotenza, invece della formula «amplia il CHECK».

[S3] Interruttori: F0 `METNOS_USER_CONTEXT`, F1 `METNOS_USER_MEMORY`, F2 `METNOS_USER_CONTEXT_CHAT`, F3 `METNOS_USER_CONTEXT_F3`, F4 `METNOS_USER_CONTEXT_F4`, F5 `METNOS_USER_CONTEXT_IMPLICIT`, F6 `METNOS_USER_ROUTINES`
  DIFETTO: Sette interruttori indipendenti, nessuna relazione di prerequisito dichiarata e nessun comportamento definito per le combinazioni incoerenti, che sono la maggioranza. Due esempi concreti e raggiungibili: (a) `METNOS_USER_CONTEXT_CHAT=1` con `METNOS_USER_MEMORY=off` — F1 dice che a interruttore spento «nessun file `user_memory.sqlite` viene creato», quindi «ricorda»/«dimentica»/inventario di F2 non hanno store e l'unico esito onesto sarebbe `UNAVAILABLE` su una capacità appena accesa; (b) `METNOS_USER_CONTEXT_F3=1` con `METNOS_USER_CONTEXT=off` — nessun principale costruito, quindi `applications` registrerebbe un soggetto non verificato (invariante 1).
  RIMEDIO: Un solo lettore centrale (`user_context.flags`) che risolve i sette valori in una fase effettiva monotona: la fase N è attiva solo se 0..N-1 lo sono; una combinazione incoerente è un rifiuto all'avvio con messaggio esplicito, non un degrado silenzioso. Prova unitaria a matrice sulle 128 combinazioni: ognuna deve mappare su una fase effettiva o su un rifiuto nominato.

[S4] F0 — Contratti (`policy.py`: `SourceKind`, `Purpose`), F1 — Contratti (`contracts.py`: `SourceKind`), F3 — Contratti (`TurnInvocationContext.purpose: PurposeCode`), F4 — Contratti (`contracts.py`: `Purpose`), F5 — Contratti (`source_events.py`: `EventKind`)
  DIFETTO: Gli stessi due vocabolari chiusi sono definiti tre volte, in moduli diversi e con membri diversi. `SourceKind`: F0 lo mette in `policy.py` con sei membri (compresi `EXTERNAL_TEXT` e `ASSISTANT_PROSE`, entrambi vietati), F1 lo ridefinisce in `contracts.py` con quattro membri, F5 lo ribattezza `EventKind` con gli stessi quattro. Due enum omonimi in due moduli che si importano a vicenda sono un errore di importazione o, peggio, due verità. `Purpose`: F0 ne definisce uno di cinque membri (`PRESENTATION/ARG_FILL/REFERENCE/RETRIEVAL/INVENTORY`) che non compare in RM-0001; F4 ne definisce un altro di sei membri, questo sì conforme a §5.8 (`profile_inventory`, `profile_answer`, `reference_resolution`, `argument_default`, `episode_recall`, `routine_resolution`); F3 usa un terzo nome, `PurposeCode`, che nessuna fase definisce. Con `budget_for(purpose)` di F0 tarato sul vocabolario sbagliato, i tetti di §2.7/invariante 24 sono applicati a chiavi che i chiamanti non usano.
  RIMEDIO: Un solo `contracts.py` (F0) come sede di tutti i vocabolari chiusi: `SourceKind` a quattro membri per gli eventi ammessi, un elenco separato e distinto per le origini VIETATE usato da `source_admissible`, e `Purpose` con i sei valori di §5.8. `policy.py` importa, non ridefinisce; F1/F4/F5 non introducono enum omonimi; `PurposeCode` sparisce.

[S5] F2 — Prove («forma dentro una citazione, un blocco di codice o un allegato non riconosciuta, invariante 4») contro F5 — Contratti (`filter_candidate`, `RejectReason.EXTERNAL_ORIGIN/ATTACHMENT/CODE_BLOCK/TOOL_OUTPUT`) e F5 — Innesti (nuovo seed `uc_quote.*`)
  DIFETTO: Il filtro d'origine dell'invariante 4 è costruito due volte con due meccanismi diversi: in F2 dentro il riconoscitore del confine chat (concetti `usercontext.*` ancorati alla clausola più esclusioni proprie), in F5 dentro `filter_candidate` con codici di scarto propri e una nuova famiglia lessicale `uc_quote.*`. Nessuna delle due sezioni dichiara di riusare l'altra. Due implementazioni della stessa difesa divergeranno: una pagina che dice «ricorda che…» può essere respinta dal ramo implicito e accettata dal ramo esplicito, o viceversa, e le prove avversariali di F2 e F5 non misurano lo stesso codice.
  RIMEDIO: Un solo predicato d'origine in `policy.py` (F0), puro e senza I/O: `origin_admissible(text, span, context) -> RejectReason | None`, con l'unica famiglia lessicale `uc_quote.*` seedata in F2. F2 e F5 lo chiamano; le trappole del corpus congelato sono eseguite una volta sola contro quel predicato.

[S6] F6 — Ordine di costruzione punto 1 («congiunge gli eventi `runtime_outcome=success` con `observations.framework_json` per `turn_id`») e F6 — Innesti («Osservazione W1»)
  DIFETTO: La fonte scelta da F6 non contiene i casi che F6 deve imparare. `record_observation` è chiamata solo sul percorso di pianificazione completa [PROVATO runtime/engine/dispatch.py:6197] e sull'esito di un piano L1 [PROVATO :6034]; il ramo che serve un hit L0 esegue e ritorna senza registrare alcuna osservazione [PROVATO :5896-5960, dove compaiono solo `_maybe_record_fastpath` a :5954 e `record_step_run` a :5960]. Le esecuzioni RIPETUTE della stessa forma — cioè esattamente ciò che rende una routine — sono servite da L0 e non lasciano riga in `observations`: `n_success` resta bloccato a uno e la soglia `METNOS_ROUTINE_MIN_SUCCESS` non si raggiunge mai per costruzione. In parallelo, la stessa informazione è già costruita da F4 in altro modo: `episode_refs.action_shape` («soli nomi canonici di executor, `>` separatore») è emessa a ogni turno, hit L0 compresi, e duplica ciò che F6 ricava dai piani.
  RIMEDIO: Fare di `episode_refs` (F4) l'unica fonte delle forme: aggiungervi, accanto ad `action_shape`, i nomi degli argomenti che hanno viaggiato per `from_step`/segnaposto (mai i valori), e ricavare `steps`, `slot_names` e `context_sig` da lì. `observations` resta materiale W1 e F6 non la legge, il che elimina anche la doppia costruzione della stessa forma canonica.

[S7] F6 — Dati (`routine_bindings.n_success`, `last_success_at`, `expires_at`) e F5 — Dati («I contatori di episodi indipendenti si derivano da `evidence` ⋈ `source_events`»)
  DIFETTO: Violazione dell'invariante 27 (nessun auto-rinforzo). In F6 una routine attiva riscrive la richiesta, il turno riesce, l'esito diventa un `runtime_outcome=success` che il passo notturno riconta come evidenza della routine stessa: `n_success` cresce, `last_success_at` avanza e la scadenza `behavioral` si sposta in avanti all'infinito, e la recenza fa vincere quella routine sulle rivali. In F5 vale lo stesso per i default: la promozione richiede «tre episodi indipendenti», ma l'indipendenza non è definita e nulla esclude i turni in cui il default promosso era già applicato. Il dato per escludere questi turni esiste: F3 registra ogni influenza esercitata in `applications`, e F6 stesso scrive `routine_id` nel registro del turno.
  RIMEDIO: Definire l'indipendenza in modo deterministico e verificabile: un evento `runtime_outcome` è evidenza per un claim o una routine solo se il turno NON porta un'applicazione di quel claim (`applications` con lo stesso `source_id`) e non porta quel `routine_id`. Prova di invariante: una routine attiva usata cento volte non aumenta `n_success` di una unità e la sua scadenza non si sposta.

[S8] F0 — Innesti (i «dodici ripieghi censiti») e F0 — Ordine di costruzione punto 4 («l'insieme dei rifiuti deve coincidere ESATTAMENTE con i dodici ripieghi censiti, né uno in più né uno in meno»)
  DIFETTO: Il censimento è provatamente incompleto e la prova che ne dipende è quindi tarata su un denominatore falso. Nel solo codice attivo `runtime/` ci sono 66 occorrenze di `or "host"` in 21 file [PROVATO: `grep -rn 'or "host"' runtime/ --include=*.py | wc -l` → 66], contro i sei file censiti. Manca in particolare il ripiego che sta sul percorso usato da F3 per riempire gli argomenti personali: `args["_actor"] = actor or "host"` al punto di strozzatura dell'invocazione [PROVATO runtime/agent_runtime.py:3603], più `orchestration.py:604` e `:1215` sulle riprese e `channels/daemon.py:464` sulla consegna. Poiché F3 promette di sostituire «i dodici ripieghi censiti», tutto il resto sopravvive alla fase che doveva chiuderli, e l'uscita di F0 («nessun percorso memoria accetta… solo `actor`») è verificata contro un elenco che non è la popolazione reale.
  RIMEDIO: Sostituire il numero fisso con un inventario generato: una prova che enumera a macchina le occorrenze di ripiego d'identità (`or "host"`, `== "host"`, `actor or`, default `actor="host"` nelle firme) e le confronta con un elenco di esenzioni motivate, riga per riga. Il passo diventa rosso quando compare un ripiego nuovo o quando un'esenzione non è più raggiungibile; F3 lavora su quell'elenco, non su un numero.

[S9] F6 «Ordine di costruzione» passo 1 e «Prove — Turno reale»
  DIFETTO: Citazione [PROVATO] falsa: «oggi accade per costruzione su Telegram [PROVATO `runtime/channels/daemon.py:1785-1788`: `run_turn` senza `owner_user_id`]». Le righe 1783-1788 sono il ramo `_consume_get_inputs_response` [PROVATO]; l'unica chiamata a `run_turn` del demone e' a `channels/daemon.py:1894-1897` [PROVATO] e passa `actor`, `channel`, `reference_images` e NON `owner_user_id`. La tesi regge, la prova indicata no — ed e' la prova su cui poggia l'astensione di F6 su Telegram.
  RIMEDIO: Correggere la citazione in `runtime/channels/daemon.py:1894-1897`.

[S10] F3 «Innesti», riga «convalida della sezione al caricamento»
  DIFETTO: «`credentials._validate_form_spec` (`runtime/credentials.py:203`) gira solo alla prima invocazione [PROVATO]» e' falso in senso peggiorativo: non esiste alcuna memoizzazione in `runtime/credentials.py` [PROVATO: nessun `lru_cache`, `credential_form_kinds` a `:231`] e il registro la invoca con una lambda nuda — `runtime/agent_runtime.py:3541` `"credential_forms": lambda: _import_credentials().credential_form_kinds()` [PROVATO] — quindi la validazione e la rilettura da disco di TUTTI i manifest avvengono a OGNI riempimento, non alla prima. Un documento normativo che dichiara provato un costo minore di quello reale fa prendere la decisione sbagliata.
  RIMEDIO: Riscrivere la riga come «gira a ogni invocazione, rileggendo il catalogo da disco» e usarla come motivazione rafforzata per lo spostamento al caricamento.

[S11] F3 «Innesti», riga «ammissione e autorita'»
  DIFETTO: Citazione [PROVATO] sbagliata: «una clausola `when` (es. `client` in `executors/create_events/manifest.toml:104`)». La riga 104 e' `hint = ["~/.local/share/metnos/calendar.ics"]` della capacita' `fs:write`; la clausola `when = { arg = "client", values = ["google_workspace"] }` sta a `:109` [PROVATO]. (La citazione gemella `executors/read_urls_html/manifest.toml:171` e' invece esatta [PROVATO].)
  RIMEDIO: Correggere in `executors/create_events/manifest.toml:109`.

[S12] F5 «Innesti», riga «Accodamento una-volta-per-turno»
  DIFETTO: «E' l'unico punto attraversato dai 14 `log.write(); return log` di `run_turn` [PROVATO :6413…:7172]». I conteggi reali: `grep -c 'log\.write()' runtime/agent_runtime.py` = 15, `grep -c 'log\.write(); return log'` = 10 [PROVATO]; e `:6413` e' un `log.write()` isolato, non la forma citata [PROVATO]. La stessa quantita' e' data come «15 siti» in F4 e «14» in F5: il dossier si contraddice su un numero che ha contato male.
  RIMEDIO: Scrivere «15 chiamate a `TurnLog.write`, di cui 10 nella forma `log.write(); return log`» e allineare F4 e F5.

[S13] F0 «Innesti» riga «Verifica del Tutor» + «Ordine di costruzione» passo 8
  DIFETTO: F0 sostituisce `runtime/tutor/handoff.py:35` `if any(chunk not in query for chunk in chunks)` [PROVATO] e riscrive `split_query_chunks` — cioe' tocca la segmentazione da cui dipende un sottosistema certificato a 115+7/134 con banda di rumore misurata di 1 caso — e dichiara come unica falsificazione «rigioco di 40 turni reali». Il numero 40 non ha alcuna misura alle spalle e non e' lo strumento che quel sottosistema usa. Un cambio in quella posizione puo' passare i 40 turni e perdere casi nella certificazione.
  RIMEDIO: Aggiungere al passo 8 la ri-esecuzione della certificazione Tutor a macchina scarica, con la regola gia' ratificata «nessun verdetto sotto i 3 casi».

[S14] F2 «Innesti» riga «Confine HTTP» + «Ordine di costruzione» passo 5
  DIFETTO: Il confine viene inserito PRIMA del Tutor a `runtime/http_routes_agent.py:1072` [PROVATO], e il testo stesso ammette che «Che cosa sai di me?» oggi la prenderebbe il Tutor. La verifica prevista e' «turno reale» piu' 30 richieste-esca: nessuna delle due puo' misurare la sottrazione di casi al Tutor. Inoltre le «30 richieste-esca» sono un numero comparso dal nulla (il corpus §11.1 e' descritto altrove con «circa 5 casi» per la sola categoria nuova).
  RIMEDIO: Condizionare l'accensione del ramo inventario al mantenimento del punteggio Tutor sulla certificazione esistente, e derivare la numerosita' delle esche dalla partizione etichettata del corpus invece che fissarla a 30.

[S15] F0/F1/F2 — le tre riscritture di `set_pref`/`delete_pref`/`list_prefs`
  DIFETTO: Il dossier riapre quelle funzioni tre volte (transazioni, revisione, tipo di ritorno, `source`) e non sana mai la violazione §7.13 che vi risiede: gli errori di validazione sono stringhe italiane cablate — `runtime/users.py:700` «utente sconosciuto: …», `:703` «pref sconosciuta: … (valide: …)», `:708-709` «valore '…' non valido per … (ammessi: …)» [PROVATO]. F2 instrada i comandi di chat proprio dentro `set_pref`, quindi quelle stringhe diventano user-facing su HTTP e Telegram: la fase peggiora la violazione mentre riscrive la funzione.
  RIMEDIO: In F2, sostituire i tre ritorni d'errore di `set_pref`/`delete_pref` con codici risolti da `messages.get`, con chiavi IT+EN nel seme.

[S16] F2 «Ordine di costruzione», confronto fra passo 5 e passo 8
  DIFETTO: Il passo 5 accende il ramo inventario e lo verifica con un turno reale (quindi testo consegnato all'utente), mentre le chiavi i18n arrivano solo al passo 8 («Ramo oblio + i18n + superficie admin»). Nel mezzo l'inventario mostra all'utente marcatori tecnici (`not_implemented`, `ready`) e testo non risolto per chiave: §7.13 violata dall'ordine stesso dei passi.
  RIMEDIO: Spostare le chiavi i18n dell'inventario al passo 2, prima di qualunque ramo che consegni testo.

[S17] F0 «Contratti» (`policy.classify_sensitivity`) contro F5 «Innesti» (famiglie `uc_sensitive.*`)
  DIFETTO: Due meccanismi diversi decidono la stessa cosa e nessuna riga dice quale prevale: F0 definisce `classify_sensitivity(text) -> Sensitivity` «tabellare, zero LLM» — per giunta senza parametro di lingua, quindi legata a un solo locale — e F5 introduce famiglie a radice flessiva IT+EN in `detection_lexicon` per lo stesso giudizio. Due fonti di verita' sul confine «sensibile» significano che F2 puo' rifiutare cio' che F5 accetta.
  RIMEDIO: Cancellare la tabella di F0 e dichiarare `detection_lexicon` fonte unica fin da F0, con `classify_sensitivity` ridotta a lettore del lessico.

[S18] F5 «Prove — Integrazione» («Tre episodi indipendenti … due episodi → resta candidate») e F6 «Ordine di costruzione» passo 3 (`METNOS_ROUTINE_MIN_SUCCESS`)
  DIFETTO: Soglie normative senza misura: F5 fissa 3 nelle prove pur dichiarando due righe sopra che «le soglie sono congelate in F0» e «preregistrate»; F6 nomina un ambiente-soglia senza dare alcun valore predefinito, unica variabile del dossier priva di predefinito. Una soglia decisa dentro la prova che dovrebbe verificarla non verifica nulla.
  RIMEDIO: Spostare entrambe le soglie nel congelamento di F0 con la misura che le giustifica, e citare la sola costante in F5 e F6.

==================== MINORE ====================

[M1] F3 — Interruttore («è ripristinata la catena `args_defaults`… cioè il comportamento di F2 è identico byte a byte») e F3 — Innesti («cattura implicita», `runtime/args_resolver.py:201`)
  DIFETTO: L'interruttore di F3 non copre tutte le modifiche che F3 introduce, quindi spegnerlo non restituisce il comportamento di F2. `remember_scope_args` smette di memorizzare gli argomenti dichiarati personalizzabili [PROVATO runtime/args_resolver.py:201, che scrive `args_defaults.set_default(actor, domain, arg, str(val))` a :215] e l'elenco di ciò che l'interruttore ripristina non menziona questa soppressione: con F3 spenta l'argomento non viene né riempito dal riempimento personale né più ricordato da `args_defaults`, cioè un comportamento che in F2 esisteva sparisce. Analogamente la convalida al caricamento è dichiarata attiva anche a interruttore spento e può togliere un executor dal catalogo: è ammissione, non comportamento, ma allora la frase «identico byte a byte» è falsa e va corretta.
  RIMEDIO: Legare la soppressione della cattura implicita allo stesso interruttore del riempimento (spenta la fase, `remember_scope_args` si comporta come in F2), e riformulare la garanzia dell'interruttore come «identico a F2 salvo il catalogo, che può rifiutare un manifest malformato con motivo esplicito», con la prova di parità eseguita su un catalogo che non contiene sezioni `[personalization]` invalide.

[M2] F0 — Dati (`user_revisions.deletion_epoch`) contro F1 — Dati (`ALTER TABLE users ADD COLUMN memory_deletion_epoch`) e F1 — Contratti (`deletion_journal.continuity(principal_id, mirrored_epoch)`)
  DIFETTO: Lo stesso valore rispecchiato è materializzato due volte nello stesso database: F0 crea la colonna `deletion_epoch` in `user_revisions` (dichiarandola «solo uno specchio»), F1 aggiunge una seconda colonna `memory_deletion_epoch` alla tabella `users`. `continuity()` confronta il journal con «quello rispecchiato» senza dire quale dei due, e `build_snapshot` legge un `deletion_epoch` solo. Due specchi dello stesso valore autorevole si disallineano, e §5.2 dice che uno snapshot non è valido se uno dei due valori è arretrato — con tre valori la regola non è più applicabile.
  RIMEDIO: Un solo specchio. Poiché F1 possiede il journal, tenere la colonna in `user_revisions` (creata in F0 insieme alle altre due revisioni) e togliere `users.memory_deletion_epoch`; `continuity()` confronta journal e `user_revisions`, e la prova di riavvio verifica che non esista una seconda colonna di epoch nello schema.

[M3] F3 — Innesti («`prefs_revision` nasce in F1») e F5 — Innesti («la stessa transazione che incrementa `prefs_revision` (F1)») contro F0 — Dati (`user_revisions`) e F2 — Dati («nasce in F0 §6.1»)
  DIFETTO: L'attribuzione di fase di `prefs_revision` e delle transazioni esplicite su `users.db` è dichiarata due volte in modo incompatibile: F0 la crea con `user_revisions` e F2 lo conferma, mentre F3 e F5 la assegnano a F1. Non è un dettaglio redazionale: F2 elenca fra le cose che restano attive a interruttore spento «l'incremento delle revisioni», e questa promessa è vera solo se il lavoro è in F0. Se la fase è F1, F2 acceso con F1 spento resta senza revisione monotona e il rilevamento di snapshot stantia dell'invariante 2 non funziona.
  RIMEDIO: Fissare in un punto solo (F0, coerente con §6.1 e con la tabella di §13 che assegna `runtime/users.py` a F0) e correggere le due frasi in F3 e F5. Aggiungere alla chiusura di F0 la prova di proprietà già prevista: per ogni funzione pubblica che muta identità o preferenze, la revisione dopo è strettamente maggiore.

[M4] F1 — Contratti (`service.inventory(principal, *, include_deleted) -> ListPage`) contro F2 — Contratti (`presentation.py`: `InventorySection`, `register_section`, `inventory(principal) -> tuple[InventorySection, ...]`)
  DIFETTO: Due funzioni omonime con forma di ritorno diversa costruiscono lo stesso oggetto utente («che cosa sai di me») in due moduli diversi, e la seconda vive per giunta in `presentation.py`, che nella direzione delle dipendenze di §13 non è la sede dell'inventario. F1 promette inoltre un inventario «byte per byte identico in due esecuzioni» sul proprio formato, mentre l'inventario visibile all'utente è quello di F2 con le sezioni registrate: la prova di determinismo di F1 non copre l'artefatto che l'utente vede, e `DeleteOutcome.residual` (decisione 1) è prodotto dal formato di F1, non da quello di F2.
  RIMEDIO: Un solo inventario, in `service.py` (F1), con il registro delle sezioni come parametro; F2 aggiunge le voci localizzate e i comandi naturali, non una seconda funzione. `DeleteOutcome.residual` e la risposta «che cosa sai di me» attraversano lo stesso costruttore, e la prova di riproducibilità si applica a quello.

[M5] F4 — Contratti (`EpisodeOutcome`, `turn_semantic_outcome`) e F4 — Dati (`episode_refs.outcome CHECK`)
  DIFETTO: Il vincolo ammette cinque valori (`success`, `partial`, `error`, `timeout`, `pending`), ma la funzione pura che F4 stessa rende autorevole ne produce tre: `turn_semantic_outcome(final_kind, counts)` deriva da `final_kind` e dai conteggi, e la derivazione odierna che F4 riloca conosce solo successo, parziale ed errore [PROVATO runtime/recurring_tasks.py:479-492, `_scheduled_turn_outcome`, con la convenzione `None` = successo]. `timeout` non è producibile da alcun percorso dichiarato; inoltre `pipeline_effect_counts` può ritornare `None` per un turno non giudicabile [PROVATO runtime/pipeline_effects.py:83-95] e nessuna sezione dice quale esito riceve un episodio senza conteggi — che non è né successo né errore, ed è proprio il caso che l'invariante 28 e §6.3 vogliono escludere dalle evidenze.
  RIMEDIO: Togliere `timeout` dal vincolo finché non esiste un percorso che lo produce, e aggiungere il valore esplicito per `counts is None` (per esempio `not_countable`), escluso per costruzione dalle evidenze di F5 e F6. Prova unitaria: l'insieme dei valori restituibili da `turn_semantic_outcome` coincide con l'insieme del vincolo `CHECK`.

[M6] F1 — Contratti (`NewMemory`, `ListPage`, `JournalEntry`, `ReplayReport`), F0 — Contratti (`Budget`), F5 — Contratti (`RelationKind`, `PreferenceSpec`, `CompileReport`, `PromotionResult`, `EnqueueReport`, `CompiledClaim.event_ids: tuple[int, ...]`)
  DIFETTO: Una decina di tipi compaiono nelle firme senza che alcuna fase li definisca; due di questi hanno conseguenze concrete. `RelationKind` (F5) non esiste in F4, che modella `kind` come colonna testuale con vincolo: il contratto `ReconcileDecision.relation: RelationKind | None` non è scrivibile. `CompiledClaim.event_ids: tuple[int, ...]` è tipizzato intero, mentre `source_events.event_id` è `TEXT PRIMARY KEY` in F1 e `compile_queue.event_id` vi si lega per chiave esterna: il compilatore di F5 non può citare le proprie evidenze con quel tipo.
  RIMEDIO: Definire tutti i tipi citati in `contracts.py` (F0/F1) e allineare `event_ids` a `tuple[str, ...]`; introdurre `RelationKind` in F4 insieme a `relations`, con i valori ammessi in F4 e l'ampliamento dichiarato in F5. Una prova di importazione statica sul modulo dei contratti rende visibile ogni nome non definito prima del codice che lo usa.

[M7] Tutte le sezioni — riferimenti `file:riga`
  DIFETTO: Deriva sistematica di 1-2 righe su citazioni marcate [PROVATO], verificate una per una: `_http_has_pending` :358→360; `submit_executor` :3838→3837; `invoke_remote` :3685→3684; `apply_credentials_extraction` :872→873; `orchestration.py` :1199→1200; `pipeline_effects.pipeline_effect_counts` :81→83 (F5) contro :83 (F4, esatto); `fastpath.normalize_hash` :229→228; `_ungrounded_mutating_args` :5889→5890; `record_observation` :6195 (F4) contro :6197 (F6, esatto); `normalize_query` :78-109→:76-105; `users.py` :240→241; `read_persons/manifest.toml` :100→101; `user_detail.html` :164→162; `sign.py:192` indica la def, la firma dei byte e' a `:233`. Nessuna singolarmente grave, ma in un documento normativo le righe verranno credute e citate a valle.
  RIMEDIO: Rigenerare i riferimenti con una passata a macchina che estragga il numero dal simbolo (nome di funzione o testo esatto della riga), non a mano.

[M8] Prosa di F0, F1, F3, F5, F6
  DIFETTO: Anglicismi, alcuni esplicitamente vietati: «gate» in F3 («il mass gate», «Gate di piano e token», «il gate lo mostra») e in F5 («adatte a un gate di copertura»); «peer» in F0 («contraffatto da un peer non fidato»); poi «store» (titolo di F1 compreso), «lease», «replay», «append-only», «baseline», «backup», «scrub», «lookup», «reason code», «audit». Il documento altrove dimostra che la traduzione e' possibile: F5 dice «Cancello di certificazione» e F0 «Nessun innesco SQL».
  RIMEDIO: Passata lessicale: gate→cancello/controllo, peer→interlocutore, store→archivio, lease→concessione a termine, replay→riesecuzione, append-only→solo in aggiunta, baseline→riferimento, scrub→ripulitura, lookup→ricerca esatta, reason code→codice di motivo, audit→registro.

[M9] F0 «Fuori da questa fase», riga sul `ReferenceSlot project`
  DIFETTO: «(oggi solo blocco di prosa per il planner, `runtime/agent_runtime.py:964` [PROVATO])» contraddice il codice citato: la docstring di `_render_project_paths_block` dice «rende record strutturati … I nomi dei campi sono identificatori semantici stabili, non prosa» [PROVATO]. Il difetto reale del punto e' un altro, ed e' colto correttamente da F3: il contenuto di `runtime/project_paths.json` e' incoerente col repository (`memory_root` su `-opt-myclaw`, «Process name: myclaw») [PROVATO].
  RIMEDIO: Sostituire «blocco di prosa» con «record strutturati non validati» e rinviare a F3 per l'incoerenza del contenuto.

[M10] F1 «Dati», F3 «Dati» (blocchi SQL) e F2 «Dati»
  DIFETTO: Marcatura incoerente rispetto alla regola dichiarata: F4 e F6 marcano i propri blocchi SQL `[IPOTESI]`, F0 marca «[PROVATO: oggi assente]», mentre F1, F2 e F3 presentano schemi e affermazioni di schema senza marcatore. In un documento in cui il marcatore e' la garanzia, l'assenza si legge come fatto.
  RIMEDIO: Marcare `[IPOTESI]` in testa a ogni blocco SQL proposto, riservando `[PROVATO]` alle sole righe che descrivono lo schema odierno.

[M11] F2 «Fuori da questa fase», secondo capoverso
  DIFETTO: «§6.1 non assegna una fase alla restituzione di origine e data dalle API di W2: se F0 la chiude, F2 la consuma; altrimenti F2 la reclama come passo 2 e lo dichiara». Il confine di fase resta indeterminato per costruzione: nessuna prova puo' dire che F0 e' finita, perche' il suo perimetro dipende da cio' che F2 decidera' di reclamare.
  RIMEDIO: Assegnare l'allargamento di `list_prefs` a F0 (dove gia' vivono le transazioni W2) e togliere l'alternativa.
