# Assenze verificate sul codice — 28 luglio 2026

Ciò che RM-0001 dava per esistente e che NON esiste, accertato aprendo i file.
Prodotto da cinque agenti di ricognizione indipendenti; i duplicati fra dossier
sono conservati perché mostrano quante lenti diverse hanno trovato la stessa cosa.

- [PROVATO] Non esiste alcun `PrincipalContext`: ricerca su tutto il repository (esclusi i file compilati) di 'PrincipalContext' — zero occorrenze. L'unico tipo affine e' `TutorPrincipal`, costruito per richiesta dagli adattatori di runtime/tutor_boundary.py:47 e :64, che vive solo dentro il Tutor e non e' autorevole per nessuna decisione di autorizzazione.
- [PROVATO] Non esiste alcuna `authz_revision` ne' concetto equivalente: ricerca su tutto il repository di 'authz_revision' — zero occorrenze. Non esiste nessun contatore monotono di revisione autorizzativa in users.db (schema a runtime/users.py:47-69), pairings.db (runtime/pairing.py:41-60 circa, colonne con `revoked_at`), devices.db (runtime/devices.py con `revoked_at` a riga 67) o active_sessions (che ha `revoked_at` e `revoke_reason` ma nessuna revisione). La revoca e' oggi un istante nullabile per riga, non una revisione confrontabile.
- [PROVATO] Non esiste `runtime/user_context/`: la directory non e' presente in runtime/ (elenco di runtime/ senza corrispondenze). Non esiste `user_memory.sqlite`: nessun riferimento nel codice.
- [PROVATO] Non esiste un punto unico di risoluzione dell'identita'. Ne esistono almeno cinque indipendenti: runtime/actor_resolver.py:39 (pairing, Telegram), runtime/http_routes_agent.py:342 (dispositivo HTTP), runtime/http_routes_agent.py:2367 (utente logico HTTP), runtime/channels/daemon.py:1245 (principale Telegram appena in tempo), runtime/devices.py:224 (attore verso proprietario). Ognuno ha la propria scala di ripieghi.
- [PROVATO] Non esiste un'identita' immutabile per turno: `run_turn` riceve attore, canale, proprietario e conversazione come quattro parametri sciolti con valori predefiniti (runtime/agent_runtime.py:6643) e li ricompone in `TurnLog` con ripieghi a valle (runtime/agent_runtime.py:6677-6680).
- [PROVATO] Non esiste omogeneita' del campo proprietario: HTTP passa un identificativo di users.db (runtime/http_routes_agent.py:1220), Telegram non passa nulla e il campo diventa la stringa di attore (runtime/channels/daemon.py:1785-1788). Chi progetta F4 non puo' assumere che `owner_user_id` sia una chiave verso users.id.
- [PROVATO] Non esiste scoping per soggetto nelle cache di piano: nessuna occorrenza di 'actor', 'owner' o 'user_id' in runtime/engine/fastpath.py e runtime/engine/autopath.py. L0 e L1 sono condivise per costruzione fra tutti i soggetti dell'istanza.
- [PROVATO] Non esiste propagazione della revoca fra i tre registri: pairing.revoke (runtime/pairing.py:279), devices.revoke_device (runtime/devices.py:625) e users.remove_channel (runtime/users.py:401) sono operazioni indipendenti, nessuna delle quali richiama le altre.
- [PROVATO] Non esiste rivalidazione dell'identita' al momento del rigioco differito: ne' i compiti ricorrenti (runtime/recurring_tasks.py:549-552) ne' i turni differiti (runtime/agent_server.py:234) verificano che l'attore persistito corrisponda ancora a un soggetto valido e non revocato.
- [PROVATO] Non esiste il ruolo di amministratore come proprieta' del soggetto: l'unica definizione e' il confronto di stringa `actor == "host"` (runtime/system/admin.py:443, runtime/vaglio.py:387, runtime/admin_chat_commands.py:64). Il ruolo HTTP 'admin' (runtime/http_auth.py:224) e il ruolo utente 'host' del registro (runtime/users.py:43) sono tre nozioni distinte che oggi nessun codice riconcilia.
- [PROVATO] Non esiste un identificativo di conversazione comune ai due canali: HTTP usa `http:<proprietario>:<conversazione>` (runtime/http_routes_agent.py:350), il nucleo e Telegram usano `<canale>:<attore>` (runtime/agent_runtime.py:4458-4461, runtime/engine/dispatch.py:5550, runtime/sandbox.py:804). Il confine comune di chat previsto da F2 va costruito, non riusato.
- [PROVATO] Non esiste un terzo registro di specifiche di preferenza: le sole sorgenti sono le chiavi chiuse di runtime/users.py (`allowed_pref_values` riga 654, `preference_allowed_map` riga 674, tabella `user_prefs` riga 684). La scelta di derivare `PreferenceSpec` a runtime da queste chiavi piu' le dichiarazioni firmate e' quindi coerente con il codice esistente, non un compromesso.
- [IPOTESI] Non risulta alcun meccanismo che impedisca oggi a un valore personale di finire in un piano memorizzato in cache: l'unico punto di iniezione tardiva esistente e' `_fill_runtime_sourced_args` (runtime/agent_runtime.py:3550) e agisce al momento dell'invocazione dell'executor, quindi DOPO il piano. F3 e' costruibile su quel punto senza inquinare L0/L1, ma nulla nel codice lo VINCOLA: se una futura sorgente riempisse l'argomento prima, nessun controllo se ne accorgerebbe.
- `runtime/user_context/` NON esiste [PROVATO]: `ls /opt/metnos/runtime/user_context` → «No such file or directory». Nessun modulo del sottosistema di conoscenza utente è iniziato.
- `user_memory.sqlite` NON esiste [PROVATO]: nessun file con quel nome né alcun file contenente «memory» sotto `/home/roberto/.local/share/metnos/`. Il database reale contiene solo `users.db` con le cinque tabelle censite.
- `prefs_revision` NON esiste [PROVATO]: il letterale non compare in alcun file Python di `runtime/`, né come colonna nello schema reale di `user_prefs` (5 colonne, verificate).
- `authz_revision` NON esiste [PROVATO]: nessuna occorrenza in `runtime/`. La revisione autorizzativa che F0 dà per fondativa è interamente da costruire.
- La tabella `user_defaults` NON esiste [PROVATO]: nessuna occorrenza del nome in `runtime/`. Quello che esiste è `args_defaults` in un file SQLite separato (args_defaults.py:31-42), con schema e semantica diversi (chiave `actor` grezzo, non identificativo utente; scadenza per inattività).
- La `PreferenceSpec` NON esiste come tipo [PROVATO]: nessuna occorrenza del nome in `runtime/`. Esiste soltanto il materiale da cui derivarla — `PREF_KEYS`/`PREF_ALLOWED` (users.py:632-651) e il modello di derivazione a runtime di `preference_specs()` (playwright_sidecar/stealth.py:167-175) — coerentemente con la correzione già decisa che la vuole derivata e non registrata a parte.
- Nessuna sezione `[personalization]` esiste in alcun manifest [PROVATO]: la ricerca del letterale sui file `.toml` non restituisce nulla. F3 parte da zero su questo confine.
- Il consumo di `tone`, `reply_length` e `units` NON esiste [PROVATO]: la ricerca dei tre letterali su `runtime/`, `executors/` e `tests/` restituisce come codice di prodotto le sole righe di definizione users.py:633 e :641-643; ogni altra occorrenza è una prova automatica o l'omonimo `install/units`. RM-0001 §2 è confermata, non smentita.
- La cancellazione a cascata delle preferenze NON esiste [PROVATO]: `delete_user` (users.py:269-278) cancella solo `user_channels` e `users`. Non è una lacuna di RM-0001 ma un difetto di oblio già presente in W2.
- WAL, `busy_timeout` e transazioni esplicite sul CRUD preferenze NON esistono [PROVATO]: `_open_db` (users.py:81-87) non emette alcun PRAGMA e il database reale è in modalità journal `delete`; l'unico `BEGIN IMMEDIATE` del modulo è a :460, fuori dalla sezione preferenze.
- La chiusura delle connessioni nel CRUD preferenze NON esiste [PROVATO]: nessuna delle quattro funzioni (users.py:695, 723, 733, 743) chiude, mentre tutte le funzioni sugli utenti chiudono in `finally`.
- Nessun manifest firmato usa `${RUNTIME:pref_<chiave>}` [PROVATO]: il risolutore esiste (engine/executor.py:285-296) ma le uniche occorrenze del letterale nel repository sono quella riga e la prova automatica tests/runtime/http/test_user_prefs.py:62-66. Il canale è costruito e mai adottato.
- `runtime/user_context/` NON esiste: `ls runtime/user_context` risponde «No such file or directory». [PROVATO]
- Nessuna occorrenza di `personalization` in tutto il repository (ricerca su `--include=*.py --include=*.toml`, zero risultati): la sezione `[personalization]` che F3 vuole introdurre non esiste ne' come dichiarazione ne' come lettore ne' come convalida. [PROVATO]
- Nessuna occorrenza di `user_memory` nei moduli di runtime: `user_memory.sqlite` e il suo schema non esistono. [PROVATO]
- Il ReferenceSlot `project` su `runtime/project_paths.json` NON esiste come meccanismo di riempimento argomenti. Il file esiste (una sola voce, `metnos`), ma l'unico lettore e' `runtime/agent_runtime.py:964 _render_project_paths_block()`, che lo trasforma in un blocco di TESTO passato al prompt del planner a `runtime/agent_runtime.py:6788`. Nessun modulo lo consulta per riempire un argomento. [PROVATO]
- Il contenuto di `runtime/project_paths.json` e' incoerente col repository: la voce `metnos` dichiara `memory_root` su `-opt-myclaw` e la descrizione «Process name: myclaw», mentre CLAUDE.md §1 fissa il nome di processo a `metnos`. Il file non attraversa alcuno schema di convalida. [PROVATO]
- I provider di `_RUNTIME_ARG_SOURCES` NON ricevono contesto: la firma effettiva e' `provider()` a zero argomenti (`args[name] = provider()`, runtime/agent_runtime.py:3550 e seguenti). Non esiste oggi alcun modo per una sorgente runtime di conoscere il principale, l'attore, il canale o la conversazione. [PROVATO]
- Non esiste un elenco chiuso delle sezioni ammesse nei manifest: nessuna occorrenza di sezioni permesse in `runtime/manifest_lint.py`, `runtime/manifest_rules.py`, `runtime/executor_standard.py`. Una sezione sconosciuta viene firmata e caricata senza obiezione, e semplicemente ignorata. [PROVATO]
- Il caricatore NON conosce `[credential_form]`: la sezione non compare in `runtime/loader.py` e non e' un campo dell'oggetto Executor costruito a `runtime/loader.py:1428`. Viene riletta dal file su disco a ogni invocazione da `runtime/credentials.py:231`, dopo che la verifica di firma e' avvenuta al caricamento del catalogo — quindi la convalida della sezione e' PIGRA e la rilettura non ripete la verifica. Per F3 questo significa: il «modello esatto» che la roadmap vuole replicare ha un difetto strutturale che non va replicato. [PROVATO]
- Non esiste alcun controllo statico che leghi un `runtime_source = "<nome>"` dichiarato in un manifest a una voce reale del registro: un nome sbagliato produce solo `log.warning("runtime_source %r sconosciuta per %s.%s")` a tempo d'esecuzione (runtime/agent_runtime.py:3565-3568) e l'argomento resta assente. [PROVATO]
- Non esiste una politica «riempi solo se assente». Le due politiche implementate sono opposte e nessuna delle due e' adatta a un default personale: le sorgenti runtime sovrascrivono sempre (runtime/agent_runtime.py:3533-3537), il risolutore di fornitore rispetta invece l'esplicito gia' valido e disponibile (runtime/backend_resolver.py:130, ramo `if avail(cur): return args`). [PROVATO]
- Il riempimento delle sorgenti runtime avviene PRIMA della decisione di collocazione remota (riempimento a runtime/agent_runtime.py:3608, ramo remoto e `invoke_remote` a runtime/agent_runtime.py:3685): oggi non esiste alcun modo di dichiarare che un argomento runtime-owned non deve attraversare il filo firmato verso un dispositivo. [PROVATO]
- La verifica preliminare d'ambito (`check_invocation_scope`) NON copre l'esecuzione remota: e' invocata a runtime/agent_runtime.py:3699-3701, cioe' dopo il ritorno anticipato del ramo remoto. [PROVATO]
- La sequenza di risoluzione argomenti per passo e' DUPLICATA in due punti del motore — ciclo seriale (runtime/engine/executor.py:2183-2213) e preparazione dell'onda parallela (runtime/engine/executor.py:1900-1915) — senza un conduttore comune. Un passaggio aggiunto in uno solo dei due crea una differenza di comportamento fra letture seriali e letture parallele. [PROVATO]
- [PROVATO] `runtime/user_context/` non esiste: `ls runtime/user_context` risponde «No such file or directory». Nessun modulo di conoscenza utente locale è presente nel repository.
- [PROVATO] `user_memory.sqlite` non esiste: nella cartella dati corrente (`~/.local/share/metnos/`) ci sono args_defaults, autopath, autopaths, detection, devices, dialog_pending, fastpaths, github_issue_qa, github_state, i18n e altri, ma nessun archivio di memoria utente. Nessuna occorrenza di `user_memory` nel codice runtime.
- [PROVATO] Non esiste alcun principale canonico condiviso: l'unico oggetto d'identità che attraversa entrambi i canali è `TutorPrincipal`, costruito da due adattatori distinti in `runtime/tutor_boundary.py:47` e `:64`, con `user_id` derivato per ripieghi successivi (`str(user_id or actor or device_id or "http-user")`, riga 56). È un principale del Tutor, non dell'istanza.
- [PROVATO] Non esiste alcun compilatore né alcun registro di eventi con epoca di cancellazione: nessuna occorrenza di `deletion_epoch`, `PreferenceSpec`, `ReferenceSlot` o di una sezione `[personalization]` nei moduli runtime e nei manifest degli executor (grep su `runtime/` ed `executors/` senza risultati).
- [PROVATO] `split_query_chunks` NON conserva gli intervalli di testo: restituisce `list[str]` ripulite (compound_decomposer.py:95-101). Il documento non può dare per esistente una segmentazione con intervalli; va prevista come estensione della stessa funzione.
- [PROVATO] Non esiste una dataclass `Intent` prodotta dall'estrattore: `extract_intent` restituisce un dizionario (intent_extractor.py:90, :305) e la dataclass `Intent` (engine/types.py:19) è costruita a mano dal ponte del runtime (agent_runtime.py:6016). Sono due tipi distinti, spesso confusi.
- [PROVATO] Il campo `Intent.keywords` esiste ma resta sempre vuoto nel percorso reale: l'estrattore non scrive mai la chiave `keywords` nel dizionario d'uscita. Di conseguenza la parte «parole chiave» della firma leggibile L1 (`autopath._compute_intent_sig`, riga 303) è sempre `verbo|oggetto|`.
- [PROVATO] Né la cache L0 (`fastpaths`, schema a engine/fastpath.py:147-160) né la cache L1 (`autopaths`/`observations`, schema a engine/autopath.py:102-137) hanno una colonna d'identità utente: sono condivise dall'intera istanza. Qualsiasi ipotesi di piani memorizzati per principale è oggi infondata.
- [PROVATO] Non esiste un confine unico dei comandi prima del pianificatore: i confini attuali sono tre e separati — la scorciatoia lessicale del runtime (`fast_path.try_fast_path`, riga 389), il confine del Tutor (`tutor_boundary.answer`, riga 80) e gli scavalcamenti interni all'estrattore d'intento (intent_extractor.py:100-125). Il «confine comune HTTP/Telegram» di F2 va costruito, non riusato.
- `runtime/user_context/` non esiste: nessuno dei sette moduli della mappa implementativa (contracts, policy, store, service, deletion_journal, boundary, personalization, references, retrieval, episodes, source_events, compiler, routines) e' presente. [PROVATO] — `ls runtime/user_context` restituisce «No such file or directory».
- `runtime/principal_context.py` non esiste: il principale canonico di F0 e' interamente da scrivere. [PROVATO] — `ls runtime/principal_context.py` restituisce «No such file or directory».
- `user_memory.sqlite` non esiste sotto la cartella dati utente. [PROVATO] — verificato per assenza del file.
- `runtime_outcome` ed `episode_refs`, citati come lavoro di F4 (RM-0001 riga 984), non compaiono in alcun sorgente Python di `runtime/`. [PROVATO] — ricerca testuale su tutto `runtime/` senza alcun riscontro.
- Non esiste alcun registro per invocazione degli executor INTERNI: l'unica traccia per invocazione (`skill_audit.audit_skill_invocation`, runtime/skill_audit.py:146) si autoesclude se il manifest non ha sezione di provenienza, cioe' per tutti i builtin. Gli episodi di F4 non hanno oggi alcuna fonte a grana d'azione da riusare: vanno emessi ex novo al choke point. [PROVATO]
- `_scheduled_turn_outcome` — l'unica derivazione `success|partial|error` esistente — NON vive nel motore ma in `runtime/recurring_tasks.py:481`, ed e' raggiunta solo dai turni SCHEDULATI. Un turno interattivo oggi non produce alcun esito semantico classificato: RM-0001 riga 185 («gli esiti runtime distinguono successo, parziale ed errore») vale solo per il ramo schedulato. [PROVATO]
- La derivazione esistente non emette mai `success`: ritorna `None` per il caso felice (runtime/recurring_tasks.py:496) e il valore `success` e' l'inizializzazione del daemon (runtime/scheduler_v2/daemon.py:256). Un F4 che si aspettasse tre valori simmetrici da quella funzione troverebbe due valori e un vuoto. [PROVATO]
- Lo stato `timeout` esiste nell'insieme ammesso degli esiti schedulati (runtime/scheduler_v2/daemon.py:271) ma non e' mai prodotto dalla derivazione semantica del turno: e' solo l'esito della scadenza del daemon. L'insieme reale degli esiti e' quattro, non tre. [PROVATO]
- Il registro dei turni NON ha politica di conservazione infinita ne' indice: e' JSONL giornaliero con 60 giorni vivi e 365 in archivio compresso (runtime/jobs/maintenance_tasks.py:210 e 299-317). Un episodio con conservazione piu' lunga del turno che cita produrrebbe una citazione morta. [PROVATO]
- Il registro delle lacune del terminatore (`terminator_log.sqlite`, runtime/engine/terminator.py:48) conserva le query testuali degli utenti senza alcun legame con un principale e senza politica di conservazione fra i reaper censiti: e' una sede di dati personali che l'oblio di RM-0001 oggi non raggiunge. [PROVATO]
- La tabella `observations` di L1 conserva il vettore semantico della query utente (runtime/engine/autopath.py:452 e 469-475) in una struttura condivisa priva di colonna di principale: RM-0001 deve trattarla come sede vietata ai valori personali, non solo come fonte da leggere. [PROVATO]
- Il ciclo W1 osserva soltanto i turni RIUSCITI e cacheabili (condizione a runtime/engine/dispatch.py:6195, con il commento esplicito «no-cache-on-error»): le routine di F6 che vi si innestano non potranno mai apprendere da un fallimento ricorrente. [PROVATO]
- Il journal dell'annullamento — precedente piu' vicino al journal write-ahead di F1 — e' deliberatamente fail-open in entrambi i suoi punti di scrittura (runtime/agent_runtime.py:3472-3474 e 3491-3493): non e' un modello di durabilita' adottabile senza modificarne la politica d'errore. [PROVATO]
