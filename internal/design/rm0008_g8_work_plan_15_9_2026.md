# RM-0008 — G8 work plan

Date: 15 September 2026.
Source baseline: `06a9472e`.
Status: **in progress; not F5 certification or activation**.
Assignment and approved principles: [decision record](decision_rm0008_g8_scope_15_9_2026.md).

## 1. Scope

Complete G8 before G9. F6 remains assigned to Codex after F5, but no G10
schema or deletion authority is introduced here. Do not repeat the F4
transition or modify the shared root checkout.

G8 has two outputs: an installation-bound, evidence-derived entry proof and
a lossless, reconciled retirement of name-based lifecycle state. A signed
summary, a green isolated test suite, or copying rows alone is not either
output. The detailed missing contracts remain enumerated in the handover
and decision record; they must be closed before their implementation.

## 2. Immediate prerequisite: faithful terminal replay

This bounded repair strengthens an existing F4 reader; it neither opens F5
nor changes a signer, schema, capability or productive caller.

- Failure location: `executor_birth_operational._decode_terminal_envelope`
  constructs the publication using the request's contract without comparing
  the contract encoded in the signed publication. `_replay_terminal` also
  does not compare the committed claim's `result_binding` with the envelope.
- Observed fact: the serializer includes both contract identity and an exact
  envelope hash in the durable result, but these two bindings are not both
  checked on replay. Existing signature-tampering tests do not cover a
  correctly signed but inconsistent contract or a modified stored hash.
- Causal hypothesis: the reconstructed value can mask an inconsistent signed
  identity; independent postcondition verification then sees the reconstructed
  request identity instead of the contradictory signed fact.
- Expected result: reject either mismatch before postcondition verification,
  checks or publication; preserve the durable record. Valid exact replay,
  historical-key rotation, signed rejection and crash recovery must retain
  their existing behavior.

Contract: schema 2 and the existing signing/hash domains stay unchanged.
The publication's encoded `contract_id` must equal the request's exact
`ContractId.value` before construction. For a committed claim, the stored
`result_binding` must equal `_terminal_binding(encoded)`. A rejected claim
must not be treated as a successful committed publication. No inference from
names, replacement signatures, fallback values or new key is permitted.

Tests: one parametrized negative replay family for the contract, result hash
and terminal-state bindings, followed by the operational/Producer replay and recovery
families whose boundary is affected. Record the red baseline before repair.
This is an isolated productive-code traversal, not a live installation or
an admission qualifying for the F5 threshold. Installed and native Windows
verification remain unproven until actually performed.

Independent internal review found one remaining state ambiguity on checkpoint
`04aa1b14`: an authentic intermediate admitted hint, stored as `rejected`,
has no publication and therefore passed the publication-presence comparison.
The result still reported `admitted` with no operational error. The affected
symbol is `_replay_terminal`; the cause is that publication presence alone
does not distinguish an intermediate hint from a final rejection.

The additional repair must compare the signed outcome and operational error
with the terminal state: committed means admitted/preexercise with no error;
rejected means rejected/needs_human/quarantined with an operational error equal
to the stored rejection code. This does not require equality between the
original check error and a later finalization error. Keep legitimate recovery
hints usable only by the existing in-progress recovery path. Extend the same
negative replay family, record the new red cases, and exercise the positive
rejection/recovery paths affected by this extra comparison.

## 3. G8 contract candidate for independent review

This section makes the approved principles executable at their owner
boundaries. It is a contract candidate, not an installed authority or a
claim that the complete G8 design has passed independent review. The
remaining freeze items in section 6 must be resolved before their code.

### 3.1 Historical verification and admission predicate — N2/N3

Use separate historical readers, not a historical object passed off as a
current selection. The ownership-chain reader authenticates the F4 anchor
and every relevant successor context; `executor_birth_prepared_root` returns
only the public verifier material pinned by those authenticated contexts.
Never return `SealedAuthoritiesV1`, call the private-key loader and discard
the key afterwards, or require an obsolete signing private key to verify
a historical signature. Historical author rotation must use the historical
public inventory, not equality with today's active author key.

`contract_store` owns complete enumeration of admission receipts and their
generation/source/journal bindings. This means persistent signed bindings,
not a claim that the original temporary source or journal still exists.
Roberto explicitly approved this distinction after the internal review;
see the decision record's historical-evidence clarification.
`executor_birth_producer_store` owns the
matching Producer bytes, issuance and terminal rows. Its historical reader
opens the existing database read-only, requires the supported schema, starts
one read transaction and never calls `_open`, `_migrate` or `_verify_claimed`.
The enclosing certifier must prove a common stable frontier; two independent
SQLite snapshots are not a cross-store snapshot.

The proposed qualifying predicate is the conjunction of:

- a valid historical `AdmissionReceipt` with `kind=admission`;
- revision class in `first_birth`, `code_revision`, `authority_revision`,
  `contract_revision`, and `approved_lifecycle=active`;
- a verified Producer receipt and its capability-specific issuance, with
  exact request, contract, objective, source, issuer and receipt-byte binding;
- a committed, authenticated schema-2 terminal with no error, report outcome
  `admitted`, and publication operation `commit_birth_snapshot`;
- equality of all overlapping signed facts: request, contract, generation,
  predecessor, candidate, semantic core, context, revision class, checks and
  embedded admission bytes, plus the exact terminal hash;
- Producer validity at the signed admission act time, not the database's
  unsigned finalization time or an invented time immediately before expiry;
- independently reread persistent receipts/generation payloads and agreement
  of their authenticated source/journal bindings, with no pending ambiguous
  recovery. Original-byte availability is recorded separately: a signed hash
  is not presented as a material reread of an absent original.

Localizations, equivalent publication, promotion, reactivation,
reattestation, rejected/quarantined results and preexercise do not qualify
under this proposed predicate. They remain in the enumerated evidence with
an exclusion reason. A malformed or unauthenticated item is not silently
discarded to make the remaining inventory appear complete.

Important source distinction: ordinary technical admissions still use the
request-v1 derivation in `executor_birth_bootstrap._request_factory`, even
after an F4 context transition. `ProducerRequestV2` is used by the context
reattestation path. Do not require a forged V2 request for ordinary technical
history. Resolve capability from the context's closed producer table and
authenticated key binding; a mutable `capability_id` string alone is not proof.

The owner must distinguish the protocols actually persisted. The V2
reattestation path stores an admission receipt and its
`reattestation-result/v1` binding without a schema-2 terminal envelope; verify
that protocol before recording it as excluded. Conversely, an ordinary
committed terminal may omit the embedded admission when the publisher reused
an existing receipt. Reread that exact durable receipt independently; never
invent missing embedded bytes or treat either valid protocol as corruption.

For ordinary request-v1, recompute the framed request digest from authenticated
issuer, capability operation, exact contract, objective and candidate-source
ID. Verify the Producer nonce against the factory's SHA-256 request-ID
derivation as well. The signed admission `issued_at` is the core's act time
captured before checks, not an independently attested finalization timestamp.

Count each admitted act once across receipt ID, request ID and exact
`(ContractId,generation_id)`. Conflicting duplicates fail; exact duplicates,
retries and context reattestations do not add admissions. Count producers
by authenticated `issuer_id`, not operation, key, directory or executor name.

### 3.2 Certification authority and activation — N1/N2

Proposed scope: one dedicated administrative F5 certification identity,
separate from author, Admission, Producer, distribution, cutover, head and
HTTP credentials. It can attest the threshold evidence only; it cannot
publish executors, change lifecycle, grant capabilities or delete objects.
Provisioning belongs to the native administrative installer. No candidate,
model or ordinary runtime request can create, select or replace this identity.

Private custody belongs outside the service-owned roots; Linux requires the
existing administrator-owned storage discipline and mode 0600, Windows an
equivalent native ACL proof. Runtime readers receive public material only.
Reuse existing canonical Ed25519 codecs and protected filesystem primitives;
do not add a general certificate service or a second installation manager.

The bounded dossier must bind policy/schema version, installation identity,
F4 anchor, authenticated chain frontier, observed source/build, complete
owner inventories, admitted/excluded act identities, both cycle IDs/profile,
defect-census/ledger head, public-registry revision and certifier identity.
Lists and counts are derived from verified owner records, never supplied as
authority by the caller. Canonical encoding rejects duplicate/unknown fields,
invalid types, truncation and inconsistent references. Large history is
paged by the owner and completely reconciled before a positive result.

Keep the historical threshold proof separate from current-release activation.
The runtime's fixed-root reader verifies the selected installation/head,
policy compatibility and current revocation state before returning a sealed
activation object. A later release does not inherit activation merely because
it descends from the certified one. Neither object hashes a manifest that
already includes that object's own digest. No permissive v1 compatibility
wrapper is needed: `load_f5_activation` has no productive caller today.

### 3.3 Complete cycles and defect evidence — N4/N5

Use the existing external HTTP certification harness where it covers the
required boundary. Register the exact versioned case/profile digest and
expected postconditions before starting; do not call two selected successful
turns a complete cycle. Fixture paths, actors and effects must remain isolated
and non-personal; no external messages or new paid API is authorized.

Record every cycle start and terminal outcome, including failure and
interruption, in sequence. The qualifying pair consists of two adjacent
complete successes on the same authenticated source/catalog/profile. A
profile or base change starts a different sequence; an unexplained difference
blocks qualification. Record real turn references and independently observed
postconditions, not just a harness return code.

Initialize the defect ledger from the known RM-0008 findings, including the
terminal-replay repair in section 2. Use an append-only, authenticated record
of opening, closing and reopening. A closure needs causal evidence, affected
tests, installed verification where required and independent review. A
developer's empty list or a scalar zero cannot establish completeness.
Reopening a relevant defect invalidates a positive current-frontier result.

### 3.4 Lossless migration and retirement — N6/N7/N8

The new epoch store belongs to the service and resolves its path through
central configuration. Version 2 cannot silently gain new meanings: freeze
version 3 explicitly before implementing historical associations and the
cutover record. Keep original copied rows immutable; store any later exact
association and its evidence separately. Retrying preservation must verify
the original bytes even after a legitimate resolution was appended.

Copy all `executor_stats`, `executor_history` and `proposal_promote` rows,
including duplicates/history, with source identity, schema fingerprint,
stable row order, count and digest, followed by independent destination
reread. Preserve source databases. `unresolved` is valid for history without
proof; it is not permission to drop effective restrictions or pending work.
New current epochs come from authenticated current generations, with zero
inherited counters and no inferred historical `name -> current` mapping.

Prefer a short, explicitly managed maintenance window for the one-time
migration over a continuously running synchronization subsystem. The writer
census must cover direct SQL as well as aging/promoter APIs, all services,
scheduled callbacks and administrative commands. The barrier must remain
effective across interruption and cover already-open database connections.
Individual SQLite locks or an in-memory flag do not establish that barrier.

Before disabling a legacy reader, determine which stored restrictions are
still effective against the authenticated current catalog. Preserve each
effective restriction with an explicit exact-generation disposition; if the
binding cannot be established, stop that cutover. A new present-time
disposition must not be misrepresented as proof of a historical association.

Drain or explicitly dispose of open promotion/grace/review work before
retirement. Never turn an already active generation into preexercise.
No live row is automatically discarded because its old mechanism is retiring.
Only after complete reconciliation, necessary dispositions and durable
cutover may legacy writers become unreachable/refuse mutations and legacy
views become non-decisional. Crash/retry must not reopen those writers.

### 3.5 Closed prerequisite slice: public historical context reader

The independent reviewer completed the concrete reader contract after
Roberto's historical-evidence approval. This slice introduces no signer,
certificate, activation, mutation or candidate-selected root. It does not
freeze the remaining certification/ledger/migration contracts.

- `executor_birth_context_v1` decodes the existing canonical material schema:
  reconstruct historical context/epoch from the recorded components, and
  recompute the registry component whose complete configuration is retained.
  Other component source hashes remain historical bindings, not reread code.
  Do not compare historical component versions or source lists with today's
  catalog. Require exact field sets, types, bounded bytes and canonical JSON.
- `executor_birth_prepared_set` reads a historical public set through an
  already locked session, checking set/content identity, mandatory chain pins,
  registry documents, sorted unique key IDs and self-identifying public bytes.
  Preserve the authenticated `producer_id:operation -> store_name` mapping;
  do not construct operational capabilities or infer a current producer table.
- `executor_birth_prepared_root.load_historical_context_verifiers_v1(context_id)`
  treats its argument only as a selector. Internally read the fixed ownership
  chain before and after, select only an unambiguous authenticated transition,
  and return an immutable, inert public view with frontier/transition/set/
  context/epoch and verified document hashes. No caller-supplied chain, root,
  callback, `PreparedSetV1`, `ContextSelectionV1` or `SealedAuthoritiesV1`.
- Remove the private dependency also from the existing
  `_historical_birth_public_inventory_v1`, which the chain's public registry
  reader calls. Use the fixed marker and the same public set reader, without
  calling the chain recursively. Its key-exclusion result includes all current
  self-identifying author public files as well as the marker-set keys. Extra
  author keys can only increase the forbidden set; they never enter a
  historical verification ring not declaring them.

Keep private runtime loading unchanged. The historical reader never reads
`keystore.json` or `private/*`, nor compares with today's active author key.
Existing global locking, protected handle-relative reads, domains, set schema
and context schema are reused. New result types are not signing authorities.

One new discriminating family covers valid history with private access denied
and author rotation; rejection of altered set/material/key/Producer binding;
unknown or ambiguous context; changed chain frontier; immutable result and
no writes. Reuse one existing key-exclusion traversal after changing that
dependency, not all F4 certification tests. Structural boundary review is
required because the read graph changes. Installed and Windows proof remain
open until run.

Explicit limits: this first reader selects target contexts present in the
authenticated F4 chain; the predecessor of its first transition is not
automatically covered. The existing Ownership key-exclusion check does not
cover Admission/Producer keys of later sets; retain that as an N1 finding,
not a guarantee supplied by this slice. Neither limit permits incomplete
history to be reported as complete F5 evidence.

### 3.6 Closed prerequisite slice: historical Producer author declaration

The independent reviewer accepted this bounded extension after §5.3. Reuse
the existing distribution reader and table; introduce no policy interpreter,
historical executable loader, issuer authority or release-specific allowlist.

- `executor_birth_distribution_manifest.read_verified_distribution_file_v1`
  accepts a verified distribution and an exact path/role, derives the root
  from that verified artifact, and reuses the protected exact-tree capture.
  Require the sealed payload, one matching manifest entry, size/content hash
  and unchanged tree. This reader does not independently select the current
  release. The current deployment-descriptor reader retains its own complete
  current-release checks before and after capture.
- `executor_birth_prepared_root.load_historical_producer_authors_v1` selects
  the public context from the fixed chain, as in §3.5. Bind the historical
  distribution by the selected transition's `closed_build_id`, checking its
  chain/head/sequence association. Preserve refusal of distinct transitions
  for one context; equal policy bytes do not resolve a set ambiguity.
- Read the table's public source from that chain's verified current
  distribution. Its size, role and framed `file_content_hash` must match the
  exact historical entry. No hardcoded observed hash or release number, no
  caller root and no arbitrary old-source execution are allowed.
- Project only the unique literal `PRODUCER_AUTHOR_V1` declaration: tuple
  keys of two literal namespace strings, values naming a known
  `RevisionAuthor` member, enclosed in the existing `MappingProxyType` form.
  Reuse bounded AST checks. Reject duplicate bindings/keys, computed values,
  unpacking, unsupported syntax and missing historical namespaces. Do not
  import the source or trust an already loaded Python table/function.
- Return immutable namespace/author data with the context, set, transition,
  historical build, source hash and chain frontier. Reread the fixed chain
  around the whole acquisition. The result proves a signed declaration, not
  arbitrary module effects, allowed origins, an `IssuerRegistry` or a
  qualifying admission. Unsupported history remains an explicit refusal.

One new test family covers the declaration grammar and historical binding,
including runtime table changes, changed source/frontier and namespace gaps.
Exercise the shared exact-file reader and only the existing readers affected
by refactoring. The installed source-availability result is reused, not
recounted as an execution test of this new interface.

## 4. Implementation sequence

1. Close N1–N5 into exact public historical reader, evidence, certification
   and activation contracts. Do not expose `SealedAuthoritiesV1` or call
   `_open`/`_verify_claimed` to perform supposedly non-mutating historical
   verification. Derive validity time from authenticated act evidence.
2. Close N6–N8 using the actual source/owner/writer census and approved
   lossless/disposition rules. Freeze the next epoch schema and recovery
   protocol before implementing it; retain unresolved history without
   fabricating associations or importing old counters into new epochs.
3. Implement the closed contracts in causal slices, with risk-to-test
   mapping, boundary review and independent review at each new authority.
4. Obtain real installed evidence and complete G8 exit checks. Missing
   producers, cycles, defect closures or dispositions remain blockers,
   never synthetic evidence. Only then enter G9.

## 5. Verification ledger

| Requirement | Symbol | Module/flow test | Installed proof | Result |
|---|---|---|---|---|
| Exact signed publication contract | `_decode_terminal_envelope` | Negative replay family | Not run | Red reproduced, local green |
| Exact terminal hash/state/outcome/error | `_replay_terminal` | Same family; nine cases after internal review | Not run | Red reproduced, local green; bounded diff reviewed |
| Existing replay/recovery behavior | Operational, Producer and bootstrap owners | Initial 75 + 7; later 20 affected cases, overlapping | Not run | Local green |
| Productive graph | Existing boundary checker | Structural findings: 0 | Not run | Public-only historical read graph reviewed |
| Reviewed source binding | Compiled source-review checker | Full checker rejects changed source digest | Not run | Expected pending-review mismatch; pin unchanged |
| Public historical context | Decoder, set reader and fixed-root selector in §3.5 | 32 new cases; 15 affected existing cases | Two live contexts, isolated hybrid proof in §5.2 | Reader verified within stated scope; not full candidate deployment |
| Historical Producer author declaration | Shared exact-file reader and inert projection in §3.6 | 45 new cases; 10 affected existing cases | Source availability only, §5.3 | Local green and independently reviewed; not Admission qualification |
| G8 entry threshold and migration | Not yet implemented | Not run | Not run | Open |

The four new cases failed before repair in two disjoint runs (2 + 2), then
passed within the 75-test operational/Producer run (5.79 seconds). Seven
additional bootstrap/recovery cases passed (0.34 seconds). Do not add the red
runs or rerun cases to the 82-case green total. `check_contract_boundary_policy`
passed. The full closed-build checker still reports
`birth_closed_source_review_mismatch`; it is not reported as a green check.

Repeatable test commands, each with fresh empty `METNOS_USER_DATA` and
`METNOS_USER_CONFIG` roots, using `/opt/metnos/.venv/bin/python`:

```text
-m pytest -q tests/runtime/executors/test_executor_birth_operational.py tests/runtime/executors/test_executor_birth_producer_store.py
-m pytest -q tests/runtime/executors/test_executor_birth_bootstrap.py -k 'new_build_can_claim_after_expired_terminal_staging_rejection or real_release_request_factory_accepts_a_first_admission or recovery_'
```

The subsequent independent review and repair are recorded in
`../reports/rm0008_g8_review_15_9_2026.md`. Five additional negative cases were
red before the outcome/error comparison. The affected replay family then
passed 17 cases (1.45 seconds), followed by the one new preexercise terminal
case (0.23 seconds) and two existing crash-recovery cases (0.29 seconds):
20 unique cases in this increment. The 20 are not added to the earlier 82 as
if disjoint; inherited Producer/store suites were not repeated.

Repeat the later set only if its boundary changes, using the same isolated
roots and interpreter:

```text
-m pytest -q tests/runtime/executors/test_executor_birth_operational.py -k 'inconsistent_durable_bindings or non_admission_is_durably or admitted_pipeline or replay_survives or revoked_historical or prepublication_diagnostic or ambiguous_publisher_failure_keeps_claim or crash_between_publisher_postcondition_and_finalize'
```

No live `/agent/turn`, native Windows certification, source-pin refresh,
public publication or deployment has been performed for this repair.
Public behavior and signing schemas are unchanged; the candidate
must remain unreleased until the applicable verification and review gates.

### 5.1 Public historical-reader increment

Implemented the closed prerequisite contract in §3.5. The first new test
reproduced the transitive private dependency: the Ownership public-registry
path tried to read `author-root-v1/keystore.json`. It now traverses real
protected public reads with private directories inaccessible and the active
author rotated. The replacement author extends only the Ownership exclusion
set, not the historical author verification ring.

The independent reviewer found an unnecessary `authority_registry.version ==
"1"` check. A second red test prepared real material with another recorded
version, then emptied today's component and Producer catalogs. Removing the
version comparison preserves the canonical schema and recomputed registry
digest while allowing that historical evidence to be read.

Verification on this increment:

- New public-reader family: 29 passed in 20.55 seconds. Covers exact pins,
  rotated/public-only keys, altered set/material/registry/key/namespace,
  malformed material, immutable result, absent/ambiguous context and changed
  head/records/transitions. Fixed-root selector cases stub only the chain
  boundary; they are not a cryptographic test of the installed chain.
- Affected existing prepared-set family plus one real Ownership key-exclusion
  traversal: 15 passed in 9.52 seconds. No inherited operational or Producer
  suite was rerun.
- Three additional malformed document cases exposed leaking `ValueError`,
  `UnicodeEncodeError` and `RecursionError`. The shared canonical decoder now
  reports the existing `PreparedSetError`, without admitting more documents.
  These three and seven affected existing positive/refusal cases passed in
  5.23 seconds. Those seven repeat earlier coverage; they are not new cases.
- Total distinct coverage for this reader increment is 32 new cases and 15
  affected existing cases, not a sum of repeated runs. Structural R1 found
  zero findings; generated boundary-policy checks and `git diff --check`
  passed. Reviewed source pins were not refreshed.
- Final independent static review found no further blocker in this bounded
  reader. No reviewer tests were repeated.

Repeat only when this boundary changes, with fresh isolated data/config roots:

```text
-m pytest -q tests/portable/rm0008_2b/test_group8_public_history.py
-m pytest -q tests/portable/rm0008_2b/test_group3_prepared_set.py tests/portable/rm0008_2b/test_group3_authority_consumption.py::test_deployment_keys_are_disjoint_from_the_authenticated_birth_set
```

Code/test/review checkpoint: `c1dcfb19` (`Read historical Birth contexts through
public evidence only`). The result is an inert historical evidence reader,
not a certification signer, F5 activation, verified production admission count
or native Windows proof.
The first-transition predecessor and later-set Ownership exclusion limits in
§3.5 remain explicit. The live reader boundary was subsequently exercised as
described below; complete admission-owner evidence remains the next contract.

### 5.2 Live public-context read-back, without deployment

Administrative proof run `run-87e_n1bg`, 15 September 2026, completed in
26.006 seconds as service UID 995 / GID 985, HTTP PID 1432998. The native
HTTP process selected release directory `00000000000000000053`; the cold
chain independently authenticated that same required distribution, head
`sha256:e63bc648309c87f43ba9ba39a9f1a22456b0361f2bf14b707cfb8f0cb4f17743`,
and 53 transition records.

This is explicitly a hybrid integration proof: root-owned release-53 policy
and dependencies, with only the three candidate public-reader modules loaded
from their measured source bytes in an isolated process. The worktree is not
on the import path, candidate bytecode caches are not executed, and critical
module origins and source hashes are recorded. No chain callback or verifier
is replaced and no source-review pin is changed. The independent reviewer
reviewed this delimitation before the successful run.

The exact historical-context selector read both ends of that authenticated
transition history, checking the chain around each acquisition:

| Context | Set | Public result |
|---|---|---|
| `sha256:8df4f2b7481809f3c8eafed3947c413bd430e3a78eb37a952e487fb89d470266` | `61d47c62d75d25bf16732017e4d32b6a7b72ef465d105d83b36b25dc7dbc8a4f` | 2 author verifiers, 1 Admission verifier, 11 Producer namespaces |
| `sha256:13f78a4c9612ab7daaae5f2055f00d04ced7a28b42f96e4e654afb55896dd790` | `6cb8d2f76d5cec99d5b3150a00f0faf657d7ae177ae6823abaa3104c7079e19e` | 2 author verifiers, 1 Admission verifier, 11 Producer namespaces |

The read-only guard observed zero private-read attempts and zero attempted
data writes/execution/network operations. It separately blocked 153 optional
import-time permission repairs from `config.ensure_dirs`; those repairs did
not modify the installation. The only persisted output is the administrative
runner's private proof record. No services were stopped or restarted.

Probe corrections before that successful result are not product failures or
passed tests: two early attempts omitted the selected workspace; two reached
the cold verifier with the candidate's different source-review pin; two
assumed an install-root environment override that the native launcher
deliberately strips. The corrected probe uses HTTP's native module/working-
directory selection and verifies it against the authenticated distribution.
The earlier `source review` refusal alone was not evidence of production
corruption. All protections remained enabled throughout.

Reproduction source: `internal/tools/rm0008_g8_public_history_proof.sh`, SHA-256
`58b30ca7a71bd28f95fd831294a2015e948ca3ce9be3741ae134b03fcb8559ab`.
Measured reader hashes, unchanged before/after:

- Context decoder: `ce285d2606e9993d5aa3613a64e6853daf84d8eadaada08ac6997a2c5fa4822c`.
- Public set reader: `56406414cc6984a663ec70acd4e5ee9efc04ed87288eb3a0120787b90b0cd6fa`.
- Root selector: `f655ec9bdccf6a86bd8a11cd0b37e909e8342a46b26e21fb0b19834d2fced823`.

This verifies only those two context read-backs with the stated composition,
not every historical act, the complete candidate release, Windows, F5 entry
or activation. Do not repeat the probe without a change to this boundary.

### 5.3 Historical Producer policy source availability

The historical public registry binds Producer namespaces and verifier keys,
but not the allowed revision author. The ordinary runtime derives that scope
from `executor_birth_producer_table_v1.producer_author_v1`. Neither a receipt's
self-declared author nor today's unbound table can establish historical scope.
The Producer catalog digest covers namespace pairs, not that author policy.

A separate, discriminating read-only observation used the same protected
composition as §5.2, with mode `producer-policy`: run `run-37ldnu_4`, completed
in 20.478 seconds. It did not repeat the two context read-backs. The real cold
chain authenticated release records 1–53, and each record listed exactly the
same public policy source at `runtime/executor_birth_producer_table_v1.py`:

`sha256:62d266497a6d2198ab152267084668f1c6173c99e309ce3ec80b5de6335901f9`.

The installed source has exactly that framed distribution-content hash and
declared size. An authenticated public copy is therefore available for every
observed release; no relaxation of historical author-scope verification is
needed for this observed history. This is source availability, not yet a
verified policy projection, a qualifying admission count or a complete F5
inventory. Any future differing source requires an explicitly supported
historical interpretation, not a fallback to current policy or execution of
arbitrary historical code.

HTTP remained PID 1432998 on release 53, with the same authenticated head as
§5.2. The guard observed zero private-read attempts and zero data-write or
execution attempts; 153 optional import-time permission repairs were denied.
No service or installed state changed. Reproduction script SHA-256 for this
mode: `dc5427cf303f013cded5910859c4443b7691a2c1139af4d422947f88ba241299`.

### 5.4 Historical Producer author declaration: local proof

Implemented §3.6 without changing the original Producer table or adding a
framework. The generic distribution reader is a thin public wrapper around
the existing protected capture. The prepared-root reader extracts literal
data from authenticated source; it never evaluates that source or uses the
current Python table/function as historical authority.

The first feature test reached the absent API before implementation. An
earlier fixture-import error was corrected and is not counted as a product
failure. Verification after implementation:

- 35 new projection/grammar/binding cases passed in 11.05 seconds.
- 8 new shared-reader/current-descriptor cases and 10 affected existing
  context/descriptor cases passed in 6.60 seconds.
- 2 additional old-context/new-release cases passed in 1.60 seconds. An
  identical public copy can supply the old declaration; a different current
  copy is refused instead of silently replacing historical policy.

This is 45 new cases and 10 existing cases, not 55 new cases or additional
installed admissions. The existing cases overlap the earlier verification
ledger. The real signed fixture files and protected filesystem reads are
exercised; the chain and administrative metadata remain explicit test seams.
These tests are not installed-chain or native Windows certification.

The structural closed-boundary check found zero findings, the policy
projection check passed, and `git diff --check` passed. The complete source
review pin is still pending and was not refreshed. The independent reviewer
inspected the implementation without rerunning tests or modifying files and
found no blocker within this slice. No deployment or installed mutation ran.

## 6. Installed census and remaining freeze items

The read-only administrative census uses the running HTTP process's selected
paths and drops to service UID 995 / GID 985 before SQLite access. It imports
no product code and reads no private keys or row contents. Connections use
`mode=ro`, `query_only` and bounded transactions; no schema migration runs.
This is a per-store observation, not a certification frontier or a migration.

Observed at `2026-09-15T18:58:22Z`, HTTP PID 1432998, run `run-iz718pz0`:

| Selected source | Observed state |
|---|---|
| `/var/lib/metnos-service/.local/state/metnos/executor_stats.db` | Service-owned 0600, user_version 0, DELETE journal; 198 statistics rows and 468 history rows. |
| Restrictions in that source | 58 nonempty deprecation markers and 44 nonempty archive markers; counts can overlap and are not proof of currently effective restrictions. |
| `/var/lib/metnos-service/.local/share/metnos/promoter.sqlite` | Service-owned 0600, user_version 0, DELETE journal; 8 rows: 6 archived, 2 review_needed, 0 promoted_grace, 0 unclassified states. |

Separate Producer metadata observation, run `run-l73u9c2o`, same HTTP PID:
`/var/lib/metnos-service/.local/state/metnos/birth/producer_receipts.sqlite`,
service-owned 0600, schema 5, WAL; 7040 rows (7024 committed, 11 rejected,
4 in_progress, 1 available). A metadata-only technical screen found 69
candidate rows across 3 issuer strings. **This is not 69 authenticated
admissions or proof of three qualifying producers.** Signatures, lifecycle,
capability binding, history completeness and eligibility still require the
new historical verifier. In-progress rows are not silently finalized.

The previous user's home also contains legacy databases; their mere presence
does not make them selected productive sources. Do not merge or migrate those
copies implicitly. Service selection and schema must be revalidated when
the actual cutover frontier is acquired.

Evidence scripts are held by the administrative runner with these hashes:
`bacff5047d8567a0e7f9707e4a2ba988475ca2e745f3cf4c2d1b484afddf2d44`
(legacy census) and
`963a6399f75807e05ee5a241225284ae484d10e27b4246c7d19626ed8ac159e8`
(Producer metadata). The worktree script supports both explicit modes;
do not rerun the legacy census just to repeat the Producer-only observation.

The first independent review of N1–N5 is complete; its historical-evidence
decision is now approved, and the public-reader prerequisite has local tests
and bounded review in §5.1. The protocol distinctions still require owner
implementation. Still required before freezing the
remaining new G8 interfaces: exact certification/ledger wire schemas and registry
installation/revocation protocol; exact routing profile/case manifest;
complete writer inventory and persistent-barrier lock order; epoch-v3 DDL,
association/disposition record and cutover recovery states. These are open
technical work, not automatically approved implementation details or a claim
that the complete G8 contract is already frozen.

### 6.1 Source writer census refinement — N6–N8

The source census includes the following retirement obligations in addition
to the handover's owner table. No installed writers have been disabled.

| Boundary | Confirmed detail and consequence |
|---|---|
| Aging owner | `executor_aging._open` creates schema; registration, invocation, aging, feedback and unarchive paths write the statistics/history tables. A helper described as a read must use `_open_read_only` after retirement. |
| Direct statistics SQL | `change_applier.apply_dedupe_executors` and the create/dedupe rollback handlers use `PATH_USER_STATE/executor_stats.db`, ignoring `METNOS_EXECUTOR_STATS_DB`. When these differ, census both exact paths as distinct sources; do not merge their rows by name. |
| Loader | Both the aging DB mtime and `lifecycle_override_map` influence the catalog; ordinary loads also call `register`. Removing only writes would leave a legacy decision reader. Removing only the reader could expose formerly restricted capabilities. |
| Promoter owner | `promoter_state` reads call `ensure_schema`, which executes DDL and can add columns. Those are not read-only historical views. Include grace expiry, rollback, review, archive, digest notification and channel acknowledgement writers. |
| Direct promoter SQL | `admin.promotions_review._mark_review_to_pending` bypasses state helpers. A barrier only around the public promoter API misses it. |
| Effects before SQL | Dedupe writes `executor_aliases.json` before its statistics update; rollback can move code or remove aliases first. Merely rejecting the final SQL does not stop the earlier effect or false-success response. Retire/replace the complete entry path. |
| Existing maintenance | `contract_cutover_guard` owns catalog-admission then service-lifecycle exclusion and fresh stopped-service evidence. It is Linux/systemd-specific and a live session, not a crash-persistent all-writer barrier. Reuse its ordering; do not claim it alone freezes standalone SQL writers after a crash. |
| Epoch schema 2 | Migration identity is unique by table name alone; retries require every row still unresolved. Version 3 must distinguish source identity and immutable preservation from later association/disposition. Its codec must preserve SQLite values, including malformed legacy names, instead of silently dropping rows or coercing historical values. |

The migration must cover already-open SQLite connections as well as new
opens. Database-level write refusal can cover DML that bypasses Python
helpers, but does not by itself establish durable service startup inhibition,
source-file identity or retirement of side effects preceding SQL. Those
contracts remain explicit freeze items; no partial barrier is implemented or
advertised as complete.
