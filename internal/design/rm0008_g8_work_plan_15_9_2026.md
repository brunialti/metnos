# RM-0008 — G8 work plan

Date: 15 September 2026.
Source baseline: `06a9472e`.
Status: **in progress; not F5 certification or activation**.
Assignment and approved principles: [decision record](decision_rm0008_g8_scope_15_9_2026.md).

## 1. Scope

Complete G8 before G9. F6 remains assigned to Codex after F5, but no G10
schema or deletion authority is introduced here. Do not repeat the F4
transition or modify the shared root checkout.

G8 has two outputs: an installation-bound, evidence-derived entry proof and
a lossless, reconciled retirement of name-based lifecycle state. A signed
summary, a green isolated test suite, or copying rows alone is not either
output. The detailed missing contracts remain enumerated in the handover
and decision record; they must be closed before their implementation.

Roberto's clarification on 16 September: avoid unnecessary restrictions that
block normal use. Historical acquisition/verification is a certification
workflow, not a new bootstrap/routing prerequisite or user-permission gate.
A missing historical proof limits the certification claim; it must not stop
ordinary services, disable unrelated executors or invent extra consent steps.
Preserve benign structural states explicitly. Refuse the specific claim or
operation where integrity, exact identity or authority cannot be established;
do not broaden a local finding into an unrelated system-wide denial. Existing
productive safety requirements and the approved F5 entry threshold remain.

Roberto's subsequent clarification on 16 September requires incremental
certification and bounded release retention; see the decision record's
"Incremental certification and bounded retention" section. The historical
readers and whole-inventory reconciler below serve initial qualification
and explicit audits, not an ordinary Birth, startup or release prerequisite.
N2 must make the certified result reusable without reopening its whole
dossier; current validity and affected changes remain checked. Acceptance
must show that adding unrelated certified history does not increase the
ordinary verification traversal, and that removing unreferenced old release
trees does not break it. This is a requirement, not a completed optimization
or permission to discard signed history. Implementation and installed code-only
cleanup are recorded in §5.16; the administrative reader and remaining release
coordinator inventory are distinguished in §5.17.

## 2. Immediate prerequisite: faithful terminal replay

This bounded repair strengthens an existing F4 reader; it neither opens F5
nor changes a signer, schema, capability or productive caller.

- Failure location: `executor_birth_operational._decode_terminal_envelope`
  constructs the publication using the request's contract without comparing
  the contract encoded in the signed publication. `_replay_terminal` also
  does not compare the committed claim's `result_binding` with the envelope.
- Observed fact: the serializer includes both contract identity and an exact
  envelope hash in the durable result, but these two bindings are not both
  checked on replay. Existing signature-tampering tests do not cover a
  correctly signed but inconsistent contract or a modified stored hash.
- Causal hypothesis: the reconstructed value can mask an inconsistent signed
  identity; independent postcondition verification then sees the reconstructed
  request identity instead of the contradictory signed fact.
- Expected result: reject either mismatch before postcondition verification,
  checks or publication; preserve the durable record. Valid exact replay,
  historical-key rotation, signed rejection and crash recovery must retain
  their existing behavior.

Contract: schema 2 and the existing signing/hash domains stay unchanged.
The publication's encoded `contract_id` must equal the request's exact
`ContractId.value` before construction. For a committed claim, the stored
`result_binding` must equal `_terminal_binding(encoded)`. A rejected claim
must not be treated as a successful committed publication. No inference from
names, replacement signatures, fallback values or new key is permitted.

Tests: one parametrized negative replay family for the contract, result hash
and terminal-state bindings, followed by the operational/Producer replay and recovery
families whose boundary is affected. Record the red baseline before repair.
This is an isolated productive-code traversal, not a live installation or
an admission qualifying for the F5 threshold. Installed and native Windows
verification remain unproven until actually performed.

Independent internal review found one remaining state ambiguity on checkpoint
`04aa1b14`: an authentic intermediate admitted hint, stored as `rejected`,
has no publication and therefore passed the publication-presence comparison.
The result still reported `admitted` with no operational error. The affected
symbol is `_replay_terminal`; the cause is that publication presence alone
does not distinguish an intermediate hint from a final rejection.

The additional repair must compare the signed outcome and operational error
with the terminal state: committed means admitted/preexercise with no error;
rejected means rejected/needs_human/quarantined with an operational error equal
to the stored rejection code. This does not require equality between the
original check error and a later finalization error. Keep legitimate recovery
hints usable only by the existing in-progress recovery path. Extend the same
negative replay family, record the new red cases, and exercise the positive
rejection/recovery paths affected by this extra comparison.

## 3. G8 contract candidate for independent review

This section makes the approved principles executable at their owner
boundaries. It is a contract candidate, not an installed authority or a
claim that the complete G8 design has passed independent review. The
remaining freeze items in section 6 must be resolved before their code.

### 3.1 Historical verification and admission predicate — N2/N3

Use separate historical readers, not a historical object passed off as a
current selection. The ownership-chain reader authenticates the F4 anchor
and every relevant successor context; `executor_birth_prepared_root` returns
only the public verifier material pinned by those authenticated contexts.
Never return `SealedAuthoritiesV1`, call the private-key loader and discard
the key afterwards, or require an obsolete signing private key to verify
a historical signature. Historical author rotation must use the historical
public inventory, not equality with today's active author key.

`contract_store` owns complete enumeration of admission receipts and their
generation/source/journal bindings. This means persistent signed bindings,
not a claim that the original temporary source or journal still exists.
Roberto explicitly approved this distinction after the internal review;
see the decision record's historical-evidence clarification.
`executor_birth_producer_store` owns the
matching Producer bytes, issuance and terminal rows. Its historical reader
opens the existing database read-only, requires the supported schema, starts
one read transaction and never calls `_open`, `_migrate` or `_verify_claimed`.
The enclosing certifier must prove a common stable frontier; two independent
SQLite snapshots are not a cross-store snapshot.

The proposed qualifying predicate is the conjunction of:

- a valid historical `AdmissionReceipt` with `kind=admission`;
- revision class in `first_birth`, `code_revision`, `authority_revision`,
  `contract_revision`, and `approved_lifecycle=active`;
- a verified Producer receipt and its capability-specific issuance, with
  exact request, contract, objective, source, issuer and receipt-byte binding;
- a committed, authenticated schema-2 terminal with no error, report outcome
  `admitted`, and publication operation `commit_birth_snapshot`;
- equality of all overlapping signed facts: request, contract, generation,
  predecessor, candidate, semantic core, context, revision class, checks and
  embedded admission bytes, plus the exact terminal hash;
- Producer validity at the signed admission act time, not the database's
  unsigned finalization time or an invented time immediately before expiry;
- independently reread persistent receipts/generation payloads and agreement
  of their authenticated source/journal bindings, with no pending ambiguous
  recovery. Original-byte availability is recorded separately: a signed hash
  is not presented as a material reread of an absent original.

Localizations, equivalent publication, promotion, reactivation,
reattestation, rejected/quarantined results and preexercise do not qualify
under this proposed predicate. They remain in the enumerated evidence with
an exclusion reason. A malformed or unauthenticated item is not silently
discarded to make the remaining inventory appear complete.

Important source distinction: ordinary technical admissions still use the
request-v1 derivation in `executor_birth_bootstrap._request_factory`, even
after an F4 context transition. `ProducerRequestV2` is used by the context
reattestation path. Do not require a forged V2 request for ordinary technical
history. Resolve capability from the context's closed producer table and
authenticated key binding; a mutable `capability_id` string alone is not proof.

The owner must distinguish the protocols actually persisted. The V2
reattestation path stores an admission receipt and its
`reattestation-result/v1` binding without a schema-2 terminal envelope; verify
that protocol before recording it as excluded. Conversely, an ordinary
committed terminal may omit the embedded admission when the publisher reused
an existing receipt. Reread that exact durable receipt independently; never
invent missing embedded bytes or treat either valid protocol as corruption.

For ordinary request-v1, recompute the request digest using the factory's
domain plus unsigned 64-bit big-endian UTF-8 field lengths and field bytes:
issuer, capability operation, exact contract, objective and candidate-source
ID. This is `executor_birth_bootstrap._hash`, not the typed
`encode_framed_v1` codec used for Producer store names. Verify the Producer
nonce against the factory's SHA-256 request-ID
derivation as well. The signed admission `issued_at` is the core's act time
captured before checks, not an independently attested finalization timestamp.

Candidate/source identities include the original code bytes. When those
originals are absent, verify authenticated cross-bindings under the approved
historical-evidence option and state that limitation explicitly; do not claim
to recompute either identity from a manifest's code digest alone.

Count each admitted act once across receipt ID, request ID and exact
`(ContractId,generation_id)`. Conflicting duplicates fail; exact duplicates,
retries and context reattestations do not add admissions. Count producers
by authenticated `issuer_id`, not operation, key, directory or executor name.

### 3.2 Certification authority and activation — N1/N2

Roberto approved the dedicated key and the following limited purpose on
16 September (decision record, dedicated certification authority).
Scope: one dedicated administrative F5 certification identity,
separate from author, Admission, Producer, distribution, cutover, head and
HTTP credentials. It can attest the threshold evidence only; it cannot
publish executors, change lifecycle, grant capabilities or delete objects.
Provisioning belongs to the native administrative installer. No candidate,
model or ordinary runtime request can create, select or replace this identity.

Private custody belongs outside the service-owned roots; Linux requires the
existing administrator-owned storage discipline and mode 0600, Windows an
equivalent native ACL proof. Runtime readers receive public material only.
Reuse existing canonical Ed25519 codecs and protected filesystem primitives;
do not add a general certificate service or a second installation manager.

The bounded dossier must bind policy/schema version, installation identity,
F4 anchor, authenticated chain frontier, observed source/build, complete
owner inventories, admitted/excluded act identities, both cycle IDs/profile,
defect-census/ledger head, public-registry revision and certifier identity.
Lists and counts are derived from verified owner records, never supplied as
authority by the caller. Canonical encoding rejects duplicate/unknown fields,
invalid types, truncation and inconsistent references. Large history is
paged by the owner and completely reconciled before a positive result.

Keep the historical threshold proof separate from current-release activation.
The runtime's fixed-root reader verifies the selected installation/head,
policy compatibility and current revocation state before returning a sealed
activation object. A later release does not inherit activation merely because
it descends from the certified one. Neither object hashes a manifest that
already includes that object's own digest. No permissive v1 compatibility
wrapper is needed: `load_f5_activation` has no productive caller today.

### 3.3 Complete cycles and defect evidence — N4/N5

Use the existing external HTTP certification harness where it covers the
required boundary. Register the exact versioned case/profile digest and
expected postconditions before starting; do not call two selected successful
turns a complete cycle. Fixture paths, actors and effects must remain isolated
and non-personal; no external messages or new paid API is authorized.

Record every cycle start and terminal outcome, including failure and
interruption, in sequence. The qualifying pair consists of two adjacent
complete successes on the same authenticated source/catalog/profile. A
profile or base change starts a different sequence; an unexplained difference
blocks qualification. Record real turn references and independently observed
postconditions, not just a harness return code.

Initialize the defect ledger from the known RM-0008 findings, including the
terminal-replay repair in section 2. Use an append-only, authenticated record
of opening, closing and reopening. A closure needs causal evidence, affected
tests, installed verification where required and independent review. A
developer's empty list or a scalar zero cannot establish completeness.
Reopening a relevant defect invalidates a positive current-frontier result.

### 3.4 Lossless migration and retirement — N6/N7/N8

The new epoch store belongs to the service and resolves its path through
central configuration. Version 2 cannot silently gain new meanings: freeze
version 3 explicitly before implementing historical associations and the
cutover record. Keep original copied rows immutable; store any later exact
association and its evidence separately. Retrying preservation must verify
the original bytes even after a legitimate resolution was appended.

Copy all `executor_stats`, `executor_history` and `proposal_promote` rows,
including duplicates/history, with source identity, schema fingerprint,
stable row order, count and digest, followed by independent destination
reread. Preserve source databases. `unresolved` is valid for history without
proof; it is not permission to drop effective restrictions or pending work.
New current epochs come from authenticated current generations, with zero
inherited counters and no inferred historical `name -> current` mapping.

Prefer a short, explicitly managed maintenance window for the one-time
migration over a continuously running synchronization subsystem. The writer
census must cover direct SQL as well as aging/promoter APIs, all services,
scheduled callbacks and administrative commands. The barrier must remain
effective across interruption and cover already-open database connections.
Individual SQLite locks or an in-memory flag do not establish that barrier.

Before disabling a legacy reader, determine which stored restrictions are
still effective against the authenticated current catalog. Preserve each
effective restriction with an explicit exact-generation disposition; if the
binding cannot be established, stop that cutover. A new present-time
disposition must not be misrepresented as proof of a historical association.

Drain or explicitly dispose of open promotion/grace/review work before
retirement. Never turn an already active generation into preexercise.
No live row is automatically discarded because its old mechanism is retiring.
Only after complete reconciliation, necessary dispositions and durable
cutover may legacy writers become unreachable/refuse mutations and legacy
views become non-decisional. Crash/retry must not reopen those writers.

### 3.5 Closed prerequisite slice: public historical context reader

The independent reviewer completed the concrete reader contract after
Roberto's historical-evidence approval. This slice introduces no signer,
certificate, activation, mutation or candidate-selected root. It does not
freeze the remaining certification/ledger/migration contracts.

- `executor_birth_context_v1` decodes the existing canonical material schema:
  reconstruct historical context/epoch from the recorded components, and
  recompute the registry component whose complete configuration is retained.
  Other component source hashes remain historical bindings, not reread code.
  Do not compare historical component versions or source lists with today's
  catalog. Require exact field sets, types, bounded bytes and canonical JSON.
- `executor_birth_prepared_set` reads a historical public set through an
  already locked session, checking set/content identity, mandatory chain pins,
  registry documents, sorted unique key IDs and self-identifying public bytes.
  Preserve the authenticated `producer_id:operation -> store_name` mapping;
  do not construct operational capabilities or infer a current producer table.
- `executor_birth_prepared_root.load_historical_context_verifiers_v1(context_id)`
  treats its argument only as a selector. Internally read the fixed ownership
  chain before and after, select only an unambiguous authenticated transition,
  and return an immutable, inert public view with frontier/transition/set/
  context/epoch and verified document hashes. No caller-supplied chain, root,
  callback, `PreparedSetV1`, `ContextSelectionV1` or `SealedAuthoritiesV1`.
- Remove the private dependency also from the existing
  `_historical_birth_public_inventory_v1`, which the chain's public registry
  reader calls. Use the fixed marker and the same public set reader, without
  calling the chain recursively. Its key-exclusion result includes all current
  self-identifying author public files as well as the marker-set keys. Extra
  author keys can only increase the forbidden set; they never enter a
  historical verification ring not declaring them.

Keep private runtime loading unchanged. The historical reader never reads
`keystore.json` or `private/*`, nor compares with today's active author key.
Existing global locking, protected handle-relative reads, domains, set schema
and context schema are reused. New result types are not signing authorities.

One new discriminating family covers valid history with private access denied
and author rotation; rejection of altered set/material/key/Producer binding;
unknown or ambiguous context; changed chain frontier; immutable result and
no writes. Reuse one existing key-exclusion traversal after changing that
dependency, not all F4 certification tests. Structural boundary review is
required because the read graph changes. Installed and Windows proof remain
open until run.

Explicit limits: this first reader selects target contexts present in the
authenticated F4 chain; the predecessor of its first transition is not
automatically covered. The existing Ownership key-exclusion check does not
cover Admission/Producer keys of later sets; retain that as an N1 finding,
not a guarantee supplied by this slice. Neither limit permits incomplete
history to be reported as complete F5 evidence.

### 3.6 Closed prerequisite slice: historical Producer author declaration

The independent reviewer accepted this bounded extension after §5.3. Reuse
the existing distribution reader and table; introduce no policy interpreter,
historical executable loader, issuer authority or release-specific allowlist.

- `executor_birth_distribution_manifest.read_verified_distribution_file_v1`
  accepts a verified distribution and an exact path/role, derives the root
  from that verified artifact, and reuses the protected exact-tree capture.
  Require the sealed payload, one matching manifest entry, size/content hash
  and unchanged tree. This reader does not independently select the current
  release. The current deployment-descriptor reader retains its own complete
  current-release checks before and after capture.
- `executor_birth_prepared_root.load_historical_producer_authors_v1` selects
  the public context from the fixed chain, as in §3.5. Bind the historical
  distribution by the selected transition's `closed_build_id`, checking its
  chain/head/sequence association. Preserve refusal of distinct transitions
  for one context; equal policy bytes do not resolve a set ambiguity.
- Read the table's public source from that chain's verified current
  distribution. Its size, role and framed `file_content_hash` must match the
  exact historical entry. No hardcoded observed hash or release number, no
  caller root and no arbitrary old-source execution are allowed.
- Project only the unique literal `PRODUCER_AUTHOR_V1` declaration: tuple
  keys of two literal namespace strings, values naming a known
  `RevisionAuthor` member, enclosed in the existing `MappingProxyType` form.
  Reuse bounded AST checks. Reject duplicate bindings/keys, computed values,
  unpacking, unsupported syntax and missing historical namespaces. Do not
  import the source or trust an already loaded Python table/function.
- Return immutable namespace/author data with the context, set, transition,
  historical build, source hash and chain frontier. Reread the fixed chain
  around the whole acquisition. The result proves a signed declaration, not
  arbitrary module effects, allowed origins, an `IssuerRegistry` or a
  qualifying admission. Unsupported history remains an explicit refusal.

One new test family covers the declaration grammar and historical binding,
including runtime table changes, changed source/frontier and namespace gaps.
Exercise the shared exact-file reader and only the existing readers affected
by refactoring. The installed source-availability result is reused, not
recounted as an execution test of this new interface.

### 3.7 Closed prerequisite slice: public terminal evidence — N3

The independent reviewer accepted a single shared schema-2 decoder in
`executor_birth_operational`. Its inputs are bytes, an expected request ID
and exact `ContractId`, not a fabricated `BirthRequest` or `ManifestRef`.
`verify_terminal_evidence_v1` verifies with the public verifier mapping
provided by the owning caller; it neither establishes that mapping's
authority nor qualifies an Admission. The operational wrapper supplies its
existing core-owned public ring and uses this same decoder.

Freeze the actual emitted field sets and types, with canonical ASCII JSON,
a 4 MiB envelope limit before parsing and on emission, and a 64-byte Ed25519
signature. Reuse the existing signature/hash domains and canonical codec.
Validate nullable digests, unique string lists/check identities, existing
enum values, canonical base64 and optional diagnostic objects. Do not use
today's check catalog or constrain operation names to the observed history.
Preserve valid hints, rejection families, preexercise and publication with
no embedded receipt. Embedded Admission bytes still need their own verifier.

Only publication carries a signed `contract_id` in this wire schema. A
report's reconstructed contract is caller context, not an independently
signed fact when publication is absent. Likewise `repeated=True`, report
schema and publisher-call count are reconstruction defaults, not proof of
retries or successful publication. State/hash/postcondition checks remain
at operational replay; exact issuance/Admission bindings remain at N3.

One new discriminating family covers pure public verification and malformed
signed documents. Reuse the affected operational replay/recovery tests after
the shared decoder changes; do not rerun unrelated public-context tests.

### 3.8 Producer history acquisition — N3

The store owner provides `read_producer_history_v1` at the selected
`config.PATH_USER_STATE` root, with no caller-supplied database, SQL, callback
or signing authority. Move the unchanged Birth/Producer basename constants
from bootstrap into this owner and reuse them in bootstrap. This is raw
evidence acquisition, not an authenticated admission or a sealed F5 result.
The call requires an already initialized, selected runtime configuration.
Product module imports currently reach `config.ensure_dirs`; a cold
administrative certifier must install its no-mutation protection **before**
those imports. The API alone does not promise cold-import purity.

Return separate immutable receipt and issuance records with every stored
column and their original row IDs. Include all states and unmatched records;
no inner join, filtering, recovery, expiry substitution or implicit cleanup.
Require schema 5, real tables and exact supported column shapes. Preserve
the table definitions. An unsupported schema, malformed stored value,
missing database or exceeded budget is an explicit failure, not an empty or
partially successful inventory.

Use one `mode=ro`/`query_only` transaction for both tables, deterministic row
order and metadata-only byte/type checks before loading row payloads. Default
budgets are 100,000 total rows, 128 MiB total field bytes and 15 seconds, with
an 8 MiB per-row field-byte limit (SQLite receives an additional 4 KiB for
record headers). Table SQL is bounded to 64 KiB before materialization.
Positive caller budgets may narrow or widen
the inventory bound, never bypass the fixed per-row limit. Check selected
root/database identity and restrictive metadata before and after; do not
create or repair paths. Refuse linked/nonregular database or sidecar files.

This is a logical SQLite snapshot, not a physical no-write guarantee:
SQLite can maintain its WAL shared-memory sidecars during a read. Keep normal
locking; do not use `immutable` or `nolock` for the live database. See the
[SQLite WAL documentation](https://sqlite.org/wal.html#read_only_databases)
and [URI immutability contract](https://sqlite.org/uri.html#uriimmutable).
The reader does not prove a common contract/Producer/chain frontier, prevent
every transient namespace substitution or establish native Windows ACL
protection. Those remain explicit certification requirements.

One new isolated family covers complete/raw inventory, WAL-consistent reads
across concurrent commits, absent/unsupported/malformed sources, links and
replacement, and exact/over-budget boundaries. Exercise only the bootstrap
tests affected by relocating the constants. No installed qualification is
claimed from these fixtures.

The independent reviewer accepted this bounded contract, including the
explicit initialized-runtime/cold-import distinction. No additional
authority, normative waiver or installed certification follows from it.

### 3.9 Exact historical publication read-back — N3

`contract_store.read_historical_birth_evidence_v1` accepts an exact
`ContractId`, generation digest and an explicit context digest or `None`.
It resolves only the configured store: `None` selects the original V1
receipt layout; a context selects V2, without fallback to another context.
There is no caller root, invented `ManifestRef`, current-generation check,
private authority, publication lock creation or operational recovery.

Return immutable raw receipt bytes and the three exact stored generation
payloads. Reuse the owner's structural binding, path and generation digest
codecs. Bounded regular-file reads reject links, hardlinks and changed files;
binding is limited to 64 KiB, receipt/manifest/language state to 1 MiB each,
and the Ed25519 signature to exactly 64 bytes. Require the exact generation
file set and matching generation digest. Observe directory identities around
acquisition and independently reread the binding and receipt.

This proves exact bounded acquisition and the generation's content address,
not authenticity of receipt/author signatures or historical policy. It does
not parse old declarations using today's executor standard or claim absent
code/journal originals were preserved. Signature/context/act verification,
complete enumeration (including retired/unreachable contracts), common
frontier and native ACL certification remain the enclosing N3 work. The
already-initialized-runtime precondition from §3.8 applies here too.

One isolated family covers both layouts, non-current generations, no V2
fallback, all size and identity boundaries, missing/corrupt durable bytes,
link/substitution attempts and refusal of mutating/current readers. Existing
current-generation validation and publication remain unchanged.

Independent review accepted this bounded acquisition contract and found no
blocker in the implementation. The complete inventory must observe both
stored layouts explicitly: a terminal carrying a context does not establish
that its durable receipt was stored under the V2 path. Request protocol,
receipt layout and cryptographic context are separate facts.

### 3.10 Complete raw receipt inventory — N3

Extend the same store owner with `read_historical_birth_inventory_v1`.
Resolve the configured store only; enumerate every physical contract,
including contracts absent from the live catalog or with retired pointers.
Derive identity from the existing bounded canonical binding, never an
executor name, current `ManifestRef` or terminal observation. Enumerate both
stored receipt layouts independently, preserving every exact locator and
raw receipt, including duplicate, rejected or otherwise ineligible acts.
Empty receipt sets are explicit, not omitted contracts.

The immutable inventory contains its selected source path, per-contract raw
binding, separate generation/retirement ID censuses, and raw V1/V2 receipt
records. Distinguish the exact existing three-file generation and two-file
retirement shapes; observe their regular-file metadata without reading or
authenticating payloads. Receipt generation locators must name a generation,
not a tombstone. The exact-item reader owns admitted-generation bytes.
Coexisting V1/V2 locators remain separate even when receipt bytes are equal;
deduplication belongs to the enclosing certifier.
Do not reopen those payloads once per historical context merely to enumerate
receipts. Signature, act, policy and complete-certification validation remain
separate consumers, not claims of this acquisition API.

Fail without a partial return on an absent root, unknown/staging entries in
the traversed namespaces, malformed bindings/locators, links, unsafe file
types or permissions, missing referenced generations, or exhausted budgets.
Known `current`/`writer.lock` metadata may be observed without using either
as a selection, acquiring a lock, creating a file or recovering publication.
Only `binding.json` and `generations` are required at contract level; a
contract with no current pointer is still history, not silently excluded.
An existing and actually empty root returns an explicit empty inventory.

Default bounds are 100,000 traversed entries, 128 MiB of acquired binding/
receipt/observed-lock bytes and a 15-second cooperative deadline. File bounds reuse §3.9.
Bound scans before sorting/materializing; refuse rather than truncate.
Observe every traversed directory and file before its read/scan and compare
identity, size, change timestamps and directory child sets after acquisition.
This detects ordinary
concurrent namespace/data changes but is not a cross-store transaction,
crash-durable all-writer barrier or protection against every transient ABA
substitution. Cold-import and native Windows limitations remain explicit.

One new inventory family covers all contracts/layouts and empty histories,
deterministic complete output, budgets, malformed/unknown/staging namespaces,
raw failed acts, links and data/membership/replacement races. Reuse the exact
reader's unchanged file-guard evidence; do not repeat its 39 cases merely to
inflate coverage. An installed inventory probe, if run, must report aggregate
acquisition facts, not qualifying-admission counts.

Independent review approved this bounded contract and found no existing
equivalent: the live manifest inventory constructs references from the live
origin map and may enter a locking residual-binding helper;
`diagnose_store` is reference/key-based and treats an absent root as empty.
Neither can supply this complete raw receipt namespace. The review required
the tombstone census and physical-layout distinction made explicit above.

**Reviewed refinement after the installed observation:** the writer creates
the contract/generations directories and lock before binding creation. The
initial inventory contract refused any missing binding; a live diagnostic
found a namespace with just an empty `generations` and a one-byte lock.
This shape is compatible with a pre-binding phase, but proves neither crash
provenance, writer inactivity nor absence of previously deleted history.

Acquire only the exact empty-unbound shape as a **separate opaque record**:
physical storage key, single NUL lock byte, no binding/current/receipts or
other children, empty generations, regular single-link lock and protected
directories. On POSIX require the existing private 0700/0600 modes and the
same owner/group as the selected root. Keep it within the same entry/byte/
time budgets and final directory/file reread. Do not fabricate a `ContractId`,
deduplicate it away, invoke the live catalog's locking helper or clean it up.
Any deviation still refuses the complete acquisition. The output explicitly
lists `unbound_empty_namespaces`, not verified crash residues. Native Windows
ACL validation remains outside this raw observation.

This is an acquisition refinement, not a certification waiver: the certifier
must reconcile the opaque observations and Producer history under its common
barrier before treating the installation as certification-ready. No new
authority or product-specific locator exception is introduced. Independent
review recommended this explicit preservation over deleting data merely to
permit acquisition; installed results before this refinement remain failures.

### 3.11 Initial-context public keys — N3

The §3.5 selector covers authenticated transition targets only. Complete
history can also refer to the predecessor of the first transition. Extend
the public evidence selector to that one additional authenticated edge,
without selecting an executable old runtime or rerunning the F4 transition.

The existing private selector gains an explicit opt-in for the first
predecessor, disabled by default. Only the public-key evidence reader enables
it. Require exactly one match across targets and that predecessor; ambiguous
or unknown contexts fail. The existing historical Producer-author loader
remains target-only: the first successor's distribution does not authenticate
the predecessor's Producer policy merely because it references that context.

For the predecessor, read the existing fixed marker/public-set interface
under the non-creating shared Birth barrier. Match the returned set ID,
context ID and epoch to the first authenticated transition's `previous_*`
facts. This reuses the set's content-address, marker, material and public-key
checks; no current author equality or private keystore lookup is introduced.
Reread the fixed chain around acquisition as before.

Immutable evidence explicitly records `binding_kind` as `transition_target`
or `initial_predecessor`. For the latter, `transition_id` identifies the first
authenticated edge binding the predecessor, not a distribution that issued
its policy. These are public verifier facts only: historical Producer origin/
author scope, cross-store frontier and qualifying acts remain separate work.

One bounded family covers exact predecessor pins, absent/altered markers,
ambiguity, forbidden private/operational readers, changing frontier and the
continued refusal to project a successor's Producer policy backward. Reuse
the unchanged low-level public-set tests; run only affected selector cases.
Any installed proof must select this predecessor explicitly, not repeat the
two transition targets already checked in §5.2.

Independent review accepted this public-key-only extension. In particular,
the marker's provisioner-build identity is not a signed distribution policy;
the Producer-policy loader must continue to refuse this predecessor.

### 3.12 Historical origin declaration — N3

The per-act verifier also needs the executor origin derived from the exact
signed contract, not copied from a Producer receipt. Extend the existing
authenticated Producer-source projection to include the literal
`_MANIFEST_ORIGIN_TO_EXECUTOR_V1` map from that same source. Do not rebuild a
historical runtime, evaluate its Python, introduce a policy engine or use
today's loaded map.

Rename the unreleased evidence type/loader from `HistoricalProducerAuthorsV1`
and `load_historical_producer_authors_v1` to
`HistoricalProducerDeclarationsV1` and
`load_historical_producer_declarations_v1`. There are no productive callers;
keep one evidence interface rather than two separate source/chain reads.
Return both immutable maps with the existing source/build/context bindings.
The author-only projection remains a small internal function so its existing
grammar and tests remain applicable.

Share only bounded AST/literal-table selection. The origin map requires one
top-level `MappingProxyType` literal with distinct known `ManifestOrigin`
members as keys and known `ExecutorOrigin` members as values. Reject computed
entries, duplicate/reassigned declarations, unpacking and unknown enum values.
Do not require every current origin or import present-day table completeness:
the map records historical declarations. A missing origin limits the specific
act's verification, not unrelated contexts or ordinary runtime availability.

This establishes declarations only, not all historical module effects or
permission grants. Initial-predecessor policy selection remains unsupported
until its own authenticated source binding is established. Test the new map's
grammar and immutable projection plus affected historical-source bindings;
do not repeat unrelated public-set, inventory or terminal tests.

### 3.13 Ordinary historical Producer binding — N3

Add one pure owner join in `executor_birth_producer_store`,
`verify_historical_producer_binding_v1`. Inputs are exact Admission bytes,
one raw receipt row, one raw issuance row and historical declarations from
§3.12. Their acquisition/common-frontier provenance belongs to the enclosing
owner workflow; a Python evidence container is not an operational authority.
The result is inert authenticated binding evidence, not a qualifying act,
sealed request, certification or permission to mutate a store.

Verify Admission against the historical public ring and require its context
to match that same public set and its kind to be `admission`. Reattestations
use their separate protocol, never an ordinary-request fallback. Parse the
Producer only to select candidate issuer/key namespaces. Resolve exactly one
operation by historical namespace,
key and request-v1 identity; do not select it from mutable `capability_id`.
Derive origin from the signed contract and historical origin map, and author
from that namespace's declaration. Supply only that origin/author/key to the
existing signature verifier, with the signed Admission act time. Preserve
the existing temporal tolerance; never use today's time or unsigned DB times.

Move the request-v1 identity calculation into one small pure codec in
`executor_birth_receipts` and have the two existing bootstrap request-v1
sites use it unchanged. No new request format, domain or namespace is added.
Check the ordinary request nonce, exact Producer byte hash in Admission,
receipt/issuance byte equality, IDs, issuer, objective, source, provenance,
expiry, request, contract and derived namespace against stored columns.
Database `capability_id` is only an equality check after independent resolution.
State and registration/claim/finalization/lease times are not overlapping
signed facts: retain them, but do not use them as proof of success or act time.
Apply the existing owner byte bounds before parsing: Admission 1 MiB and
Producer fields 8 MiB. Normalize malformed decoding locally at this new
boundary; do not expand the scope into changing the operational receipt codec.

This join does not decide success from a DB state or validate an arbitrary
protocol as ordinary: terminal outcome/hash, publication, generation payload,
source/journal cross-bindings, reattestation protocol, deduplication and final
qualification remain separate. It cannot create a current manifest or infer
missing original source bytes. Missing policy for a particular act remains
an explicit evidence limitation, not a failure of unrelated normal use.

One discriminating family uses actual signatures and raw row types, with
wrong signatures/context/provenance, expired-at-act versus expired-now,
forged stored columns, operation/key mismatch and sparse origin declarations.
Verify byte-identical request-v1 results and affected bootstrap factory paths;
do not repeat inventory acquisition or already-verified terminal families.

### 3.14 Ordinary historical publication join — N3

Freeze this bounded slice after the independent review of the actual schema-2
publisher and reuse paths. Add a pure payload verifier to `contract_store`
and a pure composition to `executor_birth_operational`. Neither selects paths,
opens a store, reconstructs `ManifestRef`/`BirthRequest`, invokes current
lifecycle policy, acquires private keys, or enables F5.

The store verifier receives owner-acquired `HistoricalBirthEvidenceV1` and
the selected historical Admission/author public rings. Bound byte sizes as
in the acquisition owner; verify Admission, canonical contract binding,
the exact three-payload generation digest, contract/generation equality and
the manifest signature. A V2 physical context must match Admission; explicit
V1 (`None`) remains valid without inventing a missing context directory.
Language-state bytes are authenticated through the signed generation address,
not reinterpreted using today's language or manifest standard. Return the
authenticated Admission, not a current executable contract.

The operational composition receives that durable evidence, exact raw
Producer/issuance rows and §3.12 historical declarations. Reuse §3.13, then
verify the schema-2 terminal with the same historical Admission ring. Require
committed row state, no rejection code, exact terminal result hash, a completed
`commit_birth_snapshot` publication and no report/operational error. Compare
all overlapping contract/generation/predecessor, candidate/semantic/context
and revision facts. Permit both admitted/active and preexercise/preexercise;
the latter is authentic success but excluded from the technical threshold.

The embedded Admission is optional on legitimate receipt reuse. When present
it must equal the independently acquired durable bytes. Do not require its
signing key ID to equal the terminal's: historical key rotation remains valid.
Project checks through the existing `_receipt_checks` codec, adding exactly
the publisher-owned `authoring_install_journal_v1` (rule `1`, passed, exact
Admission journal hash). Reject a reserved-name collision or any other map
difference. Do not impose today's fixed check catalog on historical reports.
Semantic-review hash equals the passed semantic check hash, otherwise None.
A passed approval check must match `approval_hash`; a not-applicable check's
synthetic hash is not approval evidence and adds no implicit equality rule.

Return frozen publication evidence, not a qualifying count. Original source,
semantic-core input and temporary journal bytes are not reconstructed; their
signed cross-bindings are checked under the approved historical-evidence
option. Rejections, intermediate hints and V2 reattestations do not enter this
ordinary successful-publication protocol. They remain separate inventory
outcomes, not silently discarded errors. No global frontier or completeness
claim follows from this join.

One new signature-based family covers payload identity, exact signed terminal
bindings, valid reuse/rotation/preexercise/older check catalogs, map/hash
differences and forbidden operational access. Exercise one installed sample
through the new join using the existing explicit candidate-reader overlay;
do not repeat unchanged historical acquisition/Producer test families.

### 3.15 Consolidated historical verification — N3

Roberto requested fewer micro-increments on 16 September. Complete the
remaining historical joins, classification and reconciliation as one block,
with one targeted test family and one whole-inventory installed observation.
This does not combine G8/G9 or authorize a new signer, migration or activation.

Share the existing V2 request/objective framing as a pure data codec. Only
the existing sealed constructor creates operational requests. Authenticate
V2 reattestations against historical Admission/Producer/author rings and
exact durable payloads, request/objective/nonce, issuance columns and
`reattestation-result/v1` binding. These acts have no schema-2 terminal;
retain that valid protocol. Verify snapshot and initial-adoption marker
bindings; continuity markers remain references to earlier signed evidence,
not a claim that checks were freshly rerun. Reattestations never add a
technical admission.

Independent review identified a necessary scope distinction: the V2 request
does not encode issuer/operation, and its DB alias is not a capability grant.
Read the historical bootstrap and intent artifacts through the existing
public distribution owner. Project only the recognized literal Installer
namespace, its assembly selection and the V2 alias used by the factory.
Do not execute historical Python or infer authority from a mutable DB alias.
Unavailable source or an unrecognized protocol remains an explicit evidence
limitation; it is not a new ordinary-runtime denial.

The recognized transfer is a versioned AST profile of the complete factory
classes, constructors, inheritance, prepared carrier and assembly selector,
plus the capability carrier in intent. Only docstrings/source positions are
ignored. This is a protocol profile over already authenticated artifacts,
not a release allowlist or replacement for distribution authentication.
Separate literal namespace/alias projection cannot authorize a different
flow. V2 continuity additionally compares the two authenticated source IDs;
an ordinary predecessor retains its distinct protocol. Missing/conflicting
predecessors propagate through continuity links, independent of scan order.

Acquire multiple contexts with one authenticated chain observation and final
chain reread. Reuse exact verified source projections only within that call;
no global cache or skipped frontier check. Reconcile every raw contract
receipt, Producer row and issuance. Authenticate ordinary publications and
reattestations by their own protocols, retain other states and limitations,
and deduplicate exact receipt/request/contract-generation identities before
reporting candidate technical acts. Contradictions are findings, not silent
exclusions. A complete observed inventory is not the later all-writer
certification frontier, and candidate counts cannot activate F5.

The complete census exposed matching-source availability, rather than an
unknown protocol, as the cause of most reattestation gaps. The discriminating
read-only probe in §5.15 found exact public Git artifacts for all 53 signed
release records. Extend the same multicontent owner with bounded inert public
source candidates (no caller-selected root or Git dependency). Match their
exact path, role, size and content hash against each authenticated historical
distribution before projection. Git or any other archive supplies bytes, not
authority; unknown source cannot borrow current policy. Preserve the existing
current-public-copy path when no archive candidate matches. This is recovery
of signed public evidence, not reconstruction of temporary authoring journals
or a new permission to execute old code.

## 4. Implementation sequence

1. Close N1–N5 into exact public historical reader, evidence, certification
   and activation contracts. Do not expose `SealedAuthoritiesV1` or call
   `_open`/`_verify_claimed` to perform supposedly non-mutating historical
   verification. Derive validity time from authenticated act evidence.
2. Close N6–N8 using the actual source/owner/writer census and approved
   lossless/disposition rules. Freeze the next epoch schema and recovery
   protocol before implementing it; retain unresolved history without
   fabricating associations or importing old counters into new epochs.
3. Implement the closed contracts in causal slices, with risk-to-test
   mapping, boundary review and independent review at each new authority.
4. Obtain real installed evidence and complete G8 exit checks. Missing
   producers, cycles, defect closures or dispositions remain blockers,
   never synthetic evidence. Only then enter G9.

## 5. Verification ledger

| Requirement | Symbol | Module/flow test | Installed proof | Result |
|---|---|---|---|---|
| Exact signed publication contract | `_decode_terminal_envelope` | Negative replay family | Not run | Red reproduced, local green |
| Exact terminal hash/state/outcome/error | `_replay_terminal` | Same family; nine cases after internal review | Not run | Red reproduced, local green; bounded diff reviewed |
| Existing replay/recovery behavior | Operational, Producer and bootstrap owners | Initial 75 + 7; later 20 affected cases, overlapping | Not run | Local green |
| Productive graph | Existing boundary checker | Structural findings: 0 | Not run | Public-only historical read graph reviewed |
| Reviewed source binding | Compiled source-review checker | Full checker rejects changed source digest | Not run | Expected pending-review mismatch; pin unchanged |
| Public historical context | Decoder, set reader and fixed-root selector in §3.5 | 32 new cases; 15 affected existing cases | Two live contexts, isolated hybrid proof in §5.2 | Reader verified within stated scope; not full candidate deployment |
| Historical Producer author declaration | Shared exact-file reader and inert projection in §3.6 | 45 new cases; 10 affected existing cases | Source availability only, §5.3 | Local green and independently reviewed; not Admission qualification |
| Public terminal evidence | Shared operational decoder/verifier in §3.7 | 52 new cases; 54 operational and 21 diagnostic cases | Wire-shape observation only, §5.5 | Local green and independently reviewed; no installed qualification |
| Complete raw Producer history | Owner reader in §3.8 | 46 new cases; 3 affected bootstrap cases | Full candidate-reader snapshot, §5.6 | Local/live read green; no signature or global-frontier claim |
| Exact historical publication bytes | Owner reader in §3.9 | 39 new cases | Explicit V1/V2 samples, §5.7 | Exact acquisition verified; no authentication or inventory claim |
| Complete raw contract receipt inventory | Owner reader in §3.10 | 72 distinct new cases | Full candidate-reader census, §5.8 | Acquisition verified; one opaque unbound namespace unresolved for certification |
| Initial-context public keys | Opt-in public reader in §3.11 | 11 new, 9 affected selector and 3 affected policy cases | Explicit first-predecessor proof, §5.9 | Public keys verified; predecessor Producer policy remains separate |
| Historical Producer origin declaration | Shared source projection in §3.12 | 21 new and 38 affected existing cases | Selected target declarations, §5.10 | Local/live read green; no admission qualification |
| Initial Producer policy binding | Exact authenticated material inventory | No repeated product tests | Dedicated read-only diagnostic, §5.11 | Policy source absent from initial material; limited historical evidence claim |
| Ordinary historical Producer binding | Pure owner join and shared request-v1 codec, §3.13 | 57 new, 16 affected factory and 1 initial-installer cases | One signed join and exact durable reread, §5.12 | Verified within scope; not terminal success or N3 qualification |
| Ordinary historical publication | Pure durable verifier and terminal composition, §3.14 | 55 new signature/binding cases | One complete ordinary publication join, §5.13 | Verified publication sample; not complete inventory or F5 threshold |
| Consolidated historical inventory | Shared V2 codec, historical scope, multicontent acquisition and reconciliation, §3.15 | Consolidated/affected tests and corrected review counterexamples, §5.14 | All 7,024 durable receipts and 7,040 Producer/issuance rows, §5.14 | 24 technical candidates from two issuers; historical gaps and certification frontier remain explicit |
| Archived public scope evidence | Exact signed artifact matching at the same historical owner | 18 new archive cases plus 8 affected multicontent cases | 6,504 acts reconciled across all 53 selected target contexts, §5.15 | Selected-chain scope/continuity gaps resolved; 520 other-context receipts remain explicit |
| G8 entry threshold and migration | Not yet implemented | Not run | Not run | Open |

The four new cases failed before repair in two disjoint runs (2 + 2), then
passed within the 75-test operational/Producer run (5.79 seconds). Seven
additional bootstrap/recovery cases passed (0.34 seconds). Do not add the red
runs or rerun cases to the 82-case green total. `check_contract_boundary_policy`
passed. The full closed-build checker still reports
`birth_closed_source_review_mismatch`; it is not reported as a green check.

Repeatable test commands, each with fresh empty `METNOS_USER_DATA` and
`METNOS_USER_CONFIG` roots, using `/opt/metnos/.venv/bin/python`:

```text
-m pytest -q tests/runtime/executors/test_executor_birth_operational.py tests/runtime/executors/test_executor_birth_producer_store.py
-m pytest -q tests/runtime/executors/test_executor_birth_bootstrap.py -k 'new_build_can_claim_after_expired_terminal_staging_rejection or real_release_request_factory_accepts_a_first_admission or recovery_'
```

The subsequent independent review and repair are recorded in
`../reports/rm0008_g8_review_15_9_2026.md`. Five additional negative cases were
red before the outcome/error comparison. The affected replay family then
passed 17 cases (1.45 seconds), followed by the one new preexercise terminal
case (0.23 seconds) and two existing crash-recovery cases (0.29 seconds):
20 unique cases in this increment. The 20 are not added to the earlier 82 as
if disjoint; inherited Producer/store suites were not repeated.

Repeat the later set only if its boundary changes, using the same isolated
roots and interpreter:

```text
-m pytest -q tests/runtime/executors/test_executor_birth_operational.py -k 'inconsistent_durable_bindings or non_admission_is_durably or admitted_pipeline or replay_survives or revoked_historical or prepublication_diagnostic or ambiguous_publisher_failure_keeps_claim or crash_between_publisher_postcondition_and_finalize'
```

No live `/agent/turn`, native Windows certification, source-pin refresh,
public publication or deployment has been performed for this repair.
Public behavior and signing schemas are unchanged; the candidate
must remain unreleased until the applicable verification and review gates.

### 5.1 Public historical-reader increment

Implemented the closed prerequisite contract in §3.5. The first new test
reproduced the transitive private dependency: the Ownership public-registry
path tried to read `author-root-v1/keystore.json`. It now traverses real
protected public reads with private directories inaccessible and the active
author rotated. The replacement author extends only the Ownership exclusion
set, not the historical author verification ring.

The independent reviewer found an unnecessary `authority_registry.version ==
"1"` check. A second red test prepared real material with another recorded
version, then emptied today's component and Producer catalogs. Removing the
version comparison preserves the canonical schema and recomputed registry
digest while allowing that historical evidence to be read.

Verification on this increment:

- New public-reader family: 29 passed in 20.55 seconds. Covers exact pins,
  rotated/public-only keys, altered set/material/registry/key/namespace,
  malformed material, immutable result, absent/ambiguous context and changed
  head/records/transitions. Fixed-root selector cases stub only the chain
  boundary; they are not a cryptographic test of the installed chain.
- Affected existing prepared-set family plus one real Ownership key-exclusion
  traversal: 15 passed in 9.52 seconds. No inherited operational or Producer
  suite was rerun.
- Three additional malformed document cases exposed leaking `ValueError`,
  `UnicodeEncodeError` and `RecursionError`. The shared canonical decoder now
  reports the existing `PreparedSetError`, without admitting more documents.
  These three and seven affected existing positive/refusal cases passed in
  5.23 seconds. Those seven repeat earlier coverage; they are not new cases.
- Total distinct coverage for this reader increment is 32 new cases and 15
  affected existing cases, not a sum of repeated runs. Structural R1 found
  zero findings; generated boundary-policy checks and `git diff --check`
  passed. Reviewed source pins were not refreshed.
- Final independent static review found no further blocker in this bounded
  reader. No reviewer tests were repeated.

Repeat only when this boundary changes, with fresh isolated data/config roots:

```text
-m pytest -q tests/portable/rm0008_2b/test_group8_public_history.py
-m pytest -q tests/portable/rm0008_2b/test_group3_prepared_set.py tests/portable/rm0008_2b/test_group3_authority_consumption.py::test_deployment_keys_are_disjoint_from_the_authenticated_birth_set
```

Code/test/review checkpoint: `c1dcfb19` (`Read historical Birth contexts through
public evidence only`). The result is an inert historical evidence reader,
not a certification signer, F5 activation, verified production admission count
or native Windows proof.
The first-transition predecessor and later-set Ownership exclusion limits in
§3.5 remain explicit. The live reader boundary was subsequently exercised as
described below; complete admission-owner evidence remains the next contract.

### 5.2 Live public-context read-back, without deployment

Administrative proof run `run-87e_n1bg`, 15 September 2026, completed in
26.006 seconds as service UID 995 / GID 985, HTTP PID 1432998. The native
HTTP process selected release directory `00000000000000000053`; the cold
chain independently authenticated that same required distribution, head
`sha256:e63bc648309c87f43ba9ba39a9f1a22456b0361f2bf14b707cfb8f0cb4f17743`,
and 53 transition records.

This is explicitly a hybrid integration proof: root-owned release-53 policy
and dependencies, with only the three candidate public-reader modules loaded
from their measured source bytes in an isolated process. The worktree is not
on the import path, candidate bytecode caches are not executed, and critical
module origins and source hashes are recorded. No chain callback or verifier
is replaced and no source-review pin is changed. The independent reviewer
reviewed this delimitation before the successful run.

The exact historical-context selector read both ends of that authenticated
transition history, checking the chain around each acquisition:

| Context | Set | Public result |
|---|---|---|
| `sha256:8df4f2b7481809f3c8eafed3947c413bd430e3a78eb37a952e487fb89d470266` | `61d47c62d75d25bf16732017e4d32b6a7b72ef465d105d83b36b25dc7dbc8a4f` | 2 author verifiers, 1 Admission verifier, 11 Producer namespaces |
| `sha256:13f78a4c9612ab7daaae5f2055f00d04ced7a28b42f96e4e654afb55896dd790` | `6cb8d2f76d5cec99d5b3150a00f0faf657d7ae177ae6823abaa3104c7079e19e` | 2 author verifiers, 1 Admission verifier, 11 Producer namespaces |

The read-only guard observed zero private-read attempts and zero attempted
data writes/execution/network operations. It separately blocked 153 optional
import-time permission repairs from `config.ensure_dirs`; those repairs did
not modify the installation. The only persisted output is the administrative
runner's private proof record. No services were stopped or restarted.

Probe corrections before that successful result are not product failures or
passed tests: two early attempts omitted the selected workspace; two reached
the cold verifier with the candidate's different source-review pin; two
assumed an install-root environment override that the native launcher
deliberately strips. The corrected probe uses HTTP's native module/working-
directory selection and verifies it against the authenticated distribution.
The earlier `source review` refusal alone was not evidence of production
corruption. All protections remained enabled throughout.

Reproduction source: `internal/tools/rm0008_g8_public_history_proof.sh`, SHA-256
`58b30ca7a71bd28f95fd831294a2015e948ca3ce9be3741ae134b03fcb8559ab`.
Measured reader hashes, unchanged before/after:

- Context decoder: `ce285d2606e9993d5aa3613a64e6853daf84d8eadaada08ac6997a2c5fa4822c`.
- Public set reader: `56406414cc6984a663ec70acd4e5ee9efc04ed87288eb3a0120787b90b0cd6fa`.
- Root selector: `f655ec9bdccf6a86bd8a11cd0b37e909e8342a46b26e21fb0b19834d2fced823`.

This verifies only those two context read-backs with the stated composition,
not every historical act, the complete candidate release, Windows, F5 entry
or activation. Do not repeat the probe without a change to this boundary.

### 5.3 Historical Producer policy source availability

The historical public registry binds Producer namespaces and verifier keys,
but not the allowed revision author. The ordinary runtime derives that scope
from `executor_birth_producer_table_v1.producer_author_v1`. Neither a receipt's
self-declared author nor today's unbound table can establish historical scope.
The Producer catalog digest covers namespace pairs, not that author policy.

A separate, discriminating read-only observation used the same protected
composition as §5.2, with mode `producer-policy`: run `run-37ldnu_4`, completed
in 20.478 seconds. It did not repeat the two context read-backs. The real cold
chain authenticated release records 1–53, and each record listed exactly the
same public policy source at `runtime/executor_birth_producer_table_v1.py`:

`sha256:62d266497a6d2198ab152267084668f1c6173c99e309ce3ec80b5de6335901f9`.

The installed source has exactly that framed distribution-content hash and
declared size. An authenticated public copy is therefore available for every
observed release; no relaxation of historical author-scope verification is
needed for this observed history. This is source availability, not yet a
verified policy projection, a qualifying admission count or a complete F5
inventory. Any future differing source requires an explicitly supported
historical interpretation, not a fallback to current policy or execution of
arbitrary historical code.

HTTP remained PID 1432998 on release 53, with the same authenticated head as
§5.2. The guard observed zero private-read attempts and zero data-write or
execution attempts; 153 optional import-time permission repairs were denied.
No service or installed state changed. Reproduction script SHA-256 for this
mode: `dc5427cf303f013cded5910859c4443b7691a2c1139af4d422947f88ba241299`.

### 5.4 Historical Producer author declaration: local proof

Implemented §3.6 without changing the original Producer table or adding a
framework. The generic distribution reader is a thin public wrapper around
the existing protected capture. The prepared-root reader extracts literal
data from authenticated source; it never evaluates that source or uses the
current Python table/function as historical authority.

The first feature test reached the absent API before implementation. An
earlier fixture-import error was corrected and is not counted as a product
failure. Verification after implementation:

- 35 new projection/grammar/binding cases passed in 11.05 seconds.
- 8 new shared-reader/current-descriptor cases and 10 affected existing
  context/descriptor cases passed in 6.60 seconds.
- 2 additional old-context/new-release cases passed in 1.60 seconds. An
  identical public copy can supply the old declaration; a different current
  copy is refused instead of silently replacing historical policy.

This is 45 new cases and 10 existing cases, not 55 new cases or additional
installed admissions. The existing cases overlap the earlier verification
ledger. The real signed fixture files and protected filesystem reads are
exercised; the chain and administrative metadata remain explicit test seams.
These tests are not installed-chain or native Windows certification.

The structural closed-boundary check found zero findings, the policy
projection check passed, and `git diff --check` passed. The complete source
review pin is still pending and was not refreshed. The independent reviewer
inspected the implementation without rerunning tests or modifying files and
found no blocker within this slice. No deployment or installed mutation ran.

### 5.5 Terminal wire-shape observation and shared verification

Before tightening the shared decoder, a new metadata-only observation checked
the actual wire shapes, not the earlier admission screen. Run `run-2t66tq9d`
used `producer-terminal-shape` in the read-only census script, as service
UID 995/GID 985 against HTTP PID 1432998's selected Producer store. It observed
80 JSON terminal envelopes, no invalid JSON, a maximum envelope size of
5,499 bytes, 64-byte terminal signatures and a 757-byte maximum Producer
receipt. Publication/embedded Admission were present in 69 envelopes and
null in 11; diagnostics were absent in 78 and objects in two. The script
returned aggregate lengths/types only, never request contents or signatures.
Script SHA-256: `3c4f4c21dbaa493efb45f96298231e29e87fdcd87ac6403ee28e12a9e8b85ded`.
These are shapes, not authenticated acts, a stable certification frontier or
qualifying counts. The 4 MiB codec bound is not fitted to these observations.

Implemented §3.7 using the shared canonical codec and unchanged signing and
binding domains. The operational wrapper and public verifier share one
decoder. The internal epoch diagnostic caller no longer fabricates a
`BirthRequest` to decode history.

Verification: 52 new public-wire cases and all 54 affected operational cases
passed in 5.21 seconds. The separate 21-case epoch diagnostic harness also
passed. Its first standalone run failed before decoding because its explicit
fixture path unnecessarily acquired live author authority; the installation
correctly refused that access. The fixture-only path now avoids the unused
lookup, while the real path still authenticates its current state. One old
negative expectation also named a later twin-lookup failure; it now requires
the earlier wire/issuance contract refusal. The rejection assertion remains
mandatory. Failed setup attempts and repeated cases are not added to coverage.

The structural boundary check reported zero findings; policy projection and
diff checks passed. The independent reviewer inspected both the public codec
and the diagnostic caller without repeating tests and found no blocker in
this bounded increment. Source-review pins remain unchanged. No release,
installed signature verification, F5 certification or activation ran.

### 5.6 Complete raw Producer snapshot

Implemented §3.8 in the existing store owner; bootstrap imports the same two
basename constants from that owner. The first 36-case run exposed one fixture
assertion error: selecting an absent state root correctly preserved the old,
unselected database. The assertion now requires the selected database to stay
absent **and** the old one to remain. The extended 43-case family passed in
1.02 seconds. Three affected bootstrap/path-permission cases passed in 0.14
seconds; unchanged operational/Producer mutation suites were not repeated.

Independent static review identified two P3 boundary defects in the candidate:
oversized timeouts could escape as `OverflowError`, and BLOB-valued schema SQL
could escape as `TypeError`. Three new tests reproduced these failures. The
repair validates the timeout without overflow, clamps before multiplying and
requires TEXT schema SQL before loading it. The affected 21-case subset then
passed in 0.52 seconds: **46 distinct new history cases**, not 64. Review of
the corrections found no remaining blocker. Structural R1 findings were zero
and the policy projection check passed; the reviewed-source pin remains
unchanged and pending final review.

The existing guarded proof gained only a `producer-history` mode. It permits
the exact selected SQLite `mode=ro&cache=private` connection, retains the
pre-import mutation/private-read guards and emits aggregate results only.
Script SHA-256:
`d1f828c28ec01abe7a1d948ba3a294b0b189eb3fa427182049c0f20a7a7c1802`.
Run **`run-7o6co4z9`** completed in 20.917 seconds with exit 0 as UID 995/GID
985. The unchanged HTTP PID was 1432998; dependencies and reviewed policy
came from the selected release 53. This was an isolated hybrid process,
not a candidate deployment or a repeat of the F4 transition.

The new reader acquired all **7,040 receipt rows and 7,040 issuance rows**
from the selected schema-5 database in one logical transaction, totaling
17,426,177 field bytes. Receipt states were 7,024 committed, 11 rejected,
4 in progress and 1 available; 80 rows carried terminal bytes. No state was
filtered and none of these counts is a qualifying admission count. The
probe observed no private-read or data-write/execution attempts; 153 optional
import-time permission repairs were blocked. Python's audit does not cover
all SQLite WAL bookkeeping and is not claimed as an OS isolation boundary.

Candidate store source SHA-256 at the probe:
`1a60ea26f5419a7e8430624718adecc47be0acf460e1ebab593205ecda0ef858`.
Historical signatures, admission qualification, complete contract inventory,
common certification frontier, native Windows ACL proof and F5 activation
remain separate unfinished work. No production rows, services or release
selection were changed.

### 5.7 Exact historical publication acquisition

Implemented §3.9 without changing productive publication/current-selection
readers. The first run passed four cases and failed 27 during fixture setup:
intermediate test directories inherited the developer's group-writable
umask. The fixture now creates private directories/files; the reader's
permission requirement was not weakened. All 31 cases then passed in 0.12
seconds. Eight additional directory/link/mode/substitution cases passed in
0.09 seconds: **39 distinct new cases**, with no repeated suite total.

Independent static review found no product blocker. The structural checker
initially identified two unclassified owner functions. Both now have explicit
`store_owner` inventory entries; its conservative `store_write` capability
classification is retained even though these paths only acquire raw bytes.
No guard exception, productive authority or caller was added. Structural R1
then reported zero findings and the policy projection check passed. The
reviewed-source pin remains unchanged, pending final review.

The guarded proof acquired exact selected historical items through the new
reader, using the installed release 53 dependencies and unchanged HTTP PID
1432998. These are isolated hybrid reads as service UID 995/GID 985, not a
candidate deployment or signature verification. Candidate `contract_store`
SHA-256 throughout the probes:
`1b0e1f438952cfa69b7135be3dce490b3181dbeaf0e2b05eb99ab2c46f86cfb8`.

- `run-_p8ymamv` failed after 21.09 seconds: the oldest selected terminal's
  generation existed, but the explicitly requested V2 receipt directory did
  not. The cold `sign` import also attempted normal log initialization;
  the guard blocked it. No log write was permitted.
- `run-9wazsrch` retained that V2 failure and continued to the latest sample.
  The latter's exact V2 acquisition succeeded, including equality of the
  embedded and durable receipt. Sizes were 78-byte binding, 2,457-byte
  receipt, 6,925-byte manifest, 64-byte signature and 4,039-byte language state.
  The overall run remained `not_verified`, exit 1, after 20.635 seconds;
  the failing sample was not replaced to obtain a green result.
- `run-32_qq4x6` explicitly selected V1 for the first sample of a **new**
  Producer snapshot; it succeeded in 20.514 seconds, exit 0. Sizes were
  65-byte binding, 2,440-byte receipt, 2,784-byte manifest, 64-byte signature
  and 1,582-byte language state. Embedded/durable bytes and the generation
  content address agreed. The probe did not pin and report a sample identity
  across runs, so this result is not proof that it reread exactly the item
  rejected under V2. No automatic layout fallback was added to the product.

The final proof script SHA-256 was
`4980ab8a9ad87bda2e28f7ef74af3986f0ff152ef1637a393ceb42f5459b0e83`.
The last two runs separately recorded and **denied** one exact normal-log
initialization attempt and 153 optional metadata repairs. They observed no
private-key read or other data-write/execution attempt. Python audit limits
from §5.6 remain applicable. Review caught the cross-run sample-identity
limitation above; the report narrows its claim instead of rerunning a green
sample. Signature/context/policy verification, complete inventory, common
frontier and native Windows proof remain open. No production state changed.

### 5.8 Complete raw contract receipt inventory

Implemented §3.10 within the existing store owner. The initial family passed
52 cases in 0.24 seconds. Static review identified one P3 budget race: a file
growing after the first size observation could be materialized up to its
per-file maximum before the total byte limit refused the result. One new
test reproduced that failure (156 bytes read with 155 remaining). The shared
reader now receives `min(per_file_limit, remaining_total_bytes)`. Six new
cases, including metadata-only permissions/hardlink/FIFO boundaries, and four
affected existing cases then passed in 0.14 seconds. Eight overlapping
namespace-diagnostic cases passed after adding actionable error detail.

The first structural pass exposed conservative same-module leaf-name
propagation: a new nested helper called `read` made existing `stream.read`
callers inherit its capabilities. Renaming only that helper to
`acquire_history_bytes` left precisely three new owner scopes. These have
explicit conservative `store_write` classifications for the read-only owner
flow. No guard change, broad inventory reclassification or authority exception
was made. Final structural R1 findings were zero; policy projection, diff and
shell syntax checks passed. Source-review pins remain unchanged.

The installed observation first refused a real missing binding:

- `run-b40ywzz2`, 20.733 seconds, exit 1, script SHA-256
  `e6005eb677b640ef938c8cb3edcdf725d09447b6460f0a31bbaec19fd34c4dac`:
  `birth_history_namespace_invalid`, no partial success.
- `run-m7roerbp`, 20.613 seconds, exit 1, script SHA-256
  `757bb4bb828b49eb6cfe8e91f427bbd861073fca92a1adc85c6cc567abc5199a`:
  the added detail identified storage key
  `4e2feabf613fef06a63bddbf37396733f427cbeae5366aa75df4816d7f27611d`,
  missing `binding.json`, with no unexpected direct entries.

An exact metadata-only diagnostic found just a private empty `generations`
directory and a private single-link, one-byte `writer.lock`. The first
invocation was refused by the runner's argument grammar before execution.
Run `run-p3_48nz1` acquired those metadata but failed its final harness check:
a diagnostic variable had shadowed the source-hash dictionary. After renaming
that variable, `run-k9a4wvlt` succeeded in 20.327 seconds with script SHA-256
`34839f89c40a3d9d5c651bdf6833c32b03177d45942f71e62e74067ec7578e78`.
Neither metadata run read the lock byte, proved crash provenance or cleaned
the directory. Failed attempts are not successful installed proof.

Source inspection confirmed the pre-binding creation order. Independent
review approved the explicit opaque-namespace acquisition refinement in
§3.10, not a certification exception. Fourteen new shape/mode/race/budget
cases and two affected older cases passed in 0.23 seconds: **72 distinct new
inventory cases**, not the sum of overlapping runs. Final static review
found no blocker and repeated no tests.

The full refined inventory then succeeded in **`run-n8ghc_da`**, 24.509 seconds,
exit 0. Script SHA-256:
`83d15d657eeca9569602948ffdabb9ed558b3e8c767305c3986551a6c1045690`.
Candidate store SHA-256:
`dc8edad723f8946b4ed039fb0079b5f3a03c79857967a8e90d64deee169f4f34`.
It acquired 123 bound contracts, 194 generation entries, no retirement entry,
48 V1 and 6,976 V2 physical receipts, and one separately recorded empty-unbound
namespace with its NUL lock byte. All bound contracts had receipts. The
8,713 traversed entries carried 18,519,899 acquired binding/receipt/lock bytes.
These are raw acquisition counts, **not 7,024 authenticated or qualifying
admissions**. The opaque namespace remains to be reconciled by the certifier
under a common barrier, without inventing a historical contract identity.

The probe ran as service UID 995/GID 985 against the configured store with
release 53 dependencies and unchanged HTTP PID 1432998. One ordinary log
initialization and 153 optional metadata repairs were denied; no private-key
read or other data-write/execution attempt occurred. The inventory mode did
not connect to the Producer database. No current pointer, service, release,
production row or stored artifact was changed. Complete historical
authentication, cross-store consistency/frontier, qualifying counts, native
Windows proof and F5 certification remain unfinished.

### 5.9 Initial-context public keys

The candidate implements the opt-in selector and explicit `binding_kind`
from §3.11. Eleven new cases and nine affected existing selector cases passed
in 14.55 seconds. Three affected historical-author policy cases passed in
2.42 seconds. These runs do not add 23 new tests: the distinct new count is
11. Coverage includes exact previous-set/context/epoch pins, absent or altered
markers, initial/target ambiguity, changing chain frontier, forbidden private
readers and refusal to borrow the successor's Producer policy.

Independent static review found no blocker and repeated no tests. Boundary
findings remained zero and the contract-boundary policy check passed. The
compiled reviewed-source pins were not refreshed. Caller inspection confirmed
that these evidence readers have no ordinary bootstrap/routing caller, so
their outcomes add no startup, request-routing or user-permission gate.

The explicit `initial-public-history` mode succeeded in **`run-xsb16rrq`**,
23.235 seconds, exit 0. Script SHA-256:
`4c3d3c0dadb89a352f16384fca2ef75dddd84e00c380a8abab241d53280f36f1`.
Candidate prepared-root module SHA-256:
`49c71a46c0f98892516015387a20ae85209fe16b41d8b60fe809c1ae344d074d`.
The 53-transition authenticated chain selected only its first predecessor;
the already-proven transition targets were not probed again.

The resulting `initial_predecessor` public evidence bound context
`sha256:290264a3a057613006badeb399a92e12fd9bc9fc28f134db453e8ecf3b1cdcae`,
epoch `sha256:501fbcbd432a02b92e2c4e68511201fc12dc0d147f79e66d9666cc35d2f41657`,
and set `fac2e29592f936024a39cdfc1a2834e6ac6a3d48784511bc9834b51dd8e7fe8a`.
It contained two author verifiers, one Admission verifier and eleven
Producer namespaces. Material SHA-256 was
`9ca6dcd040cb12be97f433cd01a36862a29d98e58ce90d88ce8ef11ac45e606a`;
set document SHA-256 was
`e1b180595bd533d162bdf86cb2c7908801f317298d4c657e47fc4c81d6554212`.

The probe ran as service UID 995/GID 985 with installed release 53
dependencies and unchanged HTTP PID 1432998. Private-key reads, data-write/
execution attempts and log attempts were zero; 154 optional metadata repairs
were denied. Required head was
`sha256:e63bc648309c87f43ba9ba39a9f1a22456b0361f2bf14b707cfb8f0cb4f17743`
and installed reviewed-policy ID was
`sha256:d08440cdcf4e7db8bab178a611c9768d25795e6241f186da615984a2a7462660`.
No old context was activated or reprovisioned. This proves initial public-key
acquisition, not the predecessor's Producer policy, qualifying admissions,
a common cross-store frontier or F5 certification.

### 5.10 Historical Producer origin declaration

The unreleased author-only evidence API was renamed as specified in §3.12.
The single source/chain read now yields immutable author and executor-origin
declarations. Twenty-one new origin cases and 38 affected existing cases
passed in 14.47 seconds. Sparse and empty historical origin maps are preserved;
tests deliberately use declarations different from today's loaded table.
No unchanged inventory, terminal or low-level public-set family was rerun.
Boundary findings remained zero and the contract-boundary policy check passed.
Reviewed-source pins remain unchanged pending the final review.

Independent static review found no product blocker. It caught a probe
composition gap before execution: installed release 53 does not contain the
new `read_verified_distribution_file_v1` helper. The `producer-declarations`
mode therefore explicitly measures and overlays the candidate distribution
reader, in addition to the three previous public readers. The installed
guard/policy, ownership chain, public registries and secure filesystem remain
unchanged. Declared module origins are checked; no worktree search path,
fallback data or source-pin relaxation is introduced. This is an isolated
hybrid proof, not deployment or review of the entire candidate distribution.

The corrected mode succeeded in **`run-3bvun64e`**, 24.528 seconds, exit 0.
Script SHA-256:
`cdcac2f48f2d911326abaaf51fc0a68fd40f4e3e4cf9a8a605d15e6293495ca4`.
Candidate prepared-root SHA-256:
`8131b0a422c3a05924aa9bdc82576b82b7b503150e196c2f4057b5518ea09bc0`;
candidate distribution reader SHA-256:
`ca57ad89a74b22aec7aad0e800a0f37facaeca05124970c92a93b17592d280f1`.

The selected target context was
`sha256:13f78a4c9612ab7daaae5f2055f00d04ced7a28b42f96e4e654afb55896dd790`,
build `sha256:277b5c7ff596ad06416cd8946461146933af5d7f7a97a59b87f4a3403260b3f5`.
The exact source hash remained
`sha256:62d266497a6d2198ab152267084668f1c6173c99e309ce3ec80b5de6335901f9`.
Eleven Producer namespaces and seven historical origin declarations were
read. UID 995/GID 985, release 53, HTTP PID 1432998 and the required chain
head remained unchanged. Private reads, data-write/execution and log attempts
were zero; 154 optional metadata repairs were denied. These facts authenticate
the selected declarations, not qualifying acts or the complete N3 predicate.

### 5.11 Initial Producer-policy evidence limit

Independent source review ruled out treating the provisioner-build digest,
successor source-review or the unsigned predecessor descriptor as an implicit
historical Producer-policy signature. The descriptor inventories old source
bytes but its ID is not the previous-set ID signed by the initial dominant
receipt. Its cutover observation also does not prove unchanged policy over
every earlier act. No policy was inferred backward from a later release.

The remaining exact question was whether the already authenticated initial
material itself contains a Producer-table source record. The dedicated
`initial-policy-binding` diagnostic succeeded in **`run-q_c9k_p5`**,
24.403 seconds, exit 0, with the same script hash as §5.10. It used only the
three original candidate public readers and installed distribution logic.
It reread bounded material bytes through the owner under a shared Birth lock
and matched material SHA-256
`9ca6dcd040cb12be97f433cd01a36862a29d98e58ce90d88ce8ef11ac45e606a`
before examining the source records. There were 22 entries, 20 distinct
labels, and **no `executor_birth_producer_table_v1.py` entry**. The result
concerns those exact retained bytes, not an inference from today's catalog.

UID/GID, release, HTTP process and required chain head were unchanged; private
reads, data-write/execution and log attempts were zero, with 154 optional
metadata repairs denied. No original context was activated or source changed.

The initial context's public keys remain verified, but no sufficient original
Producer-policy source binding has been found in the examined evidence.
Keep affected acts explicitly unqualified until such proof is available or
an evidence-policy change is approved. This does not establish that the
five-act/two-producer threshold depends on those acts, invalidate later
authenticated declarations or disable ordinary services. Complete inventory
must retain the limitation rather than silently discard the records.

The signed Admission receipt's Producer hash proves Birth accepted those
bytes; it is not an independently recorded `producer/provenance` check in
`check_results`. The source has no such distinct check there. Using that
acceptance alone instead of historical scope proof would be an explicit
evidence-policy change, not a technical fallback implemented here.

### 5.12 Ordinary historical Producer binding

The pure owner join and the unchanged-format shared request-v1 codec from
§3.13 are implemented. **57 new cases passed in 0.41 seconds**. They cover
real signatures, immutable output, forged receipt/issuance columns, signed
but inconsistent facts, missing historical scope, key rotation, exact expiry
and existing 30-second tolerance, input bounds, malformed decoding, and UTF-8
request framing. The fixture's historical-context custody is explicitly a
test seam, not a live/source-authority proof.

Sixteen affected bootstrap factory cases passed in 1.06 seconds. The one
initial-installer catalog case passed in 1.25 seconds, traversing the other
factored request-ID site and existing publication/replay logic in the isolated
installation. These 17 existing cases are not additional new tests. Unchanged
inventory acquisition and terminal families were not rerun. Boundary findings
remained zero, contract-boundary policy and diff checks passed, and source-
review pins remained unchanged. Independent static review found no blocker
in the product, tests or the bounded installed probe; it repeated no tests.

The `producer-binding` probe succeeded in **`run-x83l0o7k`**, 23.458 seconds,
exit 0. Script SHA-256:
`d1d2e528b703ad311e6f3f064f8c0c90b6b9945d4bd7a01848795c07c5cfd49f`.
Candidate receipts-module SHA-256:
`220967be098e738fc407a0984f4898f5786d3fb5228df161dcdb951868fa292b`;
candidate Producer-store SHA-256:
`7271646b59a86d11178c7bfbaf852647f42c4cd726f79dc81bc5e4bd391381f7`.
The bootstrap candidate hash
`966c405faa0a3ba01f2bdbfc0b88aa5697e55f30f7d2274de56ef8739c2fd341`
was exercised locally, not overlaid in the installed probe.

The probe explicitly measured seven candidate owner modules: receipts,
distribution reader, context material, prepared set/root, Producer store and
contract store. Guard/policy, ownership chain, public registries and secure
filesystem remained installed release 53 dependencies. This is still a hybrid
candidate-reader observation, not an installed candidate release.

Unsigned terminal fields selected the largest row ID with an embedded
ordinary target-context Admission hint. The scan observed 69 such ordinary
hints, 24 referencing target contexts: **neither count is authenticated or
qualifying**. Exactly one sample was verified, with no fallback selection:
receipt row 6548 and issuance row 6548, namespace
`stack_reconcile:restart_sign_first`. Its authenticated identities were:

- Admission: `sha256:c7405c2f82292be79614a7bbbfb8de920700d3e774d18ee8ae0846a7e6d99e34`;
- Producer: `sha256:204435cd9bd3c4fff6cbbc80bbffc1a77ce3fe2faa07ecb36e83713ee73b0b34`;
- context: `sha256:73e9e3a0d34efd00e4ad27e5dbf7857bee23388e32d724163151bf8d33ebca70`.

The signed act time was `2026-09-15T14:15:51Z`, before the Producer expiry
`2026-09-15T15:15:51Z`. Verification succeeded after that expiry without
inventing a validity time. The signed contract/generation/context then selected
the exact historical V2 publication reader, whose independently reread durable
Admission bytes equalled the verified input bytes. No V1/layout fallback was
attempted. Terminal signature/outcome, full generation/source/journal predicate,
deduplication and qualifying counts were not inferred from this sample.

The probe ran as UID 995/GID 985 and observed release 53/HTTP PID 1432998 at
selection. Private-key reads and data-write/execution attempts were zero;
one ordinary log initialization and 154 optional metadata repairs were denied.
The only database connection was the declared logical read-only Producer
snapshot. As before, the Python audit does not claim zero native WAL
bookkeeping. No production row, service, authority or release was changed.

Next N3 work is terminal/protocol and exact publication qualification across
the complete retained inventory, with explicit exclusions and a common stable
frontier. No eligible-act or distinct-producer threshold is asserted yet.

### 5.13 Ordinary historical publication authentication

Implemented §3.14. The initial positive case failed on the missing API before
implementation. **55 new cases passed in 0.33 seconds**, including actual
Admission/Producer/manifest/terminal signatures, signed-but-inconsistent
facts, all new byte bounds, exact check projection, reuse without embedded
Admission, distinct old Admission and newer terminal keys, explicit V1 layout,
preexercise and nontechnical revisions. Today's manifest validation, store
root selection, operational request construction and SQLite access are denied
in the pure positive traversal. Historical context custody and acquisition
remain declared fixture seams, not claimed installation evidence.

Independent static review found no blocker and repeated no tests. Structural
R1 found zero findings; boundary-policy and diff checks passed. No source pin
was refreshed. Unchanged acquisition, Producer-join and operational-replay
families were not rerun: this slice adds inert functions without changing
those ordinary paths.

The installed `publication-binding` proof **`run-cmy6cjf5` passed**, exit 0,
24.808 seconds, as UID 995/GID 985. It used the same deterministic row 6548
from §5.12, adding authentication not previously exercised: durable manifest
signature/generation, complete terminal signature/state/hash/publication,
Admission fact equality and exact checks/source/journal cross-bindings.
Observed generation:
`sha256:55c81fe6664ec91f762c27419ef800be68a4f0faa692dd432fc63b44be6494f3`.
The authenticated outcome was `admitted`, lifecycle `active`, revision
`contract_revision`. The original source/journal were not reread, and this
sample is not a deduplicated qualifying count or a complete frontier.

This proof explicitly overlays eight measured candidate owner modules,
adding `executor_birth_operational` to §5.12's seven. Guard/policy, chain,
registries and secure filesystem remain installed release 53 dependencies.
HTTP PID 22974 selected release 53 and the previously authenticated required
head `sha256:e63bc648309c87f43ba9ba39a9f1a22456b0361f2bf14b707cfb8f0cb4f17743`.
It is a hybrid reader proof, not candidate deployment or current-policy
recertification. Private reads and data-write/execution attempts were zero;
one optional log initialization and 153 optional metadata repairs were denied.
No service restart, key access, published generation or DB mutation occurred.

Measured SHA-256:

- Proof script: `de830f90fa390929dd6c09f09c32021a825a7c0f187bd5f600c2d0cf1a5b68e3`.
- Contract store: `eacc911bbe1e7c864d6eb97b5b5f62660e89c2f10548e57d44f191cba233702d`.
- Operational owner: `712353ca809841fa762759f5a07d66d3e7931be8e21064a9fc4019e6682caa02`.

The next N3 boundary is historical V2 reattestation classification, followed
by complete-inventory reconciliation, deduplication and the common stable
frontier. The already tested ordinary protocol must not be imposed on valid
terminal-less reattestation records. N1/N2/N4–N8 and G9/G10 remain open;
neither F5 nor F6 is declared complete.

### 5.14 Consolidated historical reconciliation

Implemented §3.15 as one block. The acquiring owner shares the authenticated
chain observation and final reread across selected contexts. Exact public
source bytes/projections are cached only within that call. V2 identity uses
the same framing in the writer and historical reader without constructing
operational requests. Ordinary publication and V2 reattestation have separate
joins; the reconciler retains every unjoined receipt, Producer, issuance,
generation and unbound namespace. Technical counts are deduplicated by signed
receipt/request and exact contract-generation; conflicting identities cannot
qualify. Continuity requires the exact earlier authenticated act, with source
equality for V2 predecessors and transitive propagation of unresolved links.

Testing was consolidated, not repeatedly expanded into the full repository:
**232 affected/new cases passed in 23.55 seconds**, with 34 unrelated cases
deliberately deselected. After two review corrections, **58 focused cases
passed in 0.69 seconds** (57 consolidated history cases and one original V2
constructor case). These counts overlap and must not be added. The corrected
cases cover authority-transfer mutation, changed inheritance, symbol
rebinding, V2 source mismatch and child-first unresolved continuity. Structural
R1 reported zero findings; generated boundary policy, shell syntax and diff
checks passed. The compiled source-review pin was not refreshed.

The independent reviewer found and then confirmed fixes for two defects in
the candidate: partial AST-node matching missed intervening authority changes,
and V2 continuity omitted the authenticated source identity. A closed semantic
profile and the source comparison resolve them. The reviewer repeated no
tests and found no further blocker in the corrected delta.

Real read-only traversal **`run-9hz9nd5l`**, exit zero, completed in **32.367
seconds** as UID 995/GID 985, HTTP PID 22974, installed release 53. Its runner
script digest is
`b15214a7c33f9dec13e779554888546507ee6ef18d4a2d085d4473a944d2ecb9`.
Eleven explicitly measured candidate modules ran with the installed chain,
registries, secure filesystem and unchanged policy. The required head was
`sha256:e63bc648309c87f43ba9ba39a9f1a22456b0361f2bf14b707cfb8f0cb4f17743`.
The complete contract and Producer inventories were equal on final reread.
This double observation is not the still-required all-writer frontier.

Observed results:

- 123 contracts, 7,024 physical durable receipts, 7,040 Producer rows and
  7,040 issuance rows; none silently dropped.
- 62 context selectors occur in receipts; 53 have declarations through the
  selected authenticated chain. Nine selectors are outside that target set,
  affecting 520 receipts. The known initial-context limitation is included;
  the other selectors' provenance is not inferred from their presence.
- Exact historical reattestation scope was available for 15 contexts.
  Thirty-eight contexts lack a matching available bootstrap/intent source
  under this reader, affecting 4,636 receipts. This is evidence unavailable,
  not a diagnosis that those receipts or the ordinary service are corrupt.
- 1,868 acts passed their signature/protocol joins: 24 technical candidates,
  92 fully linked reattestations and 1,752 reattestations whose predecessor
  continuity is unresolved. The latter remain explicitly non-qualifying.
- The 24 technical candidates have two authenticated issuers:
  `builtin_contract_generator` and `stack_reconcile`. They meet the numeric
  candidate screen, **not the complete F5 certification**.
- Remaining observations include 56 unreconciled generations, 5,172 unjoined
  Producer rows and 5,172 unjoined issuance rows, plus one opaque empty
  namespace. Counts describe overlapping consequences, not distinct defects.

No private read, write or execution attempt occurred. One logger-init write
and 153 optional metadata repairs were denied separately, as in earlier
probes. SQLite's native WAL bookkeeping remains outside the Python audit
claim. No key, service, release or productive data was changed; original
temporary source/journal bytes were not claimed reread.

Next: reconcile the missing historical context/source evidence as a single
owner-level block, then finish the common frontier and certification/cycle/
ledger contracts. Candidate counts are now measured rather than speculative;
N1/N2/N4–N8, productive G9 and F6/G10 remain open.

### 5.15 Archived public artifacts close selected-chain scope gaps

The discriminating read-only observation `run-53j_dbtu` found exact public
bootstrap/intent sources in Git for **all 53 signed release records**. There
were 42 candidate public artifacts and five historical source variants;
every selected historical flow matched the already reviewed protocol profile.
Its runner digest was
`70a01178e18717d12b2a1622d6a976790f984d59b7f9f7d3ec3b7853da7473b8`.
Git was read as checkout owner UID 1000, with no checkout, filters, hooks,
network or repository mutation. Its bytes were not trusted until they matched
the exact path, size, role and hash in each authenticated distribution record.
The separate temporary availability mode was removed once permanent owner
tests and complete reconciliation covered this boundary; its root-run copy
remains retained with the evidence.

The existing multicontent owner now accepts bounded inert public-source
candidates: at most 256 artifacts, the existing per-source byte limit and
32 MiB total. No archive path or Git command enters product code. The chain
still supplies historical authority and is reread around acquisition.
Current-public-copy verification remains available when no supplied artifact
matches. No initial or unselected context borrows a successor's declarations.

**26 tests passed in 7.21 seconds**: 18 new archive-boundary cases and eight
affected multicontent cases, with 101 unrelated cases deselected. They cover
the exact historical address, role/size/path/content mismatch, duplicate signed
entries, malformed input, per-call budgets and the real owner traversal.
No independent review was added for this data-source extension: it creates no
new authority; the main agent reviewed the exact signed binding and reused the
completed critical protocol review. The previous whole history/protocol suite
was not repeated unchanged.

Complete installed reconciliation `run-aiforucs`, exit zero, took **32.294
seconds** after bounded public Git acquisition. Script digest:
`3240f070563577c77f5eb6d6df0eafcee43a7e64bdf1a4007d374080456ede34`.
Candidate `executor_birth_prepared_root.py` digest:
`b8d6199a166c912eb0be925943850b04f4c12d03d5b3ce3e118db0e000aee1a1`.
Other ten candidate module hashes, service UID/GID, PID 22974, release 53 and
required head match §5.14. Both complete raw inventories matched final reread.

All 53 selected target contexts now have exact historical scope evidence.
**6,504 acts reconcile: 6,480 reattestations and the same 24 technical candidates
from two issuers. No selected-chain scope or continuity gap remains.** This
closes the large source-availability consequence measured in §5.14 without
changing any receipt, key or historical publication.

Remaining observations are explicit: 520 receipts from nine other selectors,
48 unreconciled generations, 536 unjoined Producer rows and 536 unjoined
issuance rows, and one opaque empty namespace. The 536 includes consequences
of the 520 unresolved receipts and 16 other Producer states, not 536 newly
discovered failures. Their provenance/disposition and the common certification
frontier remain open. This is not F5 activation or completion of G8/G9/F6.

The service-reader audit recorded zero private reads and zero write/execution
attempts, with the same separately denied logger/metadata initialization.
Public Git subprocesses precede that audit and run without root privileges;
they are explicitly part of this probe, not concealed in the zero counter.
No production data or service was modified. The SQLite native-WAL caveat and
unavailable original temporary source/journals remain as previously stated.

### 5.16 Bounded runtime selection and installed code cleanup

Roberto instructed **applica** after approving incremental verification and a
small recovery set. The existing signed, root-protected required-head pointer
is the reusable runtime baseline; no new key, cache, service or certification
framework was introduced. This baseline is not the still-open F5 certificate.

`OwnershipChainStore.read_required_window_v1` reads exact named objects for
the current release and, when present, its immediate predecessor. It verifies
head/build/cutover/context bindings, current installed bytes and the unchanged
selector on final reread. Its separate `VerifiedOwnershipWindowV1` type does
not claim a full history audit. Bootstrap, prepared current/previous context
selection, readiness, transition policy and the head-crossing postcondition
now use this bounded path. An absent selector still requires proof of genuinely
initial state; damaged installed state does not fall back to initial authority.
The explicit historical readers remain unchanged.

The permanent test family constructs signed histories of different lengths,
removes all older executable trees and forbids archive enumeration. Every
ordinary read authenticates at most two records. Altered selected signatures,
context or current bytes and a concurrent selector change are refused. Damage
to unrelated history remains visible to the full auditor without preventing
ordinary selection. Transition selection covers release 2 and release 54 both
before and after head advancement, using retained positions rather than a
lifetime release number as a tuple index.

The existing release procedure now offers `prune` (preview by default), with
`--apply` and an explicit `--keep` count, default two. Successful `apply --cross`
requests the same maintenance automatically. It retains the current and recent
recovery trees, all future trees and references from processes, mounts and
systemd recipes. The existing deployment/startup/Birth locks protect selection;
attestation is checked before and after, and all targets pass metadata/layout
and descriptor checks before deletion begins. A maintenance refusal does not
turn a successful publication into a failed release. Only numbered executable
trees are eligible; signed archives, keys, Birth stores, models, user data,
worktrees and unresolved migration evidence are not deletion targets.

Validation, with overlapping runs reported separately rather than added:

- Bounded-reader family: **12 passed**; affected ownership/context/bootstrap/
  readiness/coordinator cluster: **381 passed, 1 skipped**. A missing-store
  exception-wrapper regression was found and fixed, not waived.
- Closing context-selection family after the high-sequence/readability change:
  **25 passed**. Closing full release-cycle suite: **197 passed in 1.65 seconds**.
  Its **17** retention/reference cases include process/mount/service scanning
  and unreadable-reference refusal; they are part of the 197, not extra tests.
- Structural R1: zero findings. Boundary-policy projection is regenerated;
  compiled source-review pins are intentionally not refreshed mid-increment.
  Native Windows, whole public matrix, installed candidate HTTP turn and
  productive runtime release remain unproven, not counted as passing.

Installed observation uses the existing root runner, then service UID 995 /
GID 985, with candidate public readers and unchanged installed critical
distribution/guard/filesystem dependencies. It is not candidate deployment.
The initial harness attempts exposed an import of a not-yet-installed history
helper (`run-69nzaw6e`) and the already-known old public-reader private-key
dependency (`run-vkpaqz6x`); neither was a successful proof. The harness now
omits the unrelated import and includes the already-reviewed public-only
prepared-reader prerequisites. Before cleanup, `run-miup7y3m` passed in 18.878
seconds with two authenticated contexts and no private reads or writes.

Actual code retention:

- Preview `run-eq5zdkx6`: selected release 53; only code trees 1–51 eligible.
- Apply `run-8mbmo7ww`: exit zero, removed exactly
  `/var/lib/metnos/executor-birth/releases-v1/00000000000000000001` through
  `00000000000000000051`; kept 52 and 53. Allocation fell from 1,766,080,512 to
  67,121,152 bytes: **1,698,959,360 bytes freed**. HTTP PID 22974 was unchanged,
  and installed startup attestation passed before and after. These removed
  trees were not moved to trash and have no retained deletion backup. Rebuilding
  code requires its versioned sources and dependencies; exact byte-for-byte
  restoration of every old tree is not promised. The signed metadata archive
  remains intact.
- Release-tool SHA-256:
  `ec5baca6b8c78af75a436c5393c031a43479385c436b2997f7f7bdb076fcd151`.
  Maintenance-runner SHA-256:
  `f24982882f72091e91fa5b6cbdeb01fd9660bd8fe458d036f2b0ebaa97bd51a0`.
- After cleanup, `run-v3mafe9w` passed in **18.609 seconds**, still two
  authenticated contexts, release 53 and required head
  `sha256:e63bc648309c87f43ba9ba39a9f1a22456b0361f2bf14b707cfb8f0cb4f17743`.
  Private reads and write/execution attempts were zero; 153 optional metadata
  repairs were denied. Chain-reader digest:
  `b6c633afff927c67f9deba385719d8a1577b63d8a703556b70cd1a9d13c02d12`;
  prepared-root digest:
  `ce595182bd3b95e7522c4576106295a849955f07af5fdc15bfbe28faef19fa8a`;
  proof-runner digest:
  `b8f8a217db254dd2853a688a03cc1728639702e2affbb66a263862309034776f`.
  The timing includes current-tree verification; it is not a performance
  benchmark or evidence that work is independent of current code size.
- HTTP health subsequently reported `ok=true`, `operational=true` and
  `maintenance_only=false`. Its separately disabled durable worker was not
  represented as healthy, nor was this health call counted as an E2E turn.

Bilingual architecture documentation was validated (99 indexable pages) and
published using the existing static-only deployment, which leaves the local
Tutor catalog untouched. Deployment:
`https://54a1462c.mykleos.pages.dev`. It states the development/deployment
distinction and the remaining administrative history traversal. The initial
static check found the generated interface map missing in this worktree;
the existing generator supplied it, with no generator or UI source change.

**Remaining boundary:** the standalone administrative preflight still calls
`_authenticate_fixed_ownership_snapshot_v1` for startup, certification and
candidate/cutover selection. It replays compact historical metadata, not old
executable trees. Adapt its existing capture/validator to current state and
the pending edge without importing candidate runtime authority or weakening
transaction/crash-phase bindings. Only then claim bounded history traversal
for every ordinary startup/release. Do not add another checkpoint authority.
The current runtime candidate is not installed; G8 certification, G9, F6/G10
and G11 remain open. Code retention here is not F6 collection of Birth data.

### 5.17 Administrative current-state capture — implemented, not deployed

Failure location: `_authenticate_fixed_ownership_snapshot_v1` always calls
the full archive capture, so service startup and every cutover reopen all
historical journals and signatures even after §5.16 bounded the runtime path.
The signed protected required head already selects a verified baseline.

Reuse the existing standalone validator, without importing candidate runtime
code or introducing another authority. Capture only exact objects for the
current head, its immediate predecessor and the optional pending successor.
Keep explicit scope in the internal snapshot so it is not misrepresented as
a full historical audit. Preserve the current distribution/cutover/head,
transaction/claim/context/attestation bindings and crash-phase coverage. Only
the first retained predecessor edge relies on the selected baseline instead
of recursively opening its own ancestors. Initial state and explicit audit
retain the full reader. Selected missing/altered bytes or changed snapshots
must fail; unrelated older history must not be enumerated or required.

The implementation uses the existing fixed-root owner and shared validator;
no authority, persistent cache or schema migration was added. At most three
heads, claims and transaction prefixes are captured by exact names. The
result carries `history_start_sequence`; absence means a full historical
snapshot. All productive administrative consumers share the bounded capture.

Evidence ledger (do not repeat these tests merely for another status update):

| Risk / check | Evidence | Rerun condition |
|---|---|---|
| Bounded reads, pending phases, CAS interruption, corruption, races, rollback | New parametrized window family: 23 passed; signed histories of 1, 3 and 12 releases, at most 47 distinct captured files; archive inventory forbidden. | Capture or shared reconciliation logic changes. |
| Existing signature, journal, material, launch and systemd bindings | Window + affected administrative families: 395 passed, 3 skipped, 1 source-pin failure in 5.76 s. After source review/pin alignment, only the failed test was rerun: 1 passed in 0.25 s. Thus 396 distinct passing cases, not 396 plus a full repeat. | A change crosses the corresponding boundary; Linux systemd skips are not claimed as evidence. |
| Productive authority boundary | R1: zero findings; policy projection check passed. | Productive graph/inventory changes; a pin-only edit does not add an authority edge. |
| Real installed standalone startup | `run-9e99obnh`: release 53, two heads, two transactions, 0.662 s; HTTP PID 22974 unchanged. Named persistent writes, private-key reads and historical enumeration each zero. | Relevant verifier behavior or installed release changes before the claimed release proof. |

The real proof ran under the startup shared lease using the candidate's actual
`_attest_service_startup_v1("service-http")` path. Its audit hook denied named
persistent writes/private-key reads/history enumeration; OpenSSL used the
existing sealed-memory descriptor path. This is a regression guard, not a
new sandbox claim. The first attempt (`run-l_hxjlzv`) was a harness refusal:
`subprocess.DEVNULL` was incorrectly treated as a persistent write. Exempting
only the null device fixed the harness; no product behavior was relaxed.
The successful source SHA-256 was
`bf30a4f6b455fb981b22ad1ff0c10decedb29036e48ede20596346db2b862d9f`;
script SHA-256 `098907b434004839cc783c575f6aecba150ddd108b486c36003756d97650f50d`.
Afterward only the compiled source-review literal changed in that product
file, producing SHA-256
`4c4f360d58f00854bd17795af75b30549daf70108a1dec8a6f15b8a607ea9a85`.
The executable proof script now pins that final source for future runs; the
earlier result is not relabelled as an execution of different bytes.

Current reviewed roots: private 768 sources,
`sha256:46e0deb6bc87853abb08b12171ada3b329fb324a8fb3d86aa48a75580ada8e09`;
public 756 sources,
`sha256:6205b7006cc32c09bb772b57962280681bf6af0a09160ef9fc778e8dabca8acf`.
The full archive reader still works for explicit historical reconciliation.
Separately, `_resolve_ownership_coordinator_at_v2` still inventories coordinator
journals in release-building/recovery paths; bounded administrative signature
verification does not yet make the entire release cycle independent of history.

Lean exit sequence: reuse the results above, integrate concurrent released
changes before publishing, seal the completed source set once, run the public
Linux/Windows matrix once, then perform the controlled installed transition
and its real postconditions. Do not add a result-cache framework or repeat the
historical F5 census during this release. G8/F5 certification, G9 and F6 remain
open; this change does not assert their completion.

Integration update: inherited installer fixes `f254aa9e`, `b7899c77`,
`2b068384` and the independently reviewed LRE change `67dfd970` were preserved
in this branch; only source-pin conflicts required resolution. No functional
LRE rewrite or repeated LRE suite was performed. R1 was rerun for the combined
graph and remained zero. The integrated source roots are now private 769,
`sha256:7c2669d441ff1609b1bb46bba8d2a0a941ea1fe5bccb633e70dbd8a73186bc6c`,
and public 757,
`sha256:4fb4ae8b4afa8289bcd4757e430b34395f3334ea47bf466a2385ef396bef7fee`.
Public Python inventory: 2,140 paths. The administrative code changed only
its source-review literal during this integration; proof behavior is unchanged.
Static IT/EN documentation was published using the existing Cloudflare Pages
procedure at `https://d6350b4b.mykleos.pages.dev`; both custom-domain pages
were reread and matched their local SHA-256. Tutor and services were untouched.

### 5.18 Candidate-local simplification and public-check repair

User direction: one procedure, candidate/affected-dependency scope, reuse of
valid results, and no outage of unrelated executors during certification.
The dedicated F5 authority implementation is paused while applying this
requirement to the existing productive path; no new key has been provisioned.

Observed cause: `_require_catalog_name_candidate` calls `current_contract` for
every other installed contract while holding the exclusive catalog admission
lock. That rehashes unrelated executable payloads just to obtain their signed
names. Existing tests even require an unrelated digest mismatch to block a
repair. The revised contract authenticates the current signed metadata for
name reservation, using the existing metadata-only CAS reader; executable
code verification remains mandatory for the candidate and at execution/load.
No registry, scheduler, persistent cache or additional certification service
is proposed. Existing expensive property/semantic checks are already outside
the catalog lock; the new concurrency proof must exercise that real boundary.

Public commit `3615219`, run `35068192560`, exposed prerequisite test drift:
the frozen acceptance-tree check omitted the exact portable import-path fix
from `329d51b3`; policy golden outputs omitted the two previously reviewed
read-only APIs; a release test still expected the invoking Python instead of
the fixed administrative interpreter; the installer entry inventory omitted
the newly integrated operator setup command. These are separate, diagnosed
fixture/catalog omissions, not reasons to disable checks. Repair and rerun
only these families before proceeding. The negative acceptance cases and
mutation checks remain enabled, and the exact import-path edge is pinned.

Implemented: the shared name-reservation path now reuses authenticated
metadata-only revision loading instead of `current_contract`; the missing-code
exception list and duplicate fallback parser were removed (44 net lines less
in the store). The common Birth facade and candidate checks were not replaced.
All **175 store tests** passed in two disjoint selections (8 focused and 167
remaining). Actual missing files, directory substitution and altered bytes
replace the old mocked-error cases. Names of hidden/retired contracts remain
reserved, and corrupt signed identity still prevents unsafe reservation.

The existing portable G4 end-to-end fixture was extended, not duplicated:
**2 cases passed in 0.66 seconds**. It uses the real intent adapter, sealed
publisher, signatures, store locks, postcondition reader and admitted-code
loader. During a deliberately suspended property-check boundary the installed
version is read under the real shared catalog lock and invoked successfully;
then either the candidate publishes or its failed check leaves the previous
generation callable. An unrelated damaged payload does not block the real
Birth commit and still fails its own executable-integrity check. This is an
isolated pipeline proof, not a live Synt/LLM/chat or sandbox-load benchmark.
The initial fixture attempt had zero applicable property cases; the wait now
wraps the actual property dispatch boundary and delegates the successful path
unchanged, rather than manufacturing a successful property result.

The public-check repair has **247 distinct passing targeted cases**, including
the final sealed-source assembly case (21.87 seconds). R1 is zero for the final
graph. Source roots: private 769,
`sha256:96b6049d43b197e6c420f6dd743073732757f24b86baf52d646f53cf36fcb838`;
public 757,
`sha256:309ebf4b73cd3001aa8ee55fac0a0e86fdae2745d453548411d315f6db8b9460`.
The no-skip/no-xfail check passes against the actual public export. Running
that public-only check in the private tree finds an old private runtime
fixture pin mismatch; that fixture is not exported and its pin was not waived
or changed. The public matrix must run against the actual export, not private
test-bootstrap assumptions. No production services were stopped, and no F5/F6
completion or runtime deployment is claimed.

Publication: local `830e36ca`, public `d99d2b8`; static documentation
`https://c8e932ef.mykleos.pages.dev`, with both custom-domain HTML hashes
matching the local files. Public baseline ancestry/evidence validation passed
on the actual committed public tree. Matrix `35070322598` passed all six
historical acceptance jobs. Its general Linux job exposed a further omission
in the installer-entry integration: the autonomous startup recipe pin still
described 39 legacy bindings, while the canonical catalog now has 40. The
recipe delta is exactly the operator-setup path under the existing install
operation; no new unit, executable command or privilege is introduced. Update
the reviewed recipe pin and the two exact retirement-count expectations, then
rerun the affected material/retirement families before another public run.

The repair passed **95 affected material/retirement cases** in 1.82 seconds;
all negative recipe mutants remain enabled. Final reviewed roots for this
correction: private 769,
`sha256:393f52c2ddab7fcef63364a759eb54962986f7436be626a211669bf1f5231b61`;
public 757,
`sha256:9ca3bb127ba7c0e454b6876d8dd8b55c3f5c293019a6094c10b3e088fd5e8d85`.
The earlier installed read-only window proof is not relabeled as a run of
these new bytes; the proof script now pins the current candidate for a future
read-only installed check.

Concurrent work: the LRE task installed release 54 and subsequently committed
`ee5df7f8` (undo fixtures and Tutor clarification). That later commit is not
yet integrated here. Its report records two refused admissions and pending
activation; do not overwrite its active release or working tree or claim
that the production release has fully converged. This increment remains a
development candidate until integration and controlled deployment.

## 6. Installed census and remaining freeze items

Continuation on 16 September, source `77b4a2d6`, public `6ecf2ac`:
run `35071136494` passed the portable Linux suite and all six historical
acceptance jobs. Its real Linux timer cell failed before acquiring maintenance
exclusion. The fixture builds a signed standalone startup prerequisite, not a
complete runtime installation, but `ReconcileLock()` now discovers its default
scope through the runtime ownership reader. That correctly refuses the
fixture's incomplete Birth authority. The test must bind its real lifecycle
lock to the same isolated service state and actual UID as its catalog lock;
it must not relax the productive reader, replace the lock with a fake, or
claim to test runtime installation discovery. Expected result: the existing
real systemd denial/transition/admission assertions execute unchanged.
The fixture now uses both real locks; its existing local test verifies both
service-state files and ownership. The five affected fixture tests passed in
0.59 seconds. The privileged systemd case must run in the disposable CI VM,
not against this production server.

The six concurrent LRE documentation pages from `ee5df7f8` were preserved
and republished together with the Birth pages at
`https://ec877f81.mykleos.pages.dev`; all eight affected custom-domain pages
matched local bytes. The LRE runtime increment is not yet integrated here.

The read-only administrative census uses the running HTTP process's selected
paths and drops to service UID 995 / GID 985 before SQLite access. It imports
no product code and reads no private keys or row contents. Connections use
`mode=ro`, `query_only` and bounded transactions; no schema migration runs.
This is a per-store observation, not a certification frontier or a migration.

Observed at `2026-09-15T18:58:22Z`, HTTP PID 1432998, run `run-iz718pz0`:

| Selected source | Observed state |
|---|---|
| `/var/lib/metnos-service/.local/state/metnos/executor_stats.db` | Service-owned 0600, user_version 0, DELETE journal; 198 statistics rows and 468 history rows. |
| Restrictions in that source | 58 nonempty deprecation markers and 44 nonempty archive markers; counts can overlap and are not proof of currently effective restrictions. |
| `/var/lib/metnos-service/.local/share/metnos/promoter.sqlite` | Service-owned 0600, user_version 0, DELETE journal; 8 rows: 6 archived, 2 review_needed, 0 promoted_grace, 0 unclassified states. |

Separate Producer metadata observation, run `run-l73u9c2o`, same HTTP PID:
`/var/lib/metnos-service/.local/state/metnos/birth/producer_receipts.sqlite`,
service-owned 0600, schema 5, WAL; 7040 rows (7024 committed, 11 rejected,
4 in_progress, 1 available). A metadata-only technical screen found 69
candidate rows across 3 issuer strings. **This is not 69 authenticated
admissions or proof of three qualifying producers.** Signatures, lifecycle,
capability binding, history completeness and eligibility still require the
new historical verifier. In-progress rows are not silently finalized.

The previous user's home also contains legacy databases; their mere presence
does not make them selected productive sources. Do not merge or migrate those
copies implicitly. Service selection and schema must be revalidated when
the actual cutover frontier is acquired.

Evidence scripts are held by the administrative runner with these hashes:
`bacff5047d8567a0e7f9707e4a2ba988475ca2e745f3cf4c2d1b484afddf2d44`
(legacy census) and
`963a6399f75807e05ee5a241225284ae484d10e27b4246c7d19626ed8ac159e8`
(Producer metadata). The worktree script supports both explicit modes;
do not rerun the legacy census just to repeat the Producer-only observation.

The first independent review of N1–N5 is complete; its historical-evidence
decision is now approved, and the public-reader prerequisite has local tests
and bounded review in §5.1. The protocol distinctions still require owner
implementation. Still required before freezing the
remaining new G8 interfaces: exact certification/ledger wire schemas and registry
installation/revocation protocol; exact routing profile/case manifest;
complete writer inventory and persistent-barrier lock order; epoch-v3 DDL,
association/disposition record and cutover recovery states. These are open
technical work, not automatically approved implementation details or a claim
that the complete G8 contract is already frozen.

### 6.1 Source writer census refinement — N6–N8

The source census includes the following retirement obligations in addition
to the handover's owner table. No installed writers have been disabled.

| Boundary | Confirmed detail and consequence |
|---|---|
| Aging owner | `executor_aging._open` creates schema; registration, invocation, aging, feedback and unarchive paths write the statistics/history tables. A helper described as a read must use `_open_read_only` after retirement. |
| Direct statistics SQL | `change_applier.apply_dedupe_executors` and the create/dedupe rollback handlers use `PATH_USER_STATE/executor_stats.db`, ignoring `METNOS_EXECUTOR_STATS_DB`. When these differ, census both exact paths as distinct sources; do not merge their rows by name. |
| Loader | Both the aging DB mtime and `lifecycle_override_map` influence the catalog; ordinary loads also call `register`. Removing only writes would leave a legacy decision reader. Removing only the reader could expose formerly restricted capabilities. |
| Promoter owner | `promoter_state` reads call `ensure_schema`, which executes DDL and can add columns. Those are not read-only historical views. Include grace expiry, rollback, review, archive, digest notification and channel acknowledgement writers. |
| Direct promoter SQL | `admin.promotions_review._mark_review_to_pending` bypasses state helpers. A barrier only around the public promoter API misses it. |
| Effects before SQL | Dedupe writes `executor_aliases.json` before its statistics update; rollback can move code or remove aliases first. Merely rejecting the final SQL does not stop the earlier effect or false-success response. Retire/replace the complete entry path. |
| Existing maintenance | `contract_cutover_guard` owns catalog-admission then service-lifecycle exclusion and fresh stopped-service evidence. It is Linux/systemd-specific and a live session, not a crash-persistent all-writer barrier. Reuse its ordering; do not claim it alone freezes standalone SQL writers after a crash. |
| Epoch schema 2 | Migration identity is unique by table name alone; retries require every row still unresolved. Version 3 must distinguish source identity and immutable preservation from later association/disposition. Its codec must preserve SQLite values, including malformed legacy names, instead of silently dropping rows or coercing historical values. |

The migration must cover already-open SQLite connections as well as new
opens. Database-level write refusal can cover DML that bypasses Python
helpers, but does not by itself establish durable service startup inhibition,
source-file identity or retirement of side effects preceding SQL. Those
contracts remain explicit freeze items; no partial barrier is implemented or
advertised as complete.
