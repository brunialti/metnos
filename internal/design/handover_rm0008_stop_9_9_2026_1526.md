# Handover operativo RM-0008 — stop richiesto il 9 settembre 2026

Aggiornamento: **9 settembre 2026, 15:26–15:28 CEST (Europe/Rome)**.
Questo documento prevale sui precedenti checkpoint e sugli estratti di chat.
È un documento interno: contiene percorsi e identificativi operativi, non va
pubblicato sul repository pubblico. Non contiene password o token.

## 1. Ordine attuale dell'utente e punto di arresto

Ultima richiesta: «crea un handover dettagliato per il lavoro da fare. chiederò
a un altro agente. fallo subito e ferma attività appena possibile, senza danno».

Le attività di modifica, attivazione e analisi sono state fermate. Gli agenti
paralleli sono stati avvertiti. Non avviare automaticamente altri cutover,
riavvii o reboot sulla base delle autorizzazioni precedenti: l'ultima istruzione
è fermarsi e consegnare. I servizi applicativi vengono lasciati in esecuzione.
Il workflow GitHub già avviato può terminare normalmente; non è stato annullato.

Il processo di completamento è terminato con exit 78, **non è ancora in corso**.
I processi di diagnostica e di avvio dei quattro servizi sono terminati.
All'ultimo controllo non risultavano unità `metnos-rm0008-*` in esecuzione.
Non c'è un comando di recovery sospeso in attesa di password o input.

## 2. Situazione reale, in poche parole

**La chat è tornata operativa. RM-0008 non è completamente chiusa.**

La nuova Release 2, con cookie semantici e correzioni di manutenzione, è stata
costruita, firmata, installata e selezionata. Il percorso produttivo è arrivato
alla pubblicazione della nuova head, ma ha fallito l'ultima attestazione
amministrativa: stato **HEAD_REQUIRED / fase 5**, non `PREFLIGHT_VERIFIED / 6`.

La verifica locale di avvio HTTP sulla nuova head è invece riuscita. Sono
stati avviati normalmente i quattro servizi, **senza bypassare i loro controlli
di avvio**, senza rimuovere firme e senza dichiarare conclusa l'attestazione.

Fotografia nativa delle 15:26 CEST:

| Servizio | PID | Stato | Avvio CEST | Riavvii automatici |
|---|---:|---|---|---:|
| `metnos-http.service` | 644146 | active/running | 15:23:50 | 0 |
| `metnos-playwright.service` | 644215 | active/running | 15:23:51 | 0 |
| `metnos-durable-worker.service` | 644147 | active/running | 15:23:50 | 0 |
| `metnos-telegram-daemon.service` | 644218 | active/running | 15:23:51 | 0 |

- HTTP `http://127.0.0.1:8770/agent/health`: 200, `ok=true`,
  `operational=true`, `maintenance_only=false`, worker `ready` e disponibile.
  Heartbeat osservato `2026-09-09T13:26:37Z`.
- Browser `http://127.0.0.1:8771/health`: 200, Chromium connesso, contratto
  caricato/corrente allineato, provider configurato, nessuna sessione attiva
  né apertura/approvazione/fattore in attesa.
- I quattro processi usano il Python gestito `bb5cf399…/bin/python`.
  `systemctl show WorkingDirectory` riporta `/`: questo da solo NON dimostra
  il percorso Python dei moduli caricati. Non è stata eseguita un'ispezione
  in-process dei moduli dopo il riavvio.
- Il modulo cookie nella Release 2 selezionata è byte-identico a quello testato:
  SHA256 `a754e941d406e0e5a63e0746309fcbf768542286ee5a0d5d27c5822107078aa3`.
- **Non è stata ancora eseguita una nuova prova chat/Telepass dopo questi avvii**.
  Non attribuire alle sole health una prova end-to-end del login.
- Nessun nuovo Telegram «cookie pronti» è stato inviato. Una notifica di
  recupero di base era stata inviata nella mattinata, per il recupero precedente.
- Reboot reale NON effettuato. Boot ID ancora
  `0510813b-e999-4904-b277-e6723f99846f`.

Il target complessivo e i controlli amministrativi non sono dichiarati sani:
`check-all`/attestazione completa incontrano il difetto descritto sotto.
Non confondere quattro servizi operativi con certificazione completa o prova
di riavvio dell'intera macchina.

## 3. Difetto aperto: causa precisa, non un generico problema di permessi

Il completamento autentico ha prodotto, in ordine:

```text
EXACT_SIGNED_SUCCESSOR_VERIFIED sha256:608c3a4590de2d56e9a702bde7f21943a7d103cd7f7ba80f5591e08f3c141059
SUCCESSOR_AUDIT_OK units 12 ... quiescence composite_health
SUCCESSOR_MAINTENANCE_HELD
EMERGENCY_HELPER_RECONCILED original
SIGNED_GROUP6_INSTALLED
SUCCESSOR_REFUSED OwnershipCoordinatorError birth_ownership_recovery_required preflight publication
```

Il rifiuto esterno viene da
`runtime/executor_birth_ownership_coordinator.py:5044`,
`_publish_preflight_for_crossing_v2`, chiamata nel crossing 5→6.
La sonda nativa in sola lettura ha poi isolato la causa:

```text
PREFLIGHT_DIAG_ERROR operational PreflightError birth_ownership_preflight_invalid administrative TCB signed binding
FRAME preflight.py 14144 _attest_operational_preflight_v1
FRAME preflight.py 8806 _bind_administrative_tcb_v1
FRAME preflight.py 8671 _bind_administrative_tcb_core_v1
PREFLIGHT_DIAG_OK http_startup
```

Contraddizione concreta individuata nel codice e nel descriptor N2:

1. Il builder in `install/executor_birth_distribution_release.py:480` ricava
   il Python amministrativo da `os.path.realpath(sys.executable)`.
2. Il build è stato eseguito con il Python gestito, perciò il descriptor firma
   `/var/lib/metnos/python-envs-v1/bb5cf3991b4cc15b3ea3f5ece123dc3d725f77545b5f7db67ba709ba6197e8e9/bin/python`.
3. Il verificatore cattura sempre il Python del sistema operativo, attraverso
   `PYTHON_LINK = /usr/bin/python3` e `_administrative_links_v1` (riga 8173).
4. `_bind_administrative_tcb_core_v1`, righe 8632–8671, pretende che il percorso
   Python catturato e quello firmato nel descriptor siano identici. Non lo sono.
5. La preparazione non ha verificato preventivamente questa uguaglianza, così
   la contraddizione è emersa soltanto **dopo** la nuova head.

La diagnostica nativa conferma la clausola TCB; la singola differenza di path
è confermata da codice/descriptor. Non è stato ancora emesso un censimento
nativo di tutti i confronti booleani/hash della clausola: farlo senza scritture
prima di scegliere la correzione definitiva. Non assumere che non esistano
ulteriori differenze solo perché questa è già sufficiente a causare il rifiuto.

**Cambiare interprete al comando di rilancio non cambia `PYTHON_LINK`** e non
risolve la contraddizione del descriptor già firmato. Non rilanciare alla cieca.

Direzione minima proposta dall'analisi, NON implementata:
distinguere il Python fisso del TCB OS dal Python gestito firmato del catalogo;
mantenere tutti i confronti dei digest del prerequisito e verificare il Python
gestito come target esterno firmato. Il ciclo di verifica dei target esterni
esiste già alle righe 8688–8734. Non eliminare l'intero controllo TCB, non
cambiare semplicemente `PYTHON_LINK` (i suoi hash includono il percorso), non
inventare attestazioni. Servono test di questa distinzione e un controllo
preliminare che rilevi incoerenze prima dell'arresto dei servizi/pubblicazione.

Resta da progettare e verificare il **raccordo conservativo con la head N2 già
pubblicata e i suoi artifact immutabili**. Una patch temporanea in memoria che
faccia emettere l'attestazione lasciando il vero helper incapace di `check-all`
non sarebbe una soluzione definitiva. Non riscrivere descriptor, head,
prerequisito, certificato o storia firmata per far passare i controlli.

## 4. Ripresa prevista dal prodotto e limiti dei vecchi script

Il prodotto contiene già `_resume_required_transition_v2` in
`install/birth_authority_provisioner.py:5266`.
`complete_transition_cutover_v2` la invoca prima di ripetere le fasi preparatorie.
Con head pubblicata prende i lock deployment → startup → maintenance e ripete
**solo** l'attestazione 5→6, senza rifare Producer, G6 o la pubblicazione head.
È una capacità di recovery, non un motivo per ritentare senza correggere la causa.

L'helper consente l'avvio locale di servizi autenticati già a fase 5:
`_select_ownership_epoch_v1` seleziona `sequence >= 5`; l'assenza di attestazione
finale è obbligatoriamente rifiutata quando si dichiara fase 6. La prova HTTP
nativa di questa possibilità è riuscita prima dei normali avvii delle 15:23.

Il wrapper corrente
`/tmp/metnos-rm0008-complete-continuity-successor-20260909.py` è stato scritto
per il primo passaggio N1→N2 e fa prima un audit del predecessore N1.
**Non assumerlo direttamente riutilizzabile adesso che N2 è già selezionata**:
il suo audit iniziale può non essere adatto alla recovery 5→6. Il prossimo
agente deve leggere lo stato corrente e usare il percorso autentico di ripresa
con audit coerente, preservando l'evidenza firmata salvata. Non duplicare le fasi.

Non eseguire `/tmp/m8`, script RM-0008 di settembre 5/6/8 o il vecchio
`complete-successor-20260909.py`: riguardano tentativi diversi.
Non rieseguire neppure il withdrawal del vecchio N2 sul namespace attuale.

## 5. Artifact correnti e impronte da non confondere

### Release 2 attuale, selezionata ma non attestata a fase 6

- Root: `/var/lib/metnos/executor-birth/releases-v1/00000000000000000002`.
- Source ID: `sha256:a9327fa4e620ee1b3d26fce365ad1223a9a9a8a7677499c25bf3bf048b71d769`.
- Build ID: `sha256:608c3a4590de2d56e9a702bde7f21943a7d103cd7f7ba80f5591e08f3c141059`.
- Descriptor SHA256: `99832ad05a66cc5075407892296e9af0e412719539b669ed8339588acb9cdc79`.
- Helper live e helper firmato: `35b3dc13800799b5dd2278600f9968b78ad19f4a8d7b1c8a64e7d80ca0c15d90`.
- Helper live: `/usr/libexec/metnos/executor-birth-v1/preflight.py`.
- Evidenza del build salvata root 0400, directory root 0700:
  `/var/lib/metnos-admin/rm0008-continuity-evidence-20260909/distribution.json`
  e `distribution.sig`.
- SHA payload: `ad3bbaa4258e3397f9b39598fb7fdf3b13f0e72468a2c894355189871e7c6e67`.
- SHA firma: `49e5e49f1305f3ff80b1c7b94320d839352175b28eafa932bcad2aba09872591`.
- Request ID/head ID nuovi non sono stati stampati dal comando fallito:
  ricavarli dalla selezione autenticata e dal journal attuale, non da vecchi log.

### Release 1 precedente, conservata

- Root: `/var/lib/metnos/executor-birth/releases-v1/00000000000000000001`.
- Build: `sha256:0f2354b537edcc8e790bd8f53d2cf245d290e24b9dd1dc7545391b97af44b862`.
- Source: `sha256:157c75be4e059f304e41abc57414445b75218fe7c8d1f58ce7a3430a68c68637`.
- Head: `sha256:469df47af79517e225d380a7a318f9cc17fb8747a0193e8d583d908bde7ed8b5`.
- Request: `sha256:59f0a756bbf65e46f34f5bcb4edafeff44c1788410ab1123ccbc7a1614e0cf26`.
- Cutover: `sha256:4fbdd1fdc283edd6fb895081c43e2559c25d792cc03e86044d62e7de505271ca`.
- Stato storico: fase 6 `PREFLIGHT_VERIFIED`.
- Set: `61d47c62d75d25bf16732017e4d32b6a7b72ef465d105d83b36b25dc7dbc8a4f`.
- Context: `sha256:8df4f2b7481809f3c8eafed3947c413bd430e3a78eb37a952e487fb89d470266`.

### Script di questa continuazione (solo riferimenti; leggere prima di usare)

| File in `/tmp/` | SHA256 | Esito già ottenuto |
|---|---|---|
| `metnos-rm0008-build-continuity-successor-20260909.py` | `626f269e00751405eeb47039e75e173e0589712fec9dfb807599ef1a68e819be` | build-only riuscito |
| `metnos-rm0008-complete-continuity-successor-20260909.py` | `93d1de7093343c3c7ce57c54fbc16c8591c75184a646137b9613e6a8b3eee20d` | audit riuscito; complete fermo a fase 5 |
| `metnos-rm0008-reconcile-continuity-helper-20260909.py` | `3eaae876594e6841a078339597bf9644a26551213a59ce48865689ed6c4073ef` | 53 test; raccordo eseguito dentro il vero G6 |
| `metnos-rm0008-withdraw-n2-20260909.py` | `9571aa3c604f3daadc8c5061e98535a0746130cf0d80f8d73d6ca07c395bfdc7` | 40 test; audit/withdraw vecchio tentativo riusciti |
| `metnos-rm0008-probe-current-continuity-20260909.py` | `1f5429e97b0813f027b677542c5965e2ff0cd3338f752e8508db8522c915c572` | 122/122 in sola lettura |

Diagnostica dell'errore attuale:
`/tmp/metnos-rm0008-diagnose-preflight-20260909.py`. Legge l'helper pin 35b3,
prova attestazione operativa e avvio locale HTTP, stampa solo causa e stack;
non avvia servizi né emette ricevute. Se l'helper cambia, rifiuta intenzionalmente.

`/tmp` non è uno storage garantito dopo reboot: prima di un reboot preservare
gli strumenti ancora necessari in un luogo interno appropriato, verificandone
contenuto e impronte. Non considerarli comandi permanenti di prodotto.

## 6. Cosa è stato recuperato e conservato, senza cancellazioni

Il vecchio tentativo N2, diverso dalla Release 2 attuale, aveva build
`sha256:3878c006f041536370eae081bb9b51b4153713a0af506d08dbd16e622a31a514`,
source `sha256:72109fe3c63166767c2e8f0c22fc0b16fe1e9ae5064dd078e4a82408962c6c4c`
e request `sha256:de846f6a33a54f7c72e304da7945744a85b54bf794e6a93d9198dcccfe9eeea7`.
Si era arrestato a PREPARED nella riattestazione di
`core:change_files_format/manifest.toml`, con `property_runner_unavailable`.

Prima del nuovo build sono stati spostati quattro oggetti esatti, sotto lock
deployment → startup → Birth, con rinomina atomica senza sovrascrittura e
verifica di byte/inode/metadati prima e dopo ogni mossa:

1. PREPARED coordinator → `prepared-transaction`;
2. Birth journal `.birth-provisioning-v2.txn.7f1879c1f0cbd48c899e56d552ac7627`
   → `unselected-birth-journal`;
3. vecchia release non selezionata → `unselected-release-2`;
4. successor claim collegato alla vecchia head N1 → `successor-claim.json`, ultimo.

Archivio root 0700:
`/var/lib/metnos-admin/rm0008-withdrawn-n2-20260909`.
Non è stato modificato dopo il recupero. Non cancellarlo o ripristinarlo sopra
la Release 2 corrente. Nessun servizio fermato durante questo withdrawal.

Sono rimasti al loro posto source ricevuta, ambiente Python, vecchio set
`9aca8b451d47f78059329771be686a979fb5d968e3ecf520eae7b8f3596e5f1d`, ricevute
parziali e database Producer. La richiesta terminalmente rejected
`sha256:d56bb4d40d2b795d754f89bc7d2629f2b8bac4292cdcda1b17a315a7534c9d59`
NON è stata cancellata, riaperta né riscritta. La nuova versione è un diverso
contesto autentico, non un falso replay della richiesta fallita.

Il journal N1 completato era già stato conservato alle 14:20 sotto Birth come
`.birth-provisioning-v2.completed.7231f5ab98d54ea94ccbbcfcea3478a1`.
Non è autorità runtime; contiene materiale riservato da non stampare.

L'helper d'emergenza precedente cb7f1b8 è stato conservato dal raccordo G6 in
`/var/lib/metnos-admin/rm0008-successor-20260909/preflight.repaired.py`.
Il raccordo ha ripristinato i byte originali N1 solo sotto i tre lock e poi ha
invocato il vero G6, che ha installato il nuovo helper firmato 35b3. Il raccordo
non ha prodotto firme o attestazioni. Ulteriore backup precedente:
`/var/lib/metnos-admin/rm0008-preflight-cbe320-before-availability.py`.

## 7. Codice implementato e prove da riusare

Worktree privato: `/opt/metnos/.claude/worktrees/rm0008-reboot`, branch
`codex/rm0008-reboot`, HEAD di base `8519edb1` con modifiche ereditate.
Leggere `CLAUDE.md`, `CLAUDE.mutabile.md` e `/opt/metnos/internal/AGENTS.md`.
Controllare sempre `git status`: non fare reset/checkout del lavoro dell'utente.

Export congelato: `/tmp/metnos-continuity-export.5MXSbT`.
Pin Python privato:
`sha256:ea14bca0522b3a1048e6cd38467d9bfee38147c496be380065a8145e08d92f4e`.
Pin pubblico/export:
`sha256:9cd1bb441ff4c1b91745bcc9e482be6df415ad93b92faac9e2e3d7d7ad981de8`.
Censimento export: 1768 file, 202 directory, 32271700 byte, permessi 644/755;
digest lista file `992c51ec5fe44139d1c80acf1c0cee583f3e57c745bf97196c7b752a2df5a579`.
L'identità receiver corretta usa account **`metnos`**, non `metnos-service`.
Il secondo nome è la directory home, non l'account.

### Continuità per componenti già ammessi e invariati

Modifica limitata a quattro moduli, circa +196/-7 righe:
`runtime/executor_birth_bootstrap.py`, `executor_birth_reattestation.py`,
`executor_birth_shadow.py`, `executor_birth_transition_receipts.py`.

Verifica ricevuta precedente firmata, generazione/byte attuali, identità
candidate/request, contesto precedente e risultato effettivo. Per componenti
invariati usa prova di continuità autenticata; controlli dinamici esplicitamente
N.A. con prova precedente, non un falso PASS né first adoption.
Ricevuta mancante ricade nel percorso rigoroso; contraddizioni rifiutate.
Conserva lifecycle ACTIVE/PREEXERCISE e non riscrive ricevute vecchie.

Test dedicati: `tests/portable/test_executor_birth_current_continuity.py`.
Prova nativa in sola lettura: tutti i **122** current effettivi passati,
missing 0/refused 0. Non ha invocato Producer, firmato nuove ricevute o mutato
database: letture/lock esistenti e snapshot temporanei privati, rete bloccata.
Il nuovo completamento ha poi superato il vero percorso produttivo delle
ricevute fino a G6 e alla nuova head: il precedente blocco property runner non
si è ripetuto. L'ultimo errore è distinto ed è il binding TCB sopra descritto.

### Lifecycle dei journal completati

`install/birth_authority_provisioner.py`: tre piccoli helper archiviano solo
un predecessore realmente completato/fase 6 da `.txn` a `.completed`, sotto
lock esistente; nessun journal attivo → no-op. Non viene usato come nuova
autorità. Test N1→N2→N3, crash/replay, inodi e materiale riservato preservati:
`tests/portable/test_birth_authority_journal_lifecycle_v2.py`.

### Cookie semantici

`runtime/playwright_sidecar/cookie_privacy.py`, integrazione in
`credential_injection.py` e `session_broker.py`; prompt IT/EN
`runtime/prompts/{it,en}/sites_cookie_resolution.j2`; workload locale
`sites.cookie_resolution`.

Niente lessico multilingue hardcoded: osservazione strutturale limitata e
classificazione semantica tramite modello locale. Massimo 3 pannelli,
16 controlli/pannello, 4 decisioni, 2 click, 6 secondi. Nessun LLM se non viene
osservato un ostacolo. Destinazione limitata al rifiuto dei cookie facoltativi;
DOM cambiato, controllo insicuro o ostacolo sconosciuto bloccano prima delle
credenziali. Non è un executor CAPTCHA e non amplia l'autorità del login.

Limiti noti: DOM principale; non dimostrato su iframe/shadow DOM o navigazione
articolata nelle preferenze. 38 test su Chromium reale passati, con pagine
sintetiche e classificatore di fixture; precedente prova modello reale
multilingue 6/6. Queste non sono la nuova prova end-to-end su Telepass.

### Esiti delle suite

- Privato: 2400 passed / 47 skipped, 157.50 s.
  `/tmp/metnos-continuity-private-workflow-20260909.xml`.
- Pubblico finale corretto: 2404 passed / 47 skipped, 164.86 s.
  `/tmp/metnos-continuity-public-final-20260909.xml`, SHA256
  `6a4afe18bd259805483492afca99340d35eb8dca354a539d742c6293c11d5897`.
- Manifest pubblico: 7/7;
  `/tmp/metnos-continuity-public-manifest-20260909.json`, SHA256
  `c505d400679781e7d36bb32aa38430706cff13f60aa3e25ba66fd2fd22ff46a5`.
- GII pubblico filesystem/index/export: zero rilievi; policy non indebolita.
- Raccordo helper: 53 test; withdrawal conservativo: 40 test.

I test non dimostravano il caso concreto di doppio interprete amministrativo:
aggiungere quel caso preventivo, non ripetere migliaia di test come sostituto
della verifica del punto di guasto reale.

## 8. Turno chat che non funzionava: analisi già conclusa

Turno `901ab8ad5fb44a7a`, sessione browser
`083bd8fa719c7b6764a182891023870a`.
Traccia ridotta: `/tmp/metnos-sites-901ab8ad-redacted.json`.
Screenshot copiato in directory temporanea privata e visto dal principale:
`/tmp/metnos-turn-901ab8ad.JG3Tm5/login-timeout.png`.
Originale nella directory dati servizio:
`sites-shots/host/083bd8fa719c7b6764a182891023870a_1788944635972.png`.

Open 09:01:50 UTC → discovering 09:01:51 → timeout/close 09:03:56.
Sequenza open_sites → login_sites, nessuna action intermedia. Nello screenshot
finale homepage Telepass oscurata dal banner «Che biscotti vuoi?» con
«Personalizza / Solo necessari / Accetta tutti / X». Nessun form di login o
CAPTCHA visibile. Dimostra che il banner era ancora aperto al timeout, non la
sua struttura iframe/DOM o il motivo preciso della mancata selezione.

Nessun credential_use/submit registrato; l'evento è scritto dopo entrambi i
fill, quindi non dimostra l'assenza assoluta di una digitazione parziale.
`out_of_scope` è classificazione secondaria, non causa del timeout browser.
L'analisi non ha ricontattato il sito e non ha usato credenziali dell'utente.

Quando riprenderà il lavoro, dopo verifica live del percorso nuovo, far
riprovare il turno all'utente e analizzare il **nuovo** turn ID: non ricominciare
l'analisi della sola vecchia schermata né dire «risolto» basandosi sui test.

## 9. Pubblicazione GitHub e documentazione

Clone pubblico: `/opt/metnos/.claude/worktrees/rm0008-ci-public`, branch
`codex/rm0008-ci-repair`.
Pubblicazione ordinaria fast-forward già completata; `main` remoto riletto:
`7132b4db6fc36695b9528d2167ed99c255d2f833`.

Due commit, note inglesi come chiesto dall'utente:

- `b6466540385d87e7f1e977475f4d986483ef88c8` —
  Preserve verified executor admission during successor maintenance.
- `7132b4db6fc36695b9528d2167ed99c255d2f833` —
  Keep portable fixture imports outside the frozen acceptance bootstrap.

Il primo incremento modificava due righe del conftest appartenente alla
baseline storica di acceptance. Il gate lo ha rilevato **prima del push**.
È stato ripristinato esattamente il conftest storico e corretto l'import dei
soli fixture/test coinvolti, senza indebolire policy o gate. Prodotto/pin/export
immutati; quattro file di apparato test pubblico differiscono ora dall'export.

CI avviata sul commit corretto:
https://github.com/brunialti/metnos/actions/runs/34356573475
Ultimo stato osservato `in_progress`; **non è stato verificato l'esito finale**.
Il vecchio run `34346999399` era tutto verde, ma riguarda il vecchio commit
`4e3dea4`, non usare quel verde per il nuovo incremento.

Rapporto completo: `/tmp/metnos-continuity-public-preparation-20260909.md`.
Le note di installazione inglesi sono aggiornate nell'incremento. Le guide
pubbliche IT/EN e il loro deployment finale per questi ultimi cambiamenti sono
ancora da completare. Ultima distribuzione guide nota, precedente a queste
ultime modifiche: Pages `67524226`. Non dichiarare deploy documentazione nuovo.
L'utente richiede pubblicazione incrementale solo dopo GII, note Git in inglese,
e distribuzione effettiva dei doc pubblici, non solo commit.

## 10. Ordine consigliato per il prossimo agente, dopo ripresa autorizzata

1. Leggere questo handover e i file normativi del repo; ricontrollare stato
   reale senza fermare i quattro servizi. Le health e i PID qui sono una
   fotografia, non una garanzia permanente.
2. Priorità alla chat: verificare che il percorso browser/login carichi il
   codice della release selezionata; prova innocua via chat, quindi turno
   richiesto dall'utente. Non fermare un turno attivo per certificare la macchina.
3. Eseguire censimento TCB **read-only**: selezione corrente autenticata,
   descriptor, prerequisito, path/hash catturati. Non stampare materiale di
   journal riservato o credenziali. Riprodurre il confronto divergente con un
   test mirato prima di modificare codice.
4. Correggere la distinzione tra Python OS e Python gestito in modo minimo,
   con un piano coerente di recovery della head N2 esistente. Aggiungere test
   positivi/negativi e il controllo preventivo prima del cutover. Non rifare
   l'architettura, non accrescere il sistema con reset generalizzati.
5. Solo dopo causa risolta e percorso preventivo provato: ripresa autentica
   5→6 sotto i lock già previsti, senza riemettere vecchie ricevute o ripetere G6.
   Verificare helper reale, attestazione persistita, stato fase6 e avvio delle
   unità amministrative, non soltanto i servizi applicativi.
6. Verificare manutenzione/configurazione/aggiornamento, poi boot readiness e
   reboot completo quando non interrompe prove/turni e con recovery preparata.
   Ricontrollare HTTP, Telegram, browser, worker/LRE, modello llama.cpp, timer,
   target e una vera richiesta dopo il reboot. Il vecchio contatore riavvii
   llama era storico; non confonderlo con nuovi crash.
7. Controllare il nuovo CI, aggiornare documentazione IT/EN e roadmap in modo
   veritiero, passare GII e pubblicare incrementalmente con note inglesi;
   distribuire anche le pagine. Non marcare RM-0008 chiusa prima delle prove.
8. Notificare qui e su Telegram solo disponibilità realmente verificata,
   distinguendola dalla chiusura della certificazione. Il canale esistente è
   `runtime/channels/telegram.py`, `TelegramChannel`, destinatario host nello
   store protetto. Per una sola notifica usare `state_path=False`, ambiente
   dati/config/state/workspace dell'account `metnos`, senza polling o modifica
   offset. Non stampare token/URL API contenenti token.

## 11. Sicurezza operativa e fonti storiche

- Account reale `metnos`: uid 995, gid 985, home `/var/lib/metnos-service`.
- Utente legacy `roberto`, installazione di lavoro `/opt/metnos`.
- Usare l'ambiente da `metnos_xdg_layout_v1(account).environment()` per evitare
  workspace sbagliati. Il Python di sistema non ha tutte le dipendenze del
  prodotto: non cambiare interprete o installare pacchetti per tentativi.
- I comandi nativi systemd/rete richiedono uscita dal sandbox tramite il
  meccanismo approvato. `Failed to connect to bus: Operation not permitted`
  o uid 65534 osservati dentro il sandbox NON provano un guasto/owner reale.
- Autorizzazione del tool e autenticazione sudo sono due livelli diversi.
  Non riportare la password già fornita in documenti, file, comandi o log.
  Non chiedere all'utente di rilanciare uno script senza prima leggere l'esito
  del suo ultimo lancio. Non usare strumenti generici per aggirare permessi.
- Vietati reset distruttivi, pulizie globali e modifiche arbitrarie di receipt,
  head, claim o journal. Gli archivi descritti sono recuperabili e riservati.
- Non pubblicare il worktree `/opt/metnos` in blocco: contiene modifiche e
  materiale dell'utente. Usare il clone pubblico e scope verificato.
- Documentazione storica:
  `/opt/metnos/internal/reports/rm0008-maintenance-analysis-20260909.md` e
  `/opt/metnos/.claude/worktrees/rm0008-reboot/internal/design/handover_rm0008_manutenzione_9_9_2026.md`.
  I loro checkpoint precedenti descrivono stati superati: questo handover
  è il punto di ripresa dopo lo stop richiesto.

**Esito consegnato: servizi applicativi disponibili, nuova release selezionata,
attestazione amministrativa finale ancora irrisolta, nessun reboot eseguito.
Attività fermate su richiesta dell'utente; non dichiarare RM-0008 conclusa.**
